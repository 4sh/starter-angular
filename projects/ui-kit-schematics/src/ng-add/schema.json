{
  "$schema": "http://json-schema.org/schema",
  "$id": "UiKitNgAdd",
  "title": "ng add @4sh/ui-kit-schematics",
  "type": "object",
  "properties": {
    "skipInstall": {
      "type": "boolean",
      "default": false,
      "description": "Ne pas lancer `npm install` après avoir modifié package.json."
    },
    "all": {
      "type": "boolean",
      "default": false,
      "description": "Copie tous les composants disponibles, sans prompt."
    },
    "skipComponents": {
      "type": "boolean",
      "default": false,
      "description": "Pose la fondation seule. Les composants se copient ensuite avec `ng generate @4sh/ui-kit-schematics:add`."
    },
    "skipStorybook": {
      "type": "boolean",
      "default": false,
      "description": "Ne pas poser de Storybook : ni story ni MDX à côté des composants, ni configuration `storybook/`, ni devDependencies de preview. Par défaut, le Storybook est posé."
    }
  }
}
