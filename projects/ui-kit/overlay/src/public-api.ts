/*
 * Public API Surface of ui-kit/overlay
 *
 * Helpers shared by the components that open a CDK overlay (select,
 * autocomplete, datepicker, menu, popover…) or a full-screen overlay
 * (modal, drawer, bottom-sheet, sidebar, image preview).
 */

export * from './lib/body-scroll-lock';
export * from './lib/close-on-navigation';
