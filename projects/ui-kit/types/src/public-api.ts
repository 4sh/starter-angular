/*
 * Public API Surface of ui-kit/types
 *
 * Shared, type-only building blocks used across several `ui-*` components
 * (e.g. `UiLevel`). No runtime code — a component that only needs a type
 * import pays nothing for depending on this entry point.
 */

export * from './ui-level';
