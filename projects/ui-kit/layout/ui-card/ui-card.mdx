import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import * as CardStories from './ui-card.stories';
import { ConfigTable } from '../../../../storybook/blocks/config-table';

<Meta of={CardStories} />

# ui-card

Conteneur **flexible** qui compose des régions optionnelles : média, en-tête
(titre + sous-titre), contenu et pied. Chaque région n'est rendue que si elle
porte du contenu — aucun wrapper vide, donc aucun espacement fantôme.

Surface neutre (tokens `global`) : fond `--global-background-default`, texte
`--global-text-default`, bordure `--global-border-subtle`, ombre
`--shadow-default-md` (variante `elevated`), rayon `--radius-lg`. Light **et** dark
via les modes de variables.

<Canvas of={CardStories.Complete} />

## API

<ArgTypes of={CardStories} />

## Système de slots

Le cas courant ne demande **aucun import** : le contenu par défaut passe par
`<ng-content>`, et le titre / sous-titre acceptent les inputs `header` / `subheader`.
Les slots riches utilisent des **directives marqueurs** (attribut ou élément) à
importer dans le composant hôte.

```html
<ui-card header="Titre" subheader="Sous-titre">
  Contenu de la carte (slot par défaut)
</ui-card>

<ui-card>
  <img uiCardMedia src="…" alt="…" />
  <span uiCardTitle>Titre <ui-tag label="New" /></span>
  <span uiCardSubtitle>Sous-titre</span>
  Contenu riche…
  <div uiCardFooter>
    <ui-button label="Annuler" level="low" />
    <ui-button label="Valider" />
  </div>
</ui-card>
```

<table className="doc-table">
  <thead>
    <tr>
      <th>Slot</th>
      <th>Marqueur</th>
      <th>Raccourci</th>
      <th>Rôle</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Média</strong></td>
      <td><code>[uiCardMedia]</code> · <code>ui-card-media</code></td>
      <td>—</td>
      <td>Visuel pleine largeur en tête (clippé aux coins arrondis).</td>
    </tr>
    <tr>
      <td><strong>Titre</strong></td>
      <td><code>[uiCardTitle]</code> · <code>ui-card-title</code></td>
      <td><code>header</code></td>
      <td>Titre (typo <code>title</code>).</td>
    </tr>
    <tr>
      <td><strong>Sous-titre</strong></td>
      <td><code>[uiCardSubtitle]</code> · <code>ui-card-subtitle</code></td>
      <td><code>subheader</code></td>
      <td>Sous-titre atténué (<code>--global-text-muted</code>).</td>
    </tr>
    <tr>
      <td><strong>Contenu</strong></td>
      <td><em>slot par défaut</em> (<code>&lt;ng-content&gt;</code>)</td>
      <td>—</td>
      <td>Corps de la carte.</td>
    </tr>
    <tr>
      <td><strong>Pied</strong></td>
      <td><code>[uiCardFooter]</code> · <code>ui-card-footer</code></td>
      <td>—</td>
      <td>Footer de la card</td>
    </tr>
  </tbody>
</table>

<Canvas of={CardStories.Default} />
<Canvas of={CardStories.ContentOnly} />
<Canvas of={CardStories.WithMedia} />
<Canvas of={CardStories.RichTitle} />

## Dimensions et conteneur

La carte n'a **aucune dimension intrinsèque** : c'est son conteneur qui la
dimensionne, et les dimensions se posent sur l'élément `<ui-card>` lui-même,
jamais sur un élément interne.

<table className="doc-table">
  <thead>
    <tr>
      <th>Parent</th>
      <th>Largeur par défaut</th>
      <th>Avec <code>fluid</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Bloc (<code>div</code>, colonne de grille…)</td>
      <td>100 % du parent</td>
      <td>Identique</td>
    </tr>
    <tr>
      <td><code>display: flex</code></td>
      <td>Largeur du contenu</td>
      <td>Toute la largeur disponible</td>
    </tr>
    <tr>
      <td><code>display: grid</code></td>
      <td>Largeur de la piste</td>
      <td>Identique</td>
    </tr>
  </tbody>
</table>

En **hauteur**, la boîte visible (fond, bordure, ombre) remplit toujours son
hôte : une rangée de cartes de contenus inégaux s'aligne sans CSS
supplémentaire, et `height` posé sur `<ui-card>` s'applique bien à la boîte.

<Canvas of={CardStories.Fluid} />
<Canvas of={CardStories.EqualHeight} />

## Mise en page du contenu (`contentClass`)

Plutôt que d'envelopper votre contenu dans des balises supplémentaires pour gérer sa mise
en page, `contentClass` vous donne accès au conteneur structurel existant.
En y passant vos classes utilitaires (grille, flex, alignements), vous organisez directement
l'intérieur de la carte sans alourdir le DOM.

```html
<ui-card header="Trafic" contentClass="flex-x flex-gap-x align-justify">
  <div>1 284</div>
  <div>99,8 %</div>
</ui-card>
```

<Canvas of={CardStories.ContentClass} />

## Contenu full-bleed (`contentFlush`)

Par défaut le contenu porte la gouttière de la carte. `contentFlush` la **retire**
pour laisser le consommateur maîtriser sa mise en page : bande média / couleur
edge-to-edge, tableau pleine largeur… La gouttière reste disponible via la variable
CSS `--ui-card-gutter` (à ré-appliquer sur les blocs qui doivent rester en retrait).

```html
<ui-card header="Titre" [contentFlush]="true">
  <div class="flex-y" style="--flex-gap-y: var(--units-md)">
    <div class="banniere"></div> <!-- pleine largeur -->
    <p style="padding-inline: var(--ui-card-gutter)">Paragraphe regouttiéré…</p>
  </div>
</ui-card>
```

<Canvas of={CardStories.ContentFlush} />

### Quel levier choisir

<table className="doc-table">
  <thead>
    <tr>
      <th>Besoin</th>
      <th>Levier</th>
      <th>Gouttière</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Disposer le contenu (ligne, colonne, grille)</td>
      <td><code>contentClass</code></td>
      <td>Conservée</td>
    </tr>
    <tr>
      <td>Contenu edge-to-edge (bande média, tableau pleine largeur)</td>
      <td><code>contentFlush</code></td>
      <td>Retirée, à ré-appliquer via <code>--ui-card-gutter</code></td>
    </tr>
    <tr>
      <td>Les deux : mise en page <em>et</em> edge-to-edge</td>
      <td><code>contentClass</code> + <code>contentFlush</code></td>
      <td>Retirée</td>
    </tr>
  </tbody>
</table>

## Footer

```html
<!-- actions groupées à droite -->
<div uiCardFooter class="flex-x flex-gap-x align-right" style="--flex-gap-x: var(--units-sm)">
  <ui-button label="Annuler" level="low" />
  <ui-button label="Valider" />
</div>

<!-- tags à gauche, action à droite -->
<div uiCardFooter class="flex-x align-justify" style="flex:1;align-items:center">
  <div class="flex-x flex-gap-x"><ui-tag … /><ui-tag … /></div>
  <ui-button label="Action" />
</div>
```

<Canvas of={CardStories.WithFooter} />
<Canvas of={CardStories.Complete} />

## Variantes

`variant` pilote uniquement le traitement de la **surface** (les états interactifs
ne sont pas gérés : la carte est un conteneur).

<Canvas of={CardStories.Outlined} />
<Canvas of={CardStories.Elevated} />
<Canvas of={CardStories.Flat} />

## Accessibilité

<table className="doc-table">
  <thead>
    <tr>
      <th>Règle</th>
      <th>Détail</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Sémantique</strong></td>
      <td>Conteneur neutre par défaut. Renseigner <code>ariaLabel</code> ou <code>ariaLabelledBy</code> promeut la carte en <code>role="region"</code> (repère de navigation).</td>
    </tr>
    <tr>
      <td><strong>Titre</strong></td>
      <td>Le slot titre est un simple bloc stylé : pour une hiérarchie de titres réelle, projeter un <code>&lt;h2&gt;</code>/<code>&lt;h3&gt;</code> via <code>uiCardTitle</code>.</td>
    </tr>
    <tr>
      <td><strong>Média</strong></td>
      <td>Fournir un <code>alt</code> pertinent (ou <code>alt=""</code> si décoratif) sur l’image projetée.</td>
    </tr>
    <tr>
      <td><strong>Contraste</strong></td>
      <td>Couleurs issues des tokens sémantiques <code>global</code> (WCAG AA), déclinées light / dark.</td>
    </tr>
  </tbody>
</table>

## Theming

Variables locales en tête de `ui-card.scss` (spécifiques à ce composant). Voir le
[fonctionnement de la configuration](?path=/docs/components-configuration--docs).

<ConfigTable of="ui-card" />
