/*
 * Public API Surface of ui-kit/ui-image
 *
 * Includes `ui-image-preview`: it renders the source `ui-image` has already
 * resolved, so the two live in one entry point (a separate one would be a cycle).
 */

export * from './lib/ui-image-preview';
export * from './lib/ui-image';
