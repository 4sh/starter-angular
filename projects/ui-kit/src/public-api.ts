/*
 * Public API Surface of ui-kit.
 *
 * This root entry point stays intentionally minimal: every component is
 * exposed through its own secondary entry point (e.g. `@4sh/ui-kit/base/ui-icon`)
 * so consumers only pull in what they import.
 */

/** Package version, kept in sync with `package.json` at publish time. */
export const UI_KIT_VERSION = '0.0.1';
