/*
 * Public API Surface of ui-kit/ripple
 *
 * `UiRipple` / `UiRippleScope` directives + `provideUiRipple()`. The wave's own
 * styles live in the package's global stylesheet (`styles/base/_ripple.scss` →
 * `styles.css`), shipped with the design tokens.
 */

export * from './lib/ui-ripple.engine';
export * from './lib/ui-ripple';
