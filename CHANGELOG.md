# Changelog

Toutes les évolutions notables du package publié **`@4sh/ui-kit`**
(`projects/ui-kit`) sont documentées dans ce fichier.

Le format suit [Keep a Changelog](https://keepachangelog.com/fr/1.1.0/) et le projet adhère à [Semantic Versioning](https://semver.org/) (voir [VERSIONING.md](./docs/VERSIONING.md)).

> Tant que la version commence par `0.x`, l'API est considérée instable : une
> release `MINOR` peut contenir des ruptures, documentées ici. Le passage en
> `1.0.0` figera l'API publique.

> Un changement qui ne touche que Storybook, la démo, la CI ou la documentation
> n'a pas sa place ici : sans impact sur le tarball publié, il n'y a pas de
> release à documenter (voir « No release without a tarball change » dans
> [VERSIONING.md](./docs/VERSIONING.md)).

## [Unreleased]

### Added

- **`@4sh/ui-kit/ripple` : onde de pression, activable à trois portées.** Inspiré de
  l'option [Ripple de PrimeNG](https://primeng.dev/ripple), avec une activation qui ne se
  limite pas au couple « global / une directive par élément » : `provideUiRipple()` équipe
  toute l'application, `[uiRippleScope]` un sous-arbre, `[uiRipple]` un élément précis.
  `data-ripple="on"` fait onduler un élément non interactif, `data-ripple="off"` exclut un
  élément et son sous-arbre, et les portées ne se cumulent jamais : une pression produit
  une seule onde, celle de la déclaration la plus interne.
  - **Une portée déléguée coûte un écouteur, pas un par contrôle** : `pointerdown` passif,
    posé hors zone Angular (une pression ne déclenche aucune détection de changement).
    Rien n'est créé ni mesuré avant la première pression ; la couche de rognage est ensuite
    conservée sur l'élément, chaque vague est un `<span>` retiré à la fin de son animation,
    et quatre vagues au plus coexistent sur un même élément.
  - **Aucune option d'apparence en TypeScript** : couleur, opacité, durée, courbe et échelle
    passent par les custom properties `--ui-ripple-*`, qui héritent. Le défaut
    `currentColor` reste lisible sur n'importe quel fond, marque et mode, sans un seul token
    de couleur en dur ; les autres défauts pointent sur `--transition-*`.
  - **L'onde est rognée par la boîte de l'hôte et suit son `border-radius`** : la couche est
    un enfant en position absolue, donc ni l'`overflow` de l'hôte ni son anneau de focus ne
    sont touchés. `<ui-button uiRippleScope />` équipe le `<button>` interne d'un composant
    du kit, là où la directive ciblée peindrait la boîte carrée du custom element.
  - Décorative, donc invisible à l'assistance : `aria-hidden="true"`, aucune cible de
    tabulation ajoutée. Supprimée sous `prefers-reduced-motion: reduce` et sous le
    coupe-circuit `data-motion="off"` du kit (la vague n'est alors jamais créée).
  - **Douze composants sont équipés d'office** : `ui-button` (donc aussi `ui-button-split`,
    `ui-speed-dial` et tout composant qui instancie un bouton), `ui-menu`, `ui-context-menu`,
    `ui-sidebar-menu`, `ui-tabs`, `ui-paginator`, `ui-select`, `ui-autocomplete`,
    `ui-datepicker`, `ui-toggle-button`, `ui-segment-control` et `ui-file-upload` (zone de
    dépôt). Activer l'effet ne demande donc aucune retouche des templates du projet. Le
    périmètre part de celui de PrimeNG (actions, navigation, choix dans une liste) et
    l'élargit aux contrôles à surface franche ; il laisse de côté les contrôles dont le
    changement d'état est le retour visuel sur une cible trop petite, les surfaces de
    révélation, les ✕ de fermeture et les liens en ligne.
  - **Ils se déclarent par un attribut, pas par une directive.** Chacun porte
    `data-ripple="on"` sur le contrôle natif qu'il rend, et le sélecteur délégué par défaut
    est précisément `[data-ripple="on"]`. Aucun de ces neuf entry points ne dépend de
    `@4sh/ui-kit/ripple` : un projet qui n'active jamais l'effet n'en embarque pas une ligne,
    et une portée globale reste à un seul écouteur au lieu d'un par contrôle.
  - **Entrée `ripple`** (`true` par défaut) sur ces douze composants pour couper l'effet sur
    une instance, activation globale comprise. Elle est nécessaire et pas seulement pratique :
    `ui-select`, `ui-autocomplete`, `ui-datepicker`, `ui-menu` et `ui-context-menu` rendent
    leurs cibles dans un overlay CDK, donc hors du DOM du composant, où un `data-ripple="off"`
    posé sur un conteneur ne les atteindrait pas.
  - `UI_RIPPLE_INTERACTIVE_SELECTOR` fait onduler en plus tous les contrôles cliquables de
    l'application : `provideUiRipple({ selector: UI_RIPPLE_INTERACTIVE_SELECTOR })`.

## [0.7.0] - 2026-08-27

### Added

- **`ui-toggle-button` — bouton qui retient son état pressé**. Inspiré de l'API [ToggleButton de PrimeNG](https://primeng.dev/togglebutton), adaptée à l'idiome signals/tokens du kit : un `<button>` natif portant `aria-pressed`, branché aux formulaires via `BaseFieldControl` (Signal Forms, `[(ngModel)]`, reactive forms, autonome). Libellés et icônes peuvent différer par état (`onLabel`/`offLabel`, `onIcon`/`offIcon`, avec `label`/`icon` en repli commun), `iconPos` place l'icône, et sans libellé le bouton devient carré. `trueValue`/`falseValue` laissent le modèle porter n'importe quelle paire de valeurs, comme `ui-toggle` et `ui-checkbox`.
  - **Mode groupe, toujours multi-bascules.** Renseigner `options` bascule le composant en groupe : un bouton par option, `role="group"` sur l'hôte, modèle = **le tableau des valeurs pressées**. Options primitives / objets quelconques (`optionLabel`/`optionValue`/`optionDisabled`/`optionIcon`) ou forme riche `ToggleButtonOption` (`selectedIcon`, `disabled`, `ariaLabel`) ; `allowEmpty` interdit de vider le groupe, `orientation` l'empile. Il couvre le cas « N bascules → un seul tableau » que le `SelectButton` de PrimeNG traite dans un composant séparé.
  - **Le choix exclusif est délibérément absent du mode groupe.** `ui-segment-control` le porte déjà, et le porte mieux : il annonce un `radiogroup` avec un focus roving, là où un groupe de boutons `aria-pressed` dit « bascule » et non « un parmi N ». Offrir les deux revient à laisser choisir entre la bonne et la moins bonne sémantique pour un besoin identique — donc `options` implique le mode multi, sans input `multiple`. Le recouvrement restant entre les deux composants est purement visuel (boutons espacés contre piste soudée), ce qui est la raison d'être des deux.
  - **Deux axes de couleur, pas une explosion de variantes** : `level` (high/low/success/warning/error) colore l'état **pressé**, `variant` (outlined par défaut, filled, ghost) dessine l'état **relâché**. Les deux tirent du même contrat 5 états des jeux `actions.*`, donc ajouter un niveau ou une variante ne demande aucun CSS propre. L'état pressé reste toujours plein : il ne peut pas être confondu avec l'état relâché.
  - Tailles `small`/`default`/`large`, `fluid` (les boutons d'un groupe se répartissent la largeur), `rounded`. Templates `iconTemplate` et `contentTemplate` (contexte `{ checked }`) pour un rendu piloté par l'état interne, `itemTemplate` pour un bouton de groupe.
  - **Le nom accessible ne doit pas suivre l'état** : le libellé visible change à chaque bascule, donc un lecteur d'écran annoncerait un bouton différent à chaque prise de focus. Un avertissement en mode dev réclame un `ariaLabel` stable dès que `onLabel` et `offLabel` diffèrent, en icône seule, et sur un groupe sans nom.
  - **`required` n'émet pas `aria-required`** : l'attribut n'est pas autorisé sur `role="button"` (axe `aria-allowed-attr`, impact critique), et une bascule n'a pas de notion d'« obligatoire » en ARIA. Le piège n'est pas théorique : Signal Forms écrit `required` sur l'hôte dès qu'un champ porte `required()`, donc l'attribut apparaissait sans que le consommateur l'ait demandé. L'obligation se porte dans le libellé, l'échec de validation dans `aria-invalid`.
  - L'état `invalid` est peint via une **propriété personnalisée héritée** plutôt qu'une règle par état : la bordure d'erreur survit ainsi au survol et au maintien enfoncé, quelle que soit la spécificité des jeux de couleur.
  - 15 hooks `--ui-toggle-button-*` (rayon, rayon pilule, espacement icône ↔ libellé, espacement entre boutons du groupe, bordure, anneau de focus, hauteur / inset / taille de police par taille).

- **`ui-speed-dial` — bouton flottant qui déploie ses actions autour de lui** (FSHSP-173). Inspiré de l'API [SpeedDial de PrimeNG](https://primeng.dev/speeddial), adaptée à l'idiome signals/tokens du kit. Le déclencheur et chaque action sont des instances de `ui-button` (`rounded iconOnly`) — les couleurs viennent entièrement des tokens `actions.*`. Les actions sont pilotées par un modèle déclaratif `UiSpeedDialItem[]`, le sous-ensemble feuille de `UiMenuItem` (`command`/`routerLink`/`url`, `icon`, `disabled`) : un menu existant peut nourrir un dial sans être remanié.
  - Quatre dispositions, partageant un même calcul d'arc : `linear` (empilée le long de `direction`), `circle` (anneau complet), `semi-circle` (demi-anneau centré sur `direction`) et `quarter-circle` (quart d'anneau dans le coin `direction`) — `radius` (optionnel, dérivé de la taille du contrôle sinon) fixe le rayon des trois dispositions en arc. `mask` assombrit la page derrière le dial ouvert avec les mêmes tokens que le voile de `ui-modal` ; `hideOnClickOutside` (vrai par défaut) et `Échap` le ferment.
  - Le déclencheur porte `aria-haspopup`/`aria-expanded`/`aria-controls` ; les actions ouvertes forment **un seul arrêt de tabulation** (`role="menu"`, roving tabindex, même principe que `ui-menu` et la barre d'outils de `ui-editor`). Une flèche pressée sur le déclencheur **fermé** l'ouvre et place le focus sur la première action. Les actions ne sont **rendues qu'ouvertes** (`@if`) : fermées, elles n'existent pas dans le DOM, donc rien à exclure de la tabulation ni d'`aria-hidden` à maintenir. Sur une disposition en arc, la boîte de la liste — vide entre les actions — ne capte plus le clic à la place du déclencheur qu'elle recouvre (`pointer-events` réservé aux actions elles-mêmes).
  - `itemLevel` (`low` par défaut) distingue le niveau sémantique des actions de celui du déclencheur (`level`) : une action au même niveau que le déclencheur se lisait comme un second bouton principal plutôt qu'une option révélée.
  - Réduit volontairement par rapport à PrimeNG : un seul étalonnage de touches clavier commun à toutes les dispositions plutôt qu'un mappage par cas, et `showTooltips` (booléen) qui simplifie `tooltipOptions` — la position suit automatiquement le sens de déploiement.
  - 5 hooks `--ui-speed-dial-*` (empan, décalage d'apparition des actions, rayon par défaut de l'arc, palier d'empilement, assombrissement du masque).

- **`ui-editor` : outils `indent`/`outdent`** (FSHSP-160). Deux nouveaux `EditorButtonTool`,
  passthrough direct sur `document.execCommand('indent'|'outdent')` — pas de marker ni de classe
  à convertir, contrairement à `fontFamily`/`fontSize`. Absents de `DEFAULT_EDITOR_TOOLS` (même
  logique que `fontSize` : opt-in via `tools`), icônes FontAwesome `indent`/`outdent`.

- **`ui-editor` : outils `textColor`/`highlightColor`** (FSHSP-160). Deux nouveaux
  `EditorButtonTool`, chacun ouvrant un `ui-swatch-picker` en popup (`size="small"`) ancré à son
  bouton `ui-button`, plutôt qu'une commande directe — la sélection se fait dans le
  `swatchSelect` du nuancier. Palette réutilisée depuis `DEFAULT_SWATCH_PALETTE`
  (`ui-swatch-picker`) : `EDITOR_COLORS`/`EDITOR_HIGHLIGHTS` sont dérivées de la même liste pour
  que le nuancier et les classes générées portent toujours le même jeu de tokens
  `--primitives-*`. Écrits en **classe** (`ui-editor-color-*`/`ui-editor-highlight-*`) — jamais
  en style en ligne — via le même mécanisme marker → conversion que `fontFamily`/`fontSize` :
  `document.execCommand('foreColor', …)` émet un `<font color>` converti en `<span class>`,
  tandis que `hiliteColor` n'émet **pas** de `<font>` (vérifié empiriquement, Chromium et
  Firefox) — il pose un `style="background-color:…"` sur un `<span>`, converti via un sélecteur
  d'attribut `style` plutôt que `font[color]`. Le bouton de chaque outil affiche un liseré sous
  son icône dans la couleur en vigueur au curseur ; le nuancier propose une pastille
  « Aucune couleur » (`allowClear`) qui retire la classe couleur/surlignage de la sélection sans
  effacer le reste de la mise en forme — `foreColor`/`hiliteColor` n'ont pas de commande native
  « retirer la couleur ». Absents de `DEFAULT_EDITOR_TOOLS` (même logique d'opt-in que
  `fontSize`/`indent`/`outdent`).

- **`ui-swatch-picker` — nuancier de couleur réutilisable** (FSHSP-160). `ui-editor` avait
  besoin d'un nuancier couleur texte / surlignage, et rien dans le kit ne couvrait ce
  besoin : `ui-menu` structure des commandes, pas une grille de teintes. `ui-swatch-picker`
  reprend la mécanique overlay de `ui-menu` (`CdkConnectedOverlay`, `autoFlip`,
  re-position au scroll) sans porter son propre bouton trigger — même contrat de
  composition que `ui-button-split` avec `ui-menu` (`toggle(event)`/`show(event)`/
  `hide(focusTrigger?)`, `popup`).
  - `DEFAULT_SWATCH_PALETTE` (export nommé) : grille 7×5 dérivée des tokens `primitives.*`
    (7 teintes `primary`/`secondary`/`green`/`orange`/`red`/`slate`/`grey` × 5 paliers
    `900`→`100`, foncé → clair) plus `black`/`white` — chaque pastille pointe une variable
    `--primitives-{teinte}-{palier}`, jamais un hex en dur : un rebind de marque change la
    grille sans recompilation. `palette` accepte un `UiSwatchGroup[]` personnalisé.
  - Navigation clavier **grille 2D** (roving tabindex) : flèches, `Début`/`Fin`,
    `Entrée`/`Espace` sélectionnent, `Échap` ferme le popup — mêmes bases que le roving
    focus de `ui-menu`, adaptées au calcul ligne/colonne.
  - A11y : `role="listbox"` / `role="option"` sur de vrais `<button>`, `aria-selected` sur
    la pastille sélectionnée (liseré `--form-high-stroke-checked`, jamais une opacité —
    doit rester lisible sur chaque teinte), `aria-label` par pastille = son nom
    (jamais une description de la couleur seule).
  - `allowClear` (défaut `true`) ajoute une pastille « Aucune couleur » émettant
    `value = null` / `swatchSelect(null)`.

- **`ui-editor` : outils `alignLeft`/`alignCenter`/`alignRight`/`alignJustify`** (FSHSP-160).
  Quatre nouveaux `EditorButtonTool`, passthrough `execCommand('justifyLeft'|'justifyCenter'|
'justifyRight'|'justifyFull')` — contrairement à `indent`/`outdent`, ce sont des bascules
  (`EditorToggleTool`) : le navigateur garantit lui-même leur exclusion mutuelle
  (`queryCommandState`), donc elles suivent le même modèle `EditorState`/`aria-pressed` que
  gras/italique/souligné plutôt que celui d'un menu. **Dans `DEFAULT_EDITOR_TOOLS`** (à la
  différence de `fontSize`/`indent`/`outdent`/`textColor`/`highlightColor` : l'alignement est un
  outil de mise en forme standard, pas un doublon d'un autre contrôle).

### Changed

- **`ui-editor` : les sélecteurs `fontFamily`/`fontSize` passent du `<select>` natif à
  `ui-select`** (FSHSP-160). Rendu en `ui-select` `size="small"`, lié à la valeur en vigueur via
  `[ngModel]` et nommé pour les lecteurs d'écran via `ariaLabel` — cohérent avec le reste du kit,
  qui n'a qu'un seul composant de dropdown de valeurs. Bordure et fond de la boîte de champ
  retirés (`--ui-field-stroke-width: 0` + surcharge locale du fond via `::ng-deep`, scopée à
  `.ui-editor-select` — aucun autre `ui-select` du kit n'est concerné) pour que le sélecteur se
  fonde dans la barre d'outils comme les autres outils, plutôt que d'apparaître comme son propre
  champ de formulaire.

- **`ui-editor` : suppression de `blockFormat`, `fontSize` désormais dans la barre par défaut**
  (FSHSP-160). Plus de sélecteur de niveau de bloc (Normal/Titre 1-3) : l'éditeur ne produit plus
  que des `<p>`, aucun titre H1-H3 n'est accessible depuis la barre d'outils. `EditorBlock`,
  `EDITOR_BLOCKS`, `applyBlockFormat`/`readBlockFormat` et le tool `blockFormat` sont retirés de
  l'API publique — **breaking change** pour tout `tools` listant `'blockFormat'`. `fontSize`
  n'était auparavant pas dans `DEFAULT_EDITOR_TOOLS` (opt-in, pour éviter le doublon visuel avec
  `blockFormat`) ; cette raison n'existe plus, `fontSize` rejoint `fontFamily` en outil de premier
  rang, toujours actif.

- **`ui-editor` — éditeur de texte riche, sans moteur tiers** (FSHSP-160). Le kit s'arrêtait à `ui-textarea` : dès qu'un projet avait besoin de gras, de listes ou d'un lien dans un champ libre, il installait Quill ou TipTap et repartait avec un champ qui ne ressemble à aucun autre du formulaire. `ui-editor` couvre ce besoin en restant dans les règles du kit : bâti sur le shell `ui-field` en mode multiligne, il partage le libellé, la boîte, les niveaux, le helper et la validation avec les huit autres champs, et n'ajoute **aucune dépendance runtime** — les commandes de mise en forme s'appuient sur l'API d'édition native du navigateur, isolée dans `ui-editor-commands` pour qu'un futur moteur ne se substitue qu'à ce fichier.
  - **Barre d'outils configurable** : `tools` liste les outils rendus, dans l'ordre (`bold`, `italic`, `underline`, `bulletList`, `orderedList`, `link`, `clearFormat`, plus `separator`), et `toolbarPosition` la place au-dessus ou sous la zone de saisie. Elle forme **un seul arrêt de tabulation** (`role="toolbar"`, roving tabindex : ⭠ ⭢, `Home`/`End`, `Échap` rend le focus à la saisie) ; les bascules portent un `aria-pressed` recalculé à chaque déplacement du curseur, les actions ponctuelles n'en portent pas. Cliquer un outil ne déplace pas le focus et préserve la sélection. Les raccourcis `Ctrl`/`Cmd` + `B`/`I`/`U` sont pris en charge nativement, l'état de la barre suit.
  - **La valeur est du HTML assaini** : elle passe par `DomSanitizer` avant tout affichage, et le collage est en plus réduit à une liste blanche de balises (`b`, `strong`, `i`, `em`, `u`, `a`, `ul`, `ol`, `li`, `p`, `br`, `div`, `span`) avec `href` limité à `http(s)`/`mailto`/`tel` — un collage depuis Word ou une page web n'injecte ni style en ligne, ni tableau, ni balise étrangère. Le DOM de la zone n'est réécrit que lorsque la valeur vient du formulaire, jamais à la frappe : le curseur ne saute pas.
  - `maxlength` et `showCount` mesurent le **texte**, jamais le balisage, et une fois la limite atteinte les suppressions et la mise en forme restent disponibles. `minRows` fixe la hauteur minimale en lignes. `readonly` garde le curseur et la sélection en désactivant les outils ; `disabled` sort la zone du parcours de tabulation.
  - **27 hooks `--ui-editor-*`** couvrent la barre (espacement, insets, fond, trait, séparateurs), la zone (hauteurs mini/maxi, insets, interlignage, tailles de police, texte indicatif) et le contenu produit par l'éditeur (écart entre blocs, retrait et interligne des listes, graisse du gras, couleur et décoration des liens) — ce dernier étant créé hors du template, il est stylé via `::ng-deep` cantonné à la zone de saisie.
  - Le libellé est repris en `aria-label` plutôt qu'en `<label for>` : un `contenteditable` n'est pas un contrôle de formulaire et ne peut pas être la cible d'un `for`.
  - **Bloc de code.** `codeBlock` bascule le bloc sous le curseur entre `<pre>` et paragraphe, et reporte son état via `aria-pressed` comme les autres bascules. Sans coloration syntaxique : elle imposerait une dépendance tierce.
  - **Trois listes déroulantes, dont deux actives par défaut.** `blockFormat` (Normal, Titre 1-3) fixe **ce que le bloc est** — un titre porte la structure du document, qu'un lecteur d'écran parcourt ; `fontFamily` choisit la famille de caractères ; `fontSize` (Petit → Très grand) ne change qu'une apparence. Ces listes sont rendues en `<select>` et non en bouton, puisqu'elles choisissent parmi des valeurs au lieu d'en basculer une.
    - **`fontSize` est volontairement absent de la barre par défaut** : à l'écran il fait doublon avec `blockFormat` — les deux grossissent le texte — mais lui seul est décoratif. Les proposer côte à côte revient à inviter des titres qui n'en sont pas, donc des documents sans structure. Il reste disponible pour un projet qui l'ajoute à `tools`.
    - Les choix sont **fermés aux tokens** : les trois familles `--fontfamily-*` et l'échelle `--size-typography-text-*`. Une liste libre écrirait une police ou un `font-size` arbitraire dans la valeur, hors du système.
    - La liste des polices affiche le **nom réel** de chaque fonte, lu depuis le token à l'exécution (« Inter », « DM Sans », « Roboto Mono ») : écrit en dur, il nommerait une police que le projet n'utilise plus dès le premier rebind.
    - Famille et taille sont écrites en **classe**, jamais en style en ligne, si bien que rebinder le token déplace avec lui tout le contenu déjà saisi ; ces classes sont les seules que la liste blanche laisse passer au collage, aux côtés des nouvelles balises `pre`, `code` et `h1`-`h3`.
    - Les listes étant les seuls contrôles de la barre à prendre le focus, le curseur est mémorisé puis restauré avant que la commande s'applique.
    - **Chaque liste affiche la valeur en vigueur, jamais son propre nom** : au repos on lit « Normal » et « Inter », soit ce que le texte utilise réellement, et le nom de la liste n'est plus qu'un `aria-label`. La police est lue sur le **rendu effectif** plutôt que déduite de l'absence de classe : dans un titre, qui porte `--fontfamily-title`, la liste affiche « DM Sans » et non la famille de base. Seul `blockFormat` reste vide quand le curseur est dans un bloc qu'il ne propose pas (bloc de code, élément de liste) : annoncer « Normal » y inviterait un clic qui reformaterait le bloc.
    - Les listes ont toutes la **même largeur, fixe** : ajustée au contenu, elle ferait sauter la barre entière à chaque changement de valeur.

- **`ng add @4sh/ui-kit-schematics` pose l'arborescence d'assets maison** (FSHSP-164). `src/assets/` ne recevait qu'un `assets-map.json` vide, et seulement si `ui-image` faisait partie des composants copiés : le consommateur héritait de composants qui résolvent `assets/img/{marque}/{type}/…` sans un seul dossier pour les accueillir, ni rien qui dise où déposer une police ou un favicon. La fondation pose désormais `fonts/{police,icon}/` et `img/{common,brand1,brand2,brand3}/{jpg,png,svg}/` (les dossiers vides portent un `.gitkeep` — ni git ni un `Tree` de schematic ne livre un dossier sans fichier), un `src/assets/README.md` qui documente la convention et le format de l'`assets-map.json`, et l'`assets-map.json` lui-même quels que soient les composants choisis. Les sous-dossiers `light/`/`dark/` ne sont pas échafaudés : ils sont l'exception, et le README dit où les créer.
  - **`src/assets` est enfin déclaré au builder** (`assets` de la cible `build`, et de `test` quand elle est en Karma), en s'AJOUTANT au `public/` d'`ng new` plutôt qu'en le remplaçant. Sans cette entrée, tout ce que le kit résout sous `/assets/` répondait 404 dans l'application — `ui-image` tombant alors sur son placeholder sans rien dire de la cause. Les cibles `storybook`/`build-storybook` reprennent maintenant les assets de `build` au lieu de recoder la leur : la preview était le seul endroit où `/assets/` était servi, et elle y perdait au passage le `public/` du projet. Le `README.md` et les `.gitkeep` sont exclus du build (`ignore`).
  - **Deux fichiers sont fournis, le reste est un emplacement vide** : les **drapeaux** SVG (`img/common/svg/flags/`), génériques et codés en dur par la story `ui-input-group` — sans eux, son indicatif téléphonique affichait une image cassée chez le consommateur — et un **favicon** placeholder, sur lequel le `<link rel="icon">` de l'`index.html` est pointé, uniquement s'il porte encore le `favicon.ico` d'`ng new` (jamais par-dessus un `href` choisi par le projet). L'ancien `public/favicon.ico` est laissé sur le disque, simplement plus référencé.
  - **Aucun fichier de police n'est livré**, ni aucune fixture de démo : 2,2 Mo de `.ttf` de marque (dans un format qui n'est même pas celui que notre doc recommande) et 428 Ko de visuels de test arriveraient chez le consommateur comme du bruit à supprimer. À la place, un scaffold `src/styles/vendors/_fonts.scss` — mixin `variable-font`, exemple commenté, déjà `@use` par `main.scss`, et qui n'émet rien tant que rien n'est décommenté — plus la recette complète dans `src/assets/README.md`.
  - Corrigé au passage : les chemins d'assets du scaffold `src/styles/variables.scss` pointaient vers `../assets/images/`, un dossier qui n'a jamais existé (l'arborescence dit `img/`), et il manquait `$path-police`.

- **Les champs à boîte acceptent un mode `floatLabel`, avec ses trois variantes** (FSHSP-157). Le libellé n'avait qu'une place possible, au-dessus du champ : un formulaire dense n'avait pas d'autre levier que de le retirer et de rabattre l'information sur le `placeholder`, qui disparaît à la première frappe et n'est pas un libellé pour un lecteur d'écran. `floatLabel` le fait descendre **dans** le champ, où il tient le rôle du placeholder, et remonter au focus, dès qu'une valeur est présente, tant qu'un panneau ouvert retient le champ (un déclencheur comme `ui-datepicker` confie le focus à son calendrier sans cesser d'être actif), ou sur autofill du navigateur (qui ne déclenche aucun évènement de saisie). Trois positions hautes, alignées sur celles de PrimeNG : `over` (au-dessus de la boîte), `in` (bande réservée en haut de la boîte, qui grandit d'autant pour que la rangée du contrôle garde sa hauteur) et `on` (à cheval sur le trait, qu'il entaille). Disponible sur les huit champs bâtis sur le shell `ui-field` : `ui-input`, `ui-textarea`, `ui-select`, `ui-autocomplete`, `ui-input-number`, `ui-input-mask`, `ui-input-tags` et `ui-datepicker` (qui le transmet à son déclencheur). L'input est porté par `BaseFormField`, donc un champ écrit sur cette base l'hérite sans code supplémentaire. Non renseigné, rien ne change : le libellé classique reste le défaut, sur tous les champs.
  - Le libellé flottant reste un `<label for>` natif, marqueur `required` compris : il n'intercepte pas le pointeur au repos (le clic va au curseur de saisie) et le récupère une fois remonté. Seuls sa position et son échelle sont animées, jamais une `font-size` : le trajet ne provoque aucune reprise de mise en page, et la transition est coupée sous `prefers-reduced-motion`.
  - Aucun recouvrement de mise en page : `over` réserve la bande où le libellé monte, `on` la moitié qui dépasse du trait, `in` grandit la boîte. Deux champs collés verticalement ne se chevauchent pas.
  - En variante `in`, la bande est réservée par le **contrôle**, pas par la boîte : celui-ci continue de couvrir la boîte bord à bord (zone de clic, zone du curseur, fond d'autofill), seul le texte saisi descend sous le libellé. Affixes, unités, spinner, chevron, bouton d'effacement et libellé au repos restent centrés sur la boîte, qui se lit alors comme un tout.
  - Le `placeholder` natif est neutralisé tant que `floatLabel` et `label` sont tous les deux renseignés : les deux textes occupent la même place et ne seraient lisibles ni l'un ni l'autre. `floatLabel` sans `label` ne fait rien.
  - Nouveau mixin partagé `utils.field-float-inset($extra)`, dans la surface SCSS publiée : c'est par lui qu'un contrôle réserve la bande du libellé `in`. Un champ écrit hors du kit sur `ui-field` l'inclut sur son contrôle (les mixins `utils.field-native-input` le font déjà).
  - Neuf nouveaux réglages `--ui-field-float-label-*` (taille, interligne, échelle au repos, décalages, entaille de la variante `on`, retrait derrière une icône gauche), plus les trois valeurs dérivées qui en découlent : voir la table « Theming » de la doc.
- **`ui-datepicker` annonce le format de date attendu aux lecteurs d'écran** (FSHSP-118). Le `placeholder` seul (« jj/mm/aaaa ») est un support inégal selon les lecteurs d'écran, et il disparaît dès la première frappe. Un hint dédié, dérivé du même `resolvedPlaceholder`, est maintenant chaîné sur l'`aria-describedby` du déclencheur — à côté du message d'aide/erreur, jamais à sa place. Nouvel input `formatHintLabel` pour le personnaliser (ou `''` pour le désactiver) ; sans effet quand le champ n'est pas saisissable au clavier.
  - `ui-input` (donc tout champ construit dessus) accepte désormais un `ariaDescribedBy` externe, chaîné de la même façon sur son `aria-describedby` natif plutôt que de l'écraser — c'est le mécanisme qui rend le point ci-dessus possible sans dupliquer la logique dans `ui-datepicker`.
- **`ui-datepicker` : `allowInput` couvre maintenant `range` et `multiple`** (FSHSP-118), en complément de la grille (le clic continue de fonctionner à l'identique). `range` se tape dans le même champ, les deux dates séparées par `" - "` (ex. `"08/07/2026 - 18/07/2026"`) ; `multiple` accepte une liste séparée par `", "`, nombre de dates non borné. `range` bénéficie du même masque auto-"/" en direct qu'en mode `single` (les deux dates, puis leur séparateur, se construisent au fil de la frappe) ; `multiple`, dont le nombre de dates n'est pas borné, reste en texte libre, parsé au blur/Entrée uniquement — dans les deux cas avec les mêmes garanties qu'en `single` : une entrée incomplète ou invalide revient à la dernière valeur affichée, une plage tapée dans le désordre est réordonnée chronologiquement (comme un second clic dans la grille), une date dupliquée en `multiple` est supprimée (comme un clic sur une case déjà sélectionnée). Un `parseDate` custom s'applique par date individuelle, symétrique de `dateFormat`. Non couvert : la combinaison avec `showTime` (les dates tapées en `range`/`multiple` sont toujours calées à minuit — seule la grille gère l'heure sur ces modes pour l'instant).
- **Un utilitaire global `.sr-only`** dans la feuille de helpers publiée (section « Visibility ») : rend un texte visible des seuls lecteurs d'écran, pour nommer une colonne ou une action dont l'affichage ne montre rien. Ajouté pour les en-têtes de colonne vides des tableaux (`<th>` d'une colonne de sélection ou d'expansion), où `aria-label` ne suffit pas : `empty-table-header` exige du texte réellement présent. Quatre composants du kit recopient déjà ce bloc dans leur propre SCSS (`ui-datepicker`, `ui-select`, `ui-file-upload`, `ui-read-only`) — ils pourront s'appuyer dessus.

- **`ui-editor` : `clearFormat` retiré de la barre par défaut, encore disponible via `tools`**
  (FSHSP-160). `document.execCommand('removeFormat')` ne connaît que la mise en forme native du
  navigateur (gras, italique, souligné…) — jamais les `<span class="ui-editor-*">` que
  `fontFamily`/`fontSize`/`textColor`/`highlightColor` écrivent. Une première tentative,
  `clearFormatMarkers()` (déshabille ces classes sur la sélection, même mécanisme que le
  « Aucune couleur » des nuanciers), ne couvre pas encore tous les cas de façon fiable : plutôt
  qu'un bouton qui n'effectue pas toujours ce qu'il annonce, il est retiré de
  `DEFAULT_EDITOR_TOOLS` en attendant, sans rien retirer du kit — le tool, l'icône et
  `clearFormatMarkers()` restent en place pour un projet qui l'ajoute explicitement.
- **`ng add @4sh/ui-kit-schematics` écrit les commandes du gestionnaire de paquets du projet, plus `npm run` en dur** (FSHSP-177). Les scripts posés dans le `package.json` du consommateur (`storybook`, `build-storybook`, le hook `postinstall`) et la commande de démarrage affichée en fin d'installation nommaient `npm run` quel que soit le projet. Dans un projet pnpm ou yarn, ces scripts fonctionnaient — npm est toujours présent — mais contournaient le lockfile et faisaient apparaître un `package-lock.json` parasite au premier lancement. Le gestionnaire est désormais déduit du champ `packageManager` puis, à défaut, du lockfile présent, dans le même ordre de précédence que le CLI Angular (pnpm, yarn, bun, npm). L'**installation** des dépendances, elle, était déjà correcte : le CLI Angular transmet le gestionnaire qu'il détecte à `NodePackageInstallTask`.
- **Les scripts de la chaîne de doc copiés chez le consommateur nomment son gestionnaire dans leurs messages d'erreur** (FSHSP-177). `docs.config.mjs` et `docs.search.mjs` conseillaient « lance `npm run docs:config` » ; ils lisent maintenant `npm_config_user_agent` (renseigné par npm, pnpm, yarn et bun) et affichent une commande copiable telle quelle. Les messages rendus **dans le navigateur** (table `ConfigTable`, addons de recherche), qui n'ont pas accès à cette information, nomment désormais le script seul plutôt qu'un client.
- **`projects/ui-kit/README.md` (EN/FR) mène l'installation avec `pnpm add`** (FSHSP-177), en précisant que npm et yarn installent exactement la même chose — le package reste un tarball npm standard. C'est le fichier affiché sur npmjs.com.

- **`ui-datepicker` : la plage sélectionnée (`range`) se lit comme une seule zone continue** (FSHSP-166). Elle était peinte sur la cellule elle-même — fond en dégradé mi-cellule sur les deux extrémités, filets haut et bas en bordure — ce qui la faisait _encadrer_ la sélection plutôt que la porter, avec deux débordements visibles : les filets couraient sur toute la largeur des cellules de début et de fin, donc au-delà de la moitié réellement remplie, et la pastille des jours de début/fin, dont la bordure transparente s'ajoutait à sa taille (`content-box`), dépassait la hauteur de la bande. La bande est désormais une couche à part, exactement de la hauteur d'une pastille et arrêtée au centre des deux extrémités : plus rien ne peut la dépasser, ni en hauteur ni sur les côtés. Les filets sont retirés, et seuls les deux bouts d'une ligne de semaine sont arrondis — sur le retrait qu'une pastille laisse dans sa colonne, pas sur le bord de la cellule, pour qu'une plage finissant sur la dernière colonne s'aligne avec la rangée du dessus au lieu de s'arrêter une demi-gouttière trop tôt. Et une date de début dont la fin n'est pas encore choisie n'affiche plus de demi-bande partant vers une extrémité qui n'existe pas : la pastille reste seule jusqu'au second clic. Deux nouveaux réglages, `--ui-datepicker-range-background` et `--ui-datepicker-range-color`, permettent de re-skinner la bande et le chiffre qu'elle porte (voir la table « Theming » de la doc).

- **La boîte de `ui-field` est désormais enveloppée dans un `.ui-field-control`** (FSHSP-157). C'est le contexte de positionnement du libellé flottant, et il est rendu dans les deux modes plutôt que conditionnellement, pour que le DOM d'un champ ne dépende pas de l'option. Aucun impact visuel ni sur les sélecteurs publics ; un consommateur qui aurait écrit du CSS sur l'enchaînement direct `.ui-field > .ui-field-box` doit passer par le descendant.
- **`ui-label` tronque son texte quand il est contraint** au lieu de déborder (`text-overflow: ellipsis` sur `.ui-label-text`, `max-width: 100%` sur la racine). Sans contrainte de largeur, le comportement est inchangé : le texte passe à la ligne comme avant.
- **`ui-datepicker` est saisissable au clavier par défaut** (`allowInput` passe de `false` à `true`, FSHSP-118). Le champ ne proposait la sélection qu'au calendrier dans la grande majorité des configurations ; pour un non-voyant, taper une date est bien plus rapide que naviguer une grille de ~30 cases au lecteur d'écran. Repasser `allowInput` à `false` restaure l'ancien comportement (grille seule). `range`/`multiple` sont couverts aussi (voir plus haut) ; sans effet en `timeOnly` (pas de parseur pour une heure seule), qui reste lecture seule comme avant.
- **`showClear` passe de `false` à `true` par défaut, et sa priorité change face à l'icône calendrier** (FSHSP-118). Avant, la croix remplaçait systématiquement l'icône calendrier/horloge dès qu'une valeur était présente (si `showClear`) — au prix de perdre le seul déclencheur focusable capable de rouvrir le panneau. Désormais la croix ne prend le pas que si `showIcon` est à `false` : avec l'icône affichée (le défaut), elle reste cliquable pour changer la date directement, et l'effacement passe par le clavier (`allowInput`, sélectionner + supprimer le texte). Un consommateur qui utilisait déjà `showClear` avec `showIcon` à `true` verra donc la croix disparaître au profit de l'icône calendrier ; passer `showIcon` à `false` restaure son ancien comportement.
- **La règle `nested-interactive` est levée sur `ui-input-tags` et `ui-autocomplete` (mode `multiple`)** (FSHSP-106) : le bouton de suppression de chaque tag reste imbriqué dans son `role="option"`. Correctif accepté en l'état, décision prise avec l'équipe ; ticket de restructuration à suivre.
- **Deux paires de tokens à un cheveu du seuil AA sont montées d'un cran** (FSHSP-106) : `actions.low.content.default` `slate-500` → `slate-600` (4,44 → 5,45), et `navigation.highOutlined.content.active` `primary-500` → `primary-600` (4,20 → 6,00). Dans les deux cas, l'état voisin qui partageait la même valeur (`hover`) est monté d'un cran aussi, pour ne pas le fusionner avec l'état qu'on vient de corriger : `actions.low.content.hover` passe à `slate-700` (sinon identique à `default`), `navigation.highOutlined.content.hover` à `primary-600` (même défaut que `active`, non mesuré par axe car en `:hover`, mais réel). Vérifié à l'exécution sur `ui-link`, `ui-segment-control` et `ui-tabs` : zéro violation, écart visuel imperceptible. Source Figma (`src/design-tokens/semantics.json`) — la variable doit être mise à jour côté Figma pour ne pas être écrasée au prochain export.
- **La rampe de surfaces `warning` est décalée d'un cran** (FSHSP-106) : `default` passe de `orange-500` à `orange-700` (2,80 → **5,18:1** sous le texte blanc) et `hover` de `orange-700` à `orange-800` (7,31:1). Aucune couleur créée — contrairement au vert, l'orange n'avait pas de place pour un palier intermédiaire : la teinte conforme la plus claire a la même luminosité que le `700` déjà utilisé par le survol. `focused` (`orange-800`) et `pressed` (`orange-900`) sont inchangés. Conséquence visible : le `warning` par défaut est un orange nettement plus sombre. Mode clair uniquement.
- **Le clic dans le champ de `ui-datepicker` n'ouvre plus le calendrier quand le champ est saisissable** (FSHSP-180). `allowInput` étant vrai par défaut, le champ est d'abord un champ texte — or c'est le wrapper entier du déclencheur qui ouvrait le panneau, label et texte d'aide compris. Combiné au backdrop permanent de l'overlay, le champ en devenait inutilisable à la souris : le premier clic posait le curseur, le suivant était avalé par le backdrop et perdait le focus sans déplacer le curseur, donc corriger un seul segment à la souris était impossible ; un clic sur le label ouvrait un panneau que `Échap` ne fermait plus (le focus n'était nulle part) ; et l'ouverture commitait le texte tapé en direct, désarmant le masque auto-"/" en pleine frappe. L'icône calendrier, `↓` et le clavier ouvrent le panneau comme avant. Le clic garde son rôle d'ouverture là où il est la seule affordance : champ non saisissable (`allowInput="false"`, `readonly`, `timeOnly`) et `showIcon="false"`.

### Fixed

- **La saisie clavier de `ui-datepicker` inversait le jour et le mois en fin de saisie** (FSHSP-179). Avec un `dateFormat` custom et sans `parseDate`, l'ordre des champs restait celui de la locale résolue : un formatteur `fr-FR` sous un `LOCALE_ID` non configuré (donc `en-US`) affichait « 08/07/2026 » mais relisait ce même texte mois-en-premier — taper le 8 juillet commitait le 7 août, réaffiché « 07/08/2026 », le placeholder auto-dérivé annonçant pourtant l'ordre du formatteur. L'ordre jour/mois/année est maintenant **sondé sur la sortie du `dateFormat` lui-même** (son rendu d'une date d'illustration), et pilote à la fois le parser, le masque de frappe et le placeholder ; la locale reprend la main quand ce format n'est pas numérique (« 22 novembre 2023 »), cas qui demande de toute façon son `parseDate`.

- **Corriger un segment déjà tapé décalait tout ce qui suit** (FSHSP-179). Remplacer le mois « 07 » par un seul chiffre re-découpait le flux de chiffres complet : « 08/07/2026 » devenait « 08/12/026 », le « 2 » de l'année promu dans le mois et l'année réduite à trois chiffres. Le masque ne sait re-dériver le champ qu'en frappe séquentielle vers l'avant (ou en suppression depuis la fin) ; dès qu'une frappe n'a pas lieu en fin de texte, il **cède la place pour le reste de la saisie** — texte libre, parsé au blur/Entrée, exactement ce qui s'appliquait déjà quand une valeur existe. Il se réarme au commit suivant, ou dès que le champ se lit vide.

- **Le panneau de `ui-datepicker` débordait de l'écran sur un viewport étroit**. Trois causes cumulées, corrigées ensemble : le panneau était plafonné à `100vw - units-lg` mais la colonne de mois gardait une largeur fixe de 360px sans pouvoir rétrécir, donc la dernière colonne de jours et le chevron « suivant » passaient sous la bordure ; l'overlay CDK n'avait ni marge de viewport ni repoussage (`push`), donc un champ ancré n'importe où ailleurs qu'à gauche sortait le panneau hors de l'écran ; et en `numberOfMonths > 1` la rangée de mois faisait deux fois 360px, dont le second mois entièrement invisible. La colonne de mois et les grilles mois / année rétrécissent maintenant avec le panneau, la pastille d'un jour suit la largeur de sa colonne quand celle-ci passe sous `size/components/default` (la bande d'une plage la suit aussi), les mois s'empilent verticalement sous 600px avec un filet horizontal en séparateur, et l'overlay garde 12px de gouttière avec les bords du viewport. Le panneau défile désormais au-delà de `100dvh - units-3xl` plutôt que de déborder en hauteur (paysage sur téléphone), et un panneau `inline` ne dépasse plus la largeur de son conteneur.
- **`ui-image` inlinait les SVG locaux sans les assainir** (FSHSP-177). Le composant est le seul du kit à lever un `bypassSecurityTrustHtml()` — inévitable, puisque l'assainisseur d'Angular supprime `<svg>` en entier et que l'inline est ce qui permet à l'asset d'hériter du CSS (`currentColor`, tokens de thème). Mais la réponse HTTP y était passée **telle quelle** : un SVG servi depuis `assets/img/` exécutait son propre JavaScript dans l'origine de l'application. Vérifié dans un navigateur réel : un `<img onerror>` glissé dans un `<foreignObject>` s'exécutait **sans aucune interaction**, et les attributs `onload`/`onclick`/`onerror` survivaient dans le DOM. Le markup passe désormais par un scrub explicite (`sanitizeInlineSvg`) avant le bypass : `script`, `foreignObject`, `iframe`, `object`, `embed` et les éléments SMIL (`animate`, `animateTransform`, `animateMotion`, `set`, `handler`) sont retirés **avec leur contenu** ; tout attribut `on…` est supprimé ; `href`/`xlink:href`/`src` sont réduits à une référence intra-document (`#id`) ou à une URL `http(s)`, ce qui écarte `javascript:` et `data:`. Le parsing a lieu dans un `<template>` détaché, donc inerte, et l'absence de DOM (SSR) renvoie une chaîne vide plutôt que du markup brut. `SVG_CACHE` ne mémorise plus que du markup assaini.
  - Le modèle de menace n'est pas « les assets du kit » : c'est un projet qui sert `assets/img/` depuis un CDN, ou qui laisse un client déposer son logo dans un dossier de marque (white-label). Un `src` distant, lui, n'a jamais été concerné — il passe toujours par `<img [ngSrc]>`, jamais par le bypass.
  - Sans effet sur les assets légitimes : le scrub a été passé sur les 22 SVG réels de `src/assets/img/`, aucun n'est modifié (même nombre d'éléments et d'attributs, `viewBox` en casse préservée). 14 tests dans `ui-image-svg.spec.ts` verrouillent chaque vecteur.

- **Les `@media` générées depuis `responsive.json` sortaient dans l'ordre du JSON, pas par largeur croissante** — donc le desktop héritait des valeurs tablette. Les modes sont lus dans l'ordre où Figma les exporte (`modeDesktop`, `modeTablet`, `modeMobile`), et le bloc `@media (min-width: 1440px)` était émis **avant** `@media (min-width: 1024px)`. Sur un écran de 1440px les deux règles s'appliquent : à spécificité égale, c'est la dernière écrite qui gagne, et tout le desktop repartait sur l'échelle **tablette** (`--spacing-default: var(--units-md)` au lieu de `var(--units-default)`, `--grid-columns: 8` au lieu de `12`, toute la série `--size-components-*`, `--screen-width`/`--screen-height`). `scripts/tokens.build.mjs` trie maintenant les groupes `@media` par `min-width` croissante, puis par `max-width` décroissante — les deux conventions (mobile-first, desktop-first) placent ainsi le bloc le plus étroit en dernier, quel que soit l'ordre des modes dans le JSON source. Le tri comprend `min-width`/`max-width`, la syntaxe de plage (`width >= 900px`, `900px <= width`) et les unités `px`/`rem`/`em` ; une media query sans contrainte de largeur garde son ordre d'apparition.

- **En thème sombre, les jours situés _dans_ une plage `ui-datepicker` étaient illisibles** (FSHSP-166). Le chiffre prenait `--form-high-content-default`, qui bascule au quasi-blanc en sombre, alors que le fond de la bande est le même lavande clair dans les deux thèmes : blanc sur lavande, soit un contraste d'environ 1,3:1. Il prend maintenant le token de contenu apparié à ce fond (`--informative-highlightlow-content-default`, identique en clair et en sombre), ce qui remonte le contraste à ~4,7:1 (AA). Le survol et le focus d'un jour de la plage basculent sur la pastille pleine `informative.highlightHigh`, elle aussi stable d'un thème à l'autre — l'ancien fond neutre virait au gris foncé en sombre, sous un chiffre désormais foncé.

- **`ui-datepicker` en mode `timeOnly` ignorait `hourFormat` et `dateFormat`** (FSHSP-163). L'affichage formatait directement via `Intl` en `timeStyle: 'short'` sur la locale résolue, sans jamais consulter ces deux inputs : `hourFormat="24"` (le défaut) n'avait aucun effet — l'heure basculait en AM/PM dès que la locale résolue en avait un par défaut (ex. `en-US`) — et un `dateFormat` custom n'avait aucune prise sur ce mode. `hourFormat` est maintenant respecté (`hour12` forcé en conséquence, jamais laissé au défaut de la locale), et `dateFormat`, quand fourni, s'applique aussi en `timeOnly` (symétrique de son usage en `date`/`month`).
- **La saisie clavier de `ui-datepicker` (`allowInput`) pouvait mélanger les segments jour/mois/année après une suppression** (FSHSP-118). Le masque re-dérive l'affichage à chaque frappe depuis le flux brut des chiffres tapés ; une vérification de bornes (1-31, 1-12) — pensée pour rejeter un chiffre de tête invalide en cours de frappe — s'appliquait aussi après une suppression, où elle pouvait sauter un chiffre encore valide et décaler tout ce qui suit d'un cran vers le mauvais segment (le jour hérite d'un chiffre du mois, etc.) ; l'année, seule sans borne, n'était jamais concernée — d'où l'observation qu'elle seule se supprimait « proprement ». Cette vérification est maintenant désactivée quand la frappe raccourcit le texte (suppression), et rétablie dès qu'elle le rallonge. Une segmentation en place pleinement fiable quel que soit le point d'édition (clic au milieu du champ, par ex.) reste un chantier plus large, non couvert ici.
- **Effacer une date au clavier (`allowInput`) se bloquait dès que jour et mois étaient complets** (FSHSP-118). Le "/" auto-inséré entre deux segments place le curseur juste après lui, et un Retour arrière à cette position supprimait ce séparateur cosmétique plutôt qu'un chiffre — séparateur aussitôt réinséré au rendu suivant, donnant l'impression que la touche ne fait plus rien (ex. `20/08/2020` s'effaçait normalement jusqu'à `20/08/`, puis restait bloqué indéfiniment). Une suppression en fin de champ positionne maintenant le curseur juste avant ce séparateur, pas après, pour que le Retour arrière suivant retire le dernier chiffre du segment.
- **Corriger un segment d'une date déjà saisie (ex. juste le mois) décalait tout ce qui suit** (FSHSP-118), les deux points précédents n'y suffisant pas : re-dériver l'intégralité du texte à chaque frappe (le principe même de l'auto-"/") n'a de sens que pour _construire_ une date depuis un champ vide, jamais pour en corriger une déjà valide en place. Le masque se désactive désormais dès qu'une valeur existe (saisie complétée ou déjà présente au chargement) : la frappe redevient alors un champ texte ordinaire — aucun reformatage en direct, aucun risque de mélange de segments — et seul le parsing au blur/Entrée s'applique, déjà tolérant à un séparateur quelconque. Il se réactive de lui-même une fois le champ vidé, pour guider à nouveau la construction de la prochaine date — y compris en vidant le champ à la main puis en retapant aussitôt, sans passer par le blur ou par la croix : le vidage est désormais commité dès que le texte lu est vide, pas seulement au blur/Entrée.
- **Le placeholder auto-dérivé de `ui-datepicker` restait numérique (« jj/mm/aaaa ») avec un `dateFormat` custom**, alors que le champ n'affiche ni n'accepte ce format-là dans ce cas — un placeholder qui décrit une saisie que le parser va rejeter. Il reprend maintenant la sortie du `dateFormat` fourni pour une date d'illustration (ex. « Nov 22, 2023 »), cohérent avec ce que le champ affiche et attend réellement. Le hint `aria-describedby` (« Format attendu : … »), dérivé du même placeholder, en profite aussi.
- **`ui-datepicker` : taper l'heure juste après l'année (`showTime`) pouvait corrompre l'année affichée** (FSHSP-118). Le segment année, volontairement sans borne (`1-12`/`1-31` s'appliquent au jour/mois, pas à elle), ne déclenchait jamais l'insertion de son propre séparateur (l'espace avant l'heure) une fois ses 4 chiffres tapés — un suivi de position réservé aux segments bornés. L'heure tapée ensuite s'accolait donc directement à l'année (ex. `08/07/2026` + `10` tapé → `08/07/202610`, relu comme une année à 6 chiffres). Le suivi de position du moteur de masque partagé (`mask-engine.ts`, utilisé aussi par `ui-input-mask`) couvre maintenant tout segment, borné ou non ; la prévisualisation en direct attend en plus que l'heure soit complète avant de la commiter.
- **`ui-datepicker` : la saisie tapée en `range`/`multiple` pouvait couper une date en plein milieu si son `dateFormat` custom contenait le caractère du séparateur** (FSHSP-118). Le découpage retenait la première occurrence littérale de `" - "` (`range`) ou `", "` (`multiple`) dans le texte tapé, sans vérifier qu'elle délimitait bien deux dates plutôt que d'appartenir à l'une d'elles (ex. un `dateFormat` ISO contenant un tiret). Chaque occurrence candidate doit désormais faire parser valablement le texte qui la précède comme une date avant d'être retenue comme frontière.
- **`ui-datepicker` : le séparateur affiché d'une plage (`range`) était le même que celui attendu en saisie**, rendant les deux indiscernables à l'écran. L'affichage utilise maintenant un tiret cadratin dédié (« 08/07/2026 – 18/07/2026 »), découplé du séparateur de saisie/parsing (`" - "`, inchangé).
- **`ui-datepicker` : ouvrir le panneau en `range`/`multiple` — icône ou champ — renvoyait toujours le focus vers la grille, y compris pour taper** (FSHSP-118). Un premier correctif (renvoi vers la grille dès que `selectionMode() !== 'single'`) visait à restaurer le comportement de l'icône, mais s'appliquait aussi au clic sur le **champ** lui-même — qui ouvre le panneau via le même `open()` — volant le focus au clavier au moment même de cliquer pour taper, dès que `range` a reçu son propre masque en direct. `open()` distingue maintenant explicitement les deux : un clic sur l'icône renvoie vers la grille (« je veux la grille »), un clic sur le champ garde le focus dedans pour continuer à taper — dans tous les modes, pas seulement `single`.
- **`ui-input` annonce enfin sa popup aux lecteurs d'écran** (FSHSP-106). Le composant transmettait bien `aria-expanded` à l'`<input>`, mais cet attribut est interdit sur le rôle implicite `textbox` : l'information était donc silencieusement ignorée, et un non-voyant n'apprenait jamais qu'un calendrier pouvait s'ouvrir sur un `ui-datepicker`. L'input est désormais promu en `role="combobox"` — le rôle ARIA 1.2 qui accepte `aria-expanded` — **uniquement quand `ariaExpanded` est fourni**, donc l'annonce des champs texte ordinaires est inchangée. Seul `ui-datepicker` utilise ce jeu d'attributs dans le kit. Corrige 25 des violations `aria-allowed-attr` remontées par le job `a11y`.
- **`ui-input-number` annonçait des bornes invalides** (FSHSP-106). `min`/`max` non renseignés passent par `numberAttribute(undefined)`, qui rend `NaN` : le champ exposait `aria-valuemin="NaN"`/`aria-valuemax="NaN"`, une valeur ARIA invalide que les lecteurs d'écran ignorent au mieux. Les deux attributs ne sont désormais émis que si la borne est un nombre fini — sans borne, pas d'attribut, plutôt qu'une borne qui n'existe pas.
- **Le listbox des tags de `ui-input-tags` et `ui-autocomplete` était sans nom accessible** (FSHSP-106). Le nom n'était posé qu'à défaut de `label` : dès qu'un libellé visible existait, il partait sur l'`<input>` et la liste de tags n'annonçait plus rien. Le `label` sert maintenant de repli au nom du listbox. Dans `ui-autocomplete`, le `role="listbox"` couvrait en outre la boîte entière, **input combobox compris**, ce qu'un listbox n'a pas le droit de contenir : il n'enveloppe plus que les tags-options (wrapper `display: contents`, mise en page inchangée), comme cela avait déjà été fait pour `ui-input-tags`.
- **Les grilles mois et année de `ui-datepicker` étaient une structure ARIA invalide** (FSHSP-106). Leurs cellules `role="gridcell"` n'étaient enfermées dans aucun `role="row"` — obligatoire dans un `role="grid"` — là où la grille des jours, elle, l'avait. Le lecteur d'écran n'avait donc aucune structure de lignes à parcourir dans les vues mois/année. Les cellules sont désormais découpées en lignes correspondant aux colonnes réellement rendues (3 pour les mois, 2 pour les années), les wrappers étant en `display: contents` pour que la grille CSS reste identique.
- **`ui-tag`, `ui-badge` et `ui-chip` posaient un `aria-label` sur un `<span>` sans rôle** (FSHSP-106), ce qui est interdit : l'attribut est purement et simplement ignoré, donc le nom fourni par le consommateur n'existait pas pour un lecteur d'écran. Ces racines prennent maintenant `role="img"` quand un `aria-label` est fourni — et `role="group"` pour un chip retirable, dont le bouton × ne doit pas se retrouver enfermé dans un `img` (qui masque ses enfants). Sans `aria-label`, aucun rôle n'est ajouté : rien ne change.
- **Les zones scrollables de `ui-table` et `ui-modal` étaient inatteignables au clavier** (FSHSP-106). Une région qui défile doit pouvoir recevoir le focus, sinon son contenu débordant n'est accessible qu'à la souris. Le viewport de `ui-table` devient focusable dès que `scrollable` est actif. Pour `ui-modal`, la zone de corps ne le devient que si elle **déborde réellement et ne contient rien de focusable** (mesure au `ResizeObserver`, le contenu étant projeté) : un arrêt de tabulation inconditionnel se serait inséré dans l'ordre de focus de toutes les modales, ce qu'un test unitaire du focus-trap a d'ailleurs attrapé.
- **`ui-button` avalait son contenu projeté quand il portait une icône sans `label`** (FSHSP-106). La détection du contenu (`hasProjectedContent`) se fait depuis l'élément `.ui-button-content`, dont le rendu était lui-même conditionné à `isIconOnly()` — qui dérive de cette détection. Un `ui-button` avec `icon` et du seul contenu projeté ne rendait donc **jamais** ce contenu, restait en mode icône seule et se retrouvait sans nom accessible. Le wrapper est maintenant toujours rendu (le `.ui-button-content:empty { display: none; }` du SCSS, écrit pour ça, le replie quand il est vide) : un bouton icône seule est visuellement inchangé.
- **`ui-tabs` et `ui-stepper` pointaient un `aria-controls` vers un panneau qui n'existe pas** (FSHSP-106) quand ils sont utilisés sans panneaux — un menu de navigation adossé à un `router-outlet` (`ui-tabs`), ou un simple indicateur de progression (`ui-stepper`), les deux usages étant explicitement documentés. L'attribut référençait un id qui ne correspond à rien dans le DOM, une valeur ARIA invalide. Chaque conteneur tient maintenant le registre des panneaux réellement montés ; l'onglet/étape omet `aria-controls` plutôt que de le pointer dans le vide quand son panneau n'existe pas.
- **`ui-stepper` en mode vertical était une structure ARIA invalide** (FSHSP-106) : le conteneur `role="tablist"` contenait directement des `role="tabpanel"`, chaque panneau étant rendu juste sous son étape dans le même `ui-step-item` — un tablist qui contient son propre tabpanel est interdit. Le mode vertical bascule sur le pattern accordéon (`aria-expanded` sur l'en-tête à la place de `role="tab"`/`aria-selected`, `role="region"` sur le panneau à la place de `role="tabpanel"` — la même paire que `ui-accordion-panel`), sans déplacer un seul élément du DOM. Le mode horizontal (`ui-step-list`, un vrai tablist sans panneau à l'intérieur) est inchangé.
- **La surface `error` passe le seuil AA** (FSHSP-106) : `actions.error.surface.default` et `informative.errorHigh.surface.default` pointent désormais `red-600` (`#dc2626`) au lieu de `red-500` (`#ef4444`) — 3,76 → **4,83:1** sous le texte blanc. Aucune couleur créée : le palier `600` existait déjà et n'était utilisé par aucun état de la chaîne (`hover` = `700`, `focused` = `800`, `pressed` = `900`), l'écart visuel est à peine perceptible. Mode clair uniquement, le seul que le job CI mesure. Les surfaces `success` et `warning` restent en échec : leur palier `600` ne suffit pas (3,77 et 3,56), elles demandent un arbitrage de palette.
- **La rampe de surfaces `success` est décalée d'un cran** (FSHSP-106) : `default` passe de `green-500` à `green-700` (2,54 → **5,48:1** sous le texte blanc) et `hover` de `green-700` à `green-800` (7,68:1). `focused` (`green-800`) et `pressed` (`green-900`) sont inchangés. Aucune couleur créée : le `600` ne suffisait pas (3,77:1) et le décalage évite d'introduire un palier intermédiaire, en appliquant la même méthode que pour `warning`. Conséquence visible : le `success` par défaut est un vert nettement plus sombre — inhérent à WCAG 2 sur les verts saturés, aucun vert clair ne passe sous du texte blanc. Mode clair uniquement.
- **La story `ui-button/On Color Light` reposait sur un fond qui n'est plus clair** (FSHSP-106). Elle utilisait `actions.warning.surface.default` comme bandeau clair pour démontrer `onColor="light"` (texte foncé sur fond clair) ; l'assombrissement de ce token en `orange-700` a invalidé la prémisse — le texte foncé y tombait à 3,42:1. Le bandeau utilise désormais `informative.warningLow.surface.default` (`orange-50`), un fond réellement clair. Documentation seule, aucun composant touché.
- **Deux stories mettaient le check de contraste en échec sans qu'un composant soit en cause** (FSHSP-106). `ui-segment-control/Template` appliquait un `opacity: .75` en dur sur le libellé secondaire de son template de démonstration, ce qui délavait le texte sous le seuil AA — retiré, la hiérarchie visuelle tenant déjà par la taille et la graisse. `ui-button/On Color Omitted` existe au contraire **pour montrer** un contraste insuffisant (c'est le problème que `onColor` résout) : la règle y est levée avec sa justification, plutôt que de supprimer la démonstration.
- **Un élément de `ui-breadcrumb` désactivé ne l'annonçait pas** (FSHSP-106) : seule une classe CSS le distinguait, une technologie d'assistance n'avait aucun moyen de le savoir. Il porte désormais `role="link"` + `aria-disabled="true"` — le motif d'un lien qui existerait s'il était actif. Le rôle est nécessaire pour que l'attribut soit légal (`aria-disabled` n'est pas global, un `<span>` sans rôle le refuse). Effet de bord bienvenu : l'exemption de WCAG 1.4.3 pour les composants inactifs s'applique alors au contraste de son libellé atténué, que le check mesurait faute de savoir qu'il était désactivé.
- **Les jours des mois adjacents de `ui-datepicker` passent le seuil AA** (FSHSP-106). Ils sont **cliquables**, donc leur rendu atténué doit être conforme par lui-même : ils s'appuyaient sur `form.low.content.default` (`grey-500`), qui ne passe qu'à pleine opacité — atténué à 60 %, il tombait à 2,33:1. Nouveau réglage de composant **`--ui-datepicker-day-color-outside`**, par défaut `global.text.default`, l'opacité de 60 % restant inchangée : 4,68:1, et les jours adjacents restent visiblement plus pâles. Le token partagé n'est pas touché — ses 12 autres consommateurs (placeholders de `ui-select`, `ui-textarea`, `ui-autocomplete`, étoiles de `ui-rating`…) le rendent sans atténuation et sont déjà conformes.

## [0.6.1] - 2026-08-22

### Fixed

- **Les images de la doc Storybook posée par `ng add @4sh/ui-kit-schematics` étaient illisibles** (FSHSP-156). La copie des assets Storybook lisait chaque fichier en texte avant de l'écrire : sur un binaire, le décodage UTF-8 remplace tout octet invalide par U+FFFD, donc les 5 `doc-*.png` de la page `Introduction` arrivaient corrompus, signature PNG détruite et poids gonflé de 80 % (`doc-token.png` : 80 180 octets au lieu de 44 440). Seuls les `.mdx` sont désormais décodés, pour la réécriture des chemins du kit ; tout le reste est copié en binaire, octet pour octet.
- **`ng add @4sh/ui-kit-schematics` échouait dès qu'on acceptait Gridaflex** (FSHSP-155). Le scaffold `src/ng-add/files/vendors/_gridaflex-settings.scss` n'avait jamais été commité : présent sur le poste de dev, donc invisible en local, mais absent du dépôt et du tarball 0.6.0. Répondre « oui » à la question s'arrêtait sur `ENOENT` et laissait l'installation à moitié faite. Le fichier est maintenant versionné.

## [0.6.0] - 2026-08-21

### Added

- **`ui-card` accepte un input `fluid`** (FSHSP-154). Sans dimension intrinsèque, la carte prenait la largeur de son contenu dans un parent `flex`, ce qui obligeait à poser un `width` en dur (toutes les stories le faisaient) ou un `flex: 1` côté consommateur. `fluid` lui fait occuper toute la largeur du parent, avec la même API que `ui-segment-control`.
- **`update` dit maintenant ce qu'il ne couvre pas, et comment le rattraper** (FSHSP-151, remonté par le REX FSHSP-146). Il ne traite que les composants de `ui-kit.json` : tout ce que `ng add` a posé autour — serveur MCP, config Storybook, chaîne de tokens, cibles d'`angular.json`, dépendances — restait à la version de l'installation d'origine, sans que rien ne le signale. C'est ainsi qu'un projet installé en 0.2.0 puis monté en 0.5.0 n'a jamais eu le serveur MCP, arrivé en 0.4.0, et que son auteur en a conclu qu'il n'existait pas. `update` renvoie désormais vers `ng add @4sh/ui-kit-schematics --skip-components`, qui réapplique la fondation sans toucher aux composants copiés — documenté dans le README des schematics, avec ce qui est préservé et la seule réserve mesurée (une dépendance épinglée par le consommateur peut y être ré-élargie). Le rattrapage n'est pas fait d'office à dessein : rien ne consigne si une pièce manque parce qu'elle n'existait pas encore ou parce qu'elle a été écartée avec `--skip-mcp`.
- **`ng add @4sh/ui-kit-schematics` propose maintenant Gridaflex** (FSHSP-144). La grille autour de laquelle le kit est pensé n'était ni installée ni posée par la schematic : un projet fraîchement généré n'avait ni la dépendance, ni ses réglages, et les stories copiées de `ui-card`/`ui-read-only` s'affichaient à plat. Une question posée juste après le choix des composants pose désormais `gridaflex` en dépendance, `src/styles/vendors/_gridaflex-settings.scss` (réglages du consommateur, créé une fois) et son `@use` en tête de `src/styles/main.scss`. Un refus ne pose aucun des trois. `--gridaflex` / `--no-gridaflex` répond sans prompt ; hors terminal interactif, la question est sautée et rien n'est posé. Le mode librairie (`@4sh/ui-kit`) est inchangé : `styles.css` ne contient pas la grille.
- **`ui-card` accepte un input `contentClass`** (FSHSP-147). Mettre en page le contenu d'une carte imposait soit un `<div>` wrapper juste pour porter des classes utilitaires, soit `contentFlush` (qui retire la gouttière et oblige à la ré-appliquer à la main). `contentClass` pose les classes directement sur `.ui-card-content`, gouttière conservée ; `contentFlush` reste le levier du contenu edge-to-edge, et les deux se combinent.
- **`@4sh/ui-kit` expose le serveur MCP comme binaire `ui-kit-mcp`** (FSHSP-146). La configuration documentée passait par un chemin en dur (`node node_modules/@4sh/ui-kit/mcp/index.js`), relatif au répertoire courant : elle échouait dès que le client MCP lançait le serveur depuis autre chose que la racine du projet, et ne survivait ni à pnpm ni à un hoisting de monorepo. `npm install` pose désormais `node_modules/.bin/ui-kit-mcp`, d'où la forme recommandée `npx --no-install ui-kit-mcp` — `--no-install` pour que npx échoue franchement si le binaire manque, au lieu d'aller chercher sur le registre un paquet homonyme (le nom court n'y est pas réservé). L'ancienne invocation par chemin continue de fonctionner.

### Changed

- ⚠️ **`--yes` devient `--force` sur `ng generate @4sh/ui-kit-schematics:update`** (FSHSP-153). Le nom laissait croire à une simple confirmation, alors que le flag applique tout sans afficher un seul diff — et `update` remplaçant au lieu de fusionner, il écrase les retouches locales sur les composants concernés. C'est ce flag qui avait masqué le défaut corrigé par FSHSP-150 : lancé avec, rien ne s'affichait. Pas d'alias de compatibilité, l'API étant instable sous `0.x`. **Attention en CI** : `--yes` n'étant plus reconnu, `update` retombe sur la question interactive et attend l'entrée au lieu d'échouer — un pipeline qui le passait encore se bloquera. Remplacer par `--force`.
- **Le tableau des commandes des README des schematics liste maintenant les flags des deux generators** (FSHSP-153) : il ne couvrait que ceux d'`ng add`, alors que `--force` et `--all` changent radicalement le comportement.
- **Toutes les dépendances du repo sont épinglées en version exacte** (FSHSP-149), y compris `tslib` dans `projects/ui-kit/package.json` (`^2.3.0` → `2.8.1`) : un range laissait deux `npm install` à des dates différentes résoudre des versions différentes, y compris pour le tarball publié. `save-exact=true` (`.npmrc`) pour que ça reste épinglé. Les `peerDependencies` restent en range — leur rôle est d'exprimer une compatibilité, pas une résolution.
- **`@4sh/ui-kit-schematics` déclare maintenant `diff@9.0.0`** (au lieu de `^5.2.0`), aligné sur la version déjà utilisée par le tooling du repo — la seule fonction consommée (`createTwoFilesPatch`, dans `update/index.ts`) est inchangée entre les deux versions.
- **Le serveur MCP est découvrable dans les métadonnées npm** (FSHSP-146). `npm view @4sh/ui-kit` ne le mentionnait nulle part : ni la `description`, ni les `keywords` — seul le README en parlait, que npm n'affiche pas dans ces champs. S'y ajoute le retrait de `private: true` du `package.json` embarqué sous `mcp/`, qui avait déjà fait conclure à tort que le serveur n'était pas livré. Le fichier lui-même reste indispensable (il porte `type: module` et la `version` lue au runtime) ; `private` n'est retiré que dans la copie publiée, pas dans la source, où il garde son rôle de garde-fou.
- **La version annoncée au client MCP est celle du kit** (FSHSP-146). Le serveur rapportait `0.1.0` — son propre numéro interne, figé quel que soit le `@4sh/ui-kit` qui l'embarquait, donc inexploitable pour un agent voulant savoir à quelle version de la doc il parle. `scripts/mcp-bundle.build.mjs` tamponne désormais la version du kit dans le manifeste embarqué, à l'assemblage — même parti que `scripts/schematics-package.build.mjs` pour le compagnon. Rien à faire à chaque release : les deux modes (librairie et starter) passent par ce bundle, donc la montée suit toute seule. Le numéro de `projects/ui-kit-mcp/package.json` n'est plus qu'un repère de développement.

### Fixed

- **La boîte visible de `ui-card` ne remplissait pas la hauteur de son hôte** (FSHSP-154). `:host` était en `display: block` et la boîte interne en hauteur automatique : une rangée de cartes voyait ses hôtes s'étirer à la même hauteur, mais pas les fonds, bordures ni ombres (hôtes à 146 px, boîtes à 74 / 122 / 146 px sur trois contenus inégaux). Un commentaire du `.scss` promettait pourtant l'inverse, d'où des interprétations fausses du comportement du composant. L'hôte est maintenant un conteneur flex vertical dont la boîte est l'unique élément extensible : une rangée de cartes s'aligne sans CSS supplémentaire, et un `height` posé sur `<ui-card>` s'applique bien à la boîte.
- **`update` annonçait une montée qu'il n'appliquait pas** (FSHSP-150, remonté par le REX FSHSP-146). Sur un projet copié avant la 0.3.0, il affichait « ✔ 6/6 composant(s) mis à jour », écrivait `ui-kit.json` à la nouvelle version — et n'écrivait que des CREATE : l'aplatissement de l'arborescence (FSHSP-121) ayant changé tous les chemins, les fichiers neufs atterrissaient à côté des anciens sans les remplacer, et l'application continuait de compiler l'ancien code par ses imports vers `src/public-api`. Build vert, tests verts, ancienne version en production — le pire mode de défaillance pour un outil de migration. `update` refuse maintenant, avant toute écriture et sur tout le lot, en nommant les composants concernés et les deux voies de sortie. La migration automatique (suppression des anciennes arborescences, réécriture des imports de l'application) n'est pas dans ce correctif.
- **`ui-kit.json` passait à la version cible même sans rien appliquer** (FSHSP-150). `kitVersion` était écrit hors de la boucle, sans condition : un composant introuvable dans le kit, ou ignoré par l'opérateur, laissait quand même le manifeste annoncer une version que l'arborescence ne portait pas. Il ne monte plus que si tous les composants en retard ont été appliqués, et le compte-rendu passe en avertissement — avec ce qui reste à faire — dès qu'il en manque un.
- **`ng add` cassait `ng test` pour tout le projet** (FSHSP-152, remonté par le REX FSHSP-146). `updateAngularJson` écrivait `styles`, `stylePreprocessorOptions` et `inlineStyleLanguage` sur la cible `test` comme sur la cible `build`. Or le schéma de `@angular/build:unit-test` — le builder par défaut depuis Angular 20 — est en `additionalProperties: false` et n'en déclare aucune : la configuration devenait invalide et les tests ne démarraient plus, sans message exploitable. Ces options y étaient de surcroît inutiles, `unit-test` dérivant sa configuration de build du `buildTarget`, donc de la cible `build` déjà traitée. Elles ne sont plus écrites sur `test` que si son builder les déclare (Karma) ; un builder inconnu est présumé dériver du `build` plutôt que de casser `ng test` par défaut.
- **La page `Introduction` du Storybook posé par `ng add @4sh/ui-kit-schematics` renvoyait vers la démo et les fichiers Figma du starter** (FSHSP-145). `scripts/schematics-assets.build.mjs` copiait `Introduction.mdx` tel quel en le traitant comme sans dépendance au monorepo, alors que ses sections « Application de démonstration » et « Ressources Figma » sont propres à ce dépôt. `scaffoldStorybook` pose désormais un scaffold (`src/ng-add/files/storybook/docs/Introduction.mdx`, sans ces deux sections) avant la copie brute des assets, pour la court-circuiter sur ce fichier précis.

## [0.5.0] - 2026-08-20

### Fixed

- **Le serveur MCP compagnon était absent du tarball `@4sh/ui-kit` publié** (FSHSP-141). `npm run ui-kit:build`, seul appelé par `publish-ui-kit.yml`, n'embarque jamais `mcp/` — cette étape ne vit que dans `ui-kit:pack` (`ui-kit:build && mcp-embed && npm pack`), jamais utilisé par le workflow de publication. La 0.4.0 a donc été publiée sans `mcp/`, sans qu'aucune étape ne le détecte. Le workflow appelle maintenant explicitement `scripts/ui-kit-mcp-embed.build.mjs` après le build, dans les jobs `verify` et `publish` — un bundle manquant fait désormais échouer le job au lieu d'atteindre le registre.

### Added

- **Le Storybook posé par `ng add @4sh/ui-kit-schematics` a maintenant la recherche plein texte et le bouton « copier en Markdown »** (FSHSP-138). Les deux addons (`storybook/addons/text-search`, `storybook/addons/copy-as-markdown`) et la page `Introduction` n'étaient jamais sortis du Storybook du monorepo : `scripts/schematics-assets.build.mjs` les copie désormais dans `assets/`, le scaffold `main.js` les déclare (+ `staticDirs: ['./public']` pour les images d'`Introduction`), et `scripts/docs.search.mjs` (qui alimente la recherche) se détecte lui-même monorepo/consommateur, comme `docs.config.mjs`. Nouvelles devDependencies : `minisearch`, `unified`, `remark-parse`, `remark-mdx`, `unist-util-visit`. Nouveau script `docs:search`, chaîné dans `storybook`/`build-storybook`.
- **La doc `Foundations/Motion` suit maintenant la base partagée `motion` copiée par `add`** (FSHSP-138). `ui-motion.mdx`/`.stories.ts`/`.demo.ts` sont co-localisés à côté de `src/` dans `projects/ui-kit/motion/`, comme un composant — mais `schematics-assets.build.mjs` ne copiait que `src/`, jamais ces fichiers, pour aucune base partagée. Les globs `stories` de `storybook/main.js` (scaffold) gagnent aussi `ui-core/**` : sans eux, un fichier bien copié restait invisible pour Storybook.
- **`ng add @4sh/ui-kit-schematics` pose `.prettierrc`/`.prettierignore` et une devDependency `prettier@3.9.6`** (FSHSP-140). Le CLI Angular reformate les fichiers qu'il écrit (`add`/`update`) avec la config Prettier trouvée chez le consommateur — sans la même des deux côtés, un `update` comparait une copie fraîche du kit à une copie reformatée différemment, noyant le diff dans du bruit d'indentation. Config identique à celle du kit, jamais écrasée si le consommateur en a déjà une.

### Changed

- **`ng add @4sh/ui-kit` échoue désormais** (FSHSP-142). Ce point d'entrée ne faisait que logger un message d'info — aucune configuration générée, pas plus qu'un simple `npm install @4sh/ui-kit` — tout en étant la commande tentée naturellement pour le mode starter. Il renvoie maintenant une erreur explicite (`ng add @4sh/ui-kit-schematics` pour le starter, `npm install @4sh/ui-kit` directement pour la librairie) plutôt que de se terminer en succès silencieux.

## [0.4.0] - 2026-08-19

### Added

- **Serveur MCP compagnon** (FSHSP-115) : expose le catalogue des composants `ui-*`, leur API (props/types/theming) et une recherche plein texte dans toute la doc Storybook à un agent IA (Claude Code, Claude Desktop…), sans qu'il ait besoin de lire les sources. Pas de package npm à part : bundlé (esbuild, un seul fichier, zéro dépendance — `npm run mcp:bundle`) puis embarqué dans les deux tarballs publiés — sous `mcp/` dans `@4sh/ui-kit` (mode librairie, cf. son README), copié dans `.ui-kit-mcp/` par `ng add @4sh/ui-kit-schematics` (mode starter, `--skip-mcp` pour l'omettre). `npm run mcp:test` pour sa suite Vitest.
- **`ng add @4sh/ui-kit-schematics` déclare le serveur MCP `ui-kit`** (FSHSP-115) : `.mcp.json` (`node .ui-kit-mcp/index.js` — fusionné, jamais écrasé) + instruction ajoutée à `AGENTS.md` (créé si absent, complété sinon) pour qu'un agent compatible MCP interroge la doc du kit au lieu de deviner.

### Changed

- **Le Storybook du mode starter est posé par défaut** (FSHSP-134). `ng add @4sh/ui-kit-schematics` pose désormais la doc sans qu'on la demande : story et MDX à côté de chaque composant copié, configuration `storybook/`, cibles `angular.json`, devDependencies et scripts npm. `--with-storybook` disparaît au profit de `--skip-storybook`, pour le projet qui documente ailleurs. Le flag opt-in laissait le cas nominal sans doc, et la commande ne mentionnait nulle part qu'il existait : le diagnostic naturel devant `npm run storybook` → `Missing script` est que le Storybook de la schematic est cassé. La commande dit maintenant, en dernière ligne, ce qu'il reste à lancer.

### Fixed

- **Les composants ne s'affichent plus en serif chez un consommateur qui n'embarque pas les polices du starter** (FSHSP-135). Les tokens de famille ne portaient que le nom nu — `--fontfamily-base: Inter` — et les ~40 `.scss` du kit les lisent tels quels (`font-family: var(--fontfamily-base)`). Sans le fichier de police, cette déclaration retombait sur le défaut du navigateur, c'est-à-dire un serif ; la couche de base aggravait le cas en déclarant explicitement `var(--fontfamily-base), serif`. Un projet qui installait le kit et chargeait `styles.css` voyait donc tout le kit en Times.

  Chaque famille se termine désormais par une pile système, ajoutée **au build** :

  ```css
  --fontfamily-base:
    Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  --fontfamily-monospace: 'Roboto Mono', ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  ```

  La pile est déclarée par la clé `fontStacks` de la collection `typography` dans `tokens.config.json` (repli par défaut, et par famille pour le monospace ; aucun repli pour la police d'icônes, qui n'a pas de substitut lisible). Le JSON de tokens, lui, garde le nom de famille nu : c'est ce que stocke la variable Figma (scope `FONT_FAMILY`), et la parité Figma ↔ SCSS reste donc intacte.

  Sans aucune police embarquée, le rendu tombe maintenant sur le sans-serif de l'OS — San Francisco, Segoe UI, Roboto. **Les fichiers de police ne sont toujours pas livrés dans le package** : ils appartiennent au projet, comme sa marque. La mise en place, pour les deux modes de consommation, est documentée dans Storybook → _Foundations → Typography_.

  Vaut pour les deux modes : le mode schematics reçoit la même fondation, et le pipeline de tokens qu'il copie porte le correctif — un `npm run tokens:build` côté projet régénère bien les piles.

## [0.3.0] - 2026-08-18

Deux chantiers dominent cette version, un par mode de consommation. En **mode
librairie**, les composants deviennent retouchables sans recompiler : 581 custom
properties `--ui-*` exposées sur leurs valeurs structurelles, et un
`component-vars.scss` livré avec le package, prêt à copier, qui les déclare
toutes à la valeur du starter. En **mode starter**, le parcours tient en une
commande, les sources copiées ne dépendent plus de `node_modules`, leur
arborescence est aplatie, et le projet reçoit son propre Storybook.

Elle est `MINOR` malgré les ruptures qu'elle contient : tant que la version
commence par `0.x`, l'API est déclarée instable et une `MINOR` peut casser, à
condition de le documenter (voir [VERSIONING.md](./docs/VERSIONING.md)).

⚠️ **Mise à jour non triviale pour un projet en sources copiées** : tous les
chemins des fichiers copiés changent (FSHSP-121), et `@4sh/ui-kit` doit être
désinstallé du projet (FSHSP-122). Voir la section `Changed`. **Le mode
librairie est inchangé** — aucun import n'y bouge.

### Added

- **Les icônes des composants sont surchargeables à deux échelles** (FSHSP-113). `ui-icon` savait déjà rendre n'importe quelle police (`UiIconFamily` + `provideUiIconFamilies()`), mais aucun composant ne forwardait cette notion : les champs exposent le _nom_ de leur icône, jamais sa famille, et la majorité des icônes d'un composant sont **internes** — les dix chevrons du panneau de `ui-datepicker`, la coche et la loupe de `ui-select`. Basculer la famille par défaut au niveau applicatif envoyait ces noms FontAwesome codés en dur à la police cible, qui ne les connaît pas, et les icônes internes disparaissaient.

  **Directive `uiIconFamily`, granularité sous-arbre.** Posée sur un champ, une section ou la racine, elle re-déclare la famille par défaut dans son _element injector_ : tous les `ui-icon` descendants la voient, icônes internes comprises, sans `provideUiIconFamilies` global. Elle traverse les **overlays CDK** (panneaux de `ui-datepicker`, `ui-select`, `ui-autocomplete`) — la relocalisation DOM ne change pas la chaîne d'injecteurs — et vaut aussi pour les composants non-formulaire (menu, paginator, modal, tabs).

  ```html
  <form uiIconFamily="material">…</form>
  ```

  **Templates d'icône, granularité une icône.** `ui-input` expose `#iconLeft` et `#iconRight` (contexte : nom configuré, taille du champ, état `disabled`), sur le modèle déjà en place ailleurs dans le kit (`#onIcon`/`#offIcon` de `ui-rating`, `#item` de `ui-select`). La zone d'action droite garde son `<button>` accessible : seul son contenu est remplacé. `ui-datepicker` forwarde le sien vers le déclencheur sous `#icon`, ce qui permet de changer la seule icône calendrier en gardant FontAwesome sur ses dix chevrons.

  Les deux leviers se choisissent selon l'échelle visée, pas selon une hiérarchie : un template vise une icône et peut en changer le markup entier (SVG de marque) ; la directive vise une zone et atteint ce qu'aucun input n'expose. Sans rien préciser, la famille par défaut s'applique et le markup rendu est inchangé.

- **Un projet en sources copiées a désormais son propre Storybook** (FSHSP-125) : `ng add @4sh/ui-kit-schematics --with-storybook` copie, à côté de chaque composant, sa story et sa page MDX, pose la configuration Storybook (`storybook/`, cibles `angular.json`, devDependencies, scripts npm) et la chaîne qui alimente la doc (`scripts/docs.config.mjs`, bloc `<ConfigTable>`). Un `npm run storybook` suffit ensuite. Jusqu'ici le projet possédait son code mais aucune doc : il lui restait un Storybook hébergé qui décrit _nos_ composants, pas ses copies éditées.

  Deux choses font que cette doc appartient au projet. Les tables _Theming_ sont lues sur ses propres `.scss` au build, donc elles décrivent ses valeurs, rebranding compris. Et les globs couvrent tout `src/app/shared/components/**` : une story écrite à côté d'un composant maison apparaît sans toucher à la configuration.

  Les imports suivent les copies, comme pour les sources — mais les extraits de code des pages ne sont pas réécrits : ce sont des exemples destinés au lecteur, pas des imports à résoudre. Les liens `parameters.design` vers notre fichier Figma sont retirés à la copie : le consommateur n'y a pas accès. Les pages transverses embarquées se limitent à _Foundations_, _Spécifications_ et _Configuration_ ; `Introduction`, `NpmPackage` et `Overview` parlent du kit lui-même et restent ici.

  Désactivé par défaut : sans Storybook, story et MDX sont deux fichiers qui importent des packages absents. Le choix est retenu dans `ui-kit.json` et `update` s'y tient.

- **Les README du kit (EN + FR) présentent les deux modes de consommation** (FSHSP-116) : section « Deux modes de consommation » avant l'installation — tableau comparatif _dépendance_ / _starter_ (ce qui arrive dans le dépôt, imports, styles, documentation, personnalisation, mise à jour), qui renvoie au package compagnon pour la voie starter. FSHSP-109 avait livré le mode starter sans qu'il apparaisse nulle part sur la page npm, où le README est la seule documentation.
- **Les composants sont personnalisables en mode package, via des custom properties `--ui-*` exposées.** Jusqu'ici le seul levier de personnalisation était le rebinding SCSS (`_ui-config.scss`, variables locales) : inaccessible quand le kit est consommé en dépendance, puisque son SCSS est déjà compilé. Un projet ne pouvait retoucher que les design tokens — donc rien de propre à un composant, ni à une seule de ses tailles. Chaque valeur **structurelle** des 54 composants est maintenant lue à travers un hook dont le défaut est le réglage livré :

  ```scss
  // avant : valeur compilée, hors d'atteinte
  height: var(--size-components-sm);
  // après : surchargeable, même défaut
  height: var(--ui-button-height-small, var(--size-components-sm));
  ```

  **601 hooks** exposés, nommés `--ui-{famille}[-{partie}]-{propriété}[-{modifieur}]` (modifieur en dernier, comme les tokens). Le composant ne déclare jamais ces noms : les poser sur `:root` (tout le projet), sur un sélecteur (une zone) ou sur l'élément suffit à gagner.

  ```scss
  :root {
    --ui-button-height-small: 24px;
  }
  .toolbar ui-button {
    --ui-button-height-small: 24px;
  }
  ```

  Les **couleurs** restent volontairement sur les tokens sémantiques (clair/sombre × 3 marques × contraste WCAG) : seuls quelques hooks de couleur existent, là où repeindre **un exemplaire** est un cas d'usage réel (voile de masque, marqueur de chargement, reflet de squelette, remplissage de notation).

  Aucun défaut ne change : la migration a été vérifiée fichier par fichier en comparant le CSS compilé avant/après, hook réduit à son défaut.

- **`@4sh/ui-kit/styles/component-vars.scss`** — livré avec le package : un fichier prêt à copier où les **598 hooks** réglables sont déclarés à la valeur livrée par le starter, groupés par catégorie → composant → type de propriété (dimensions, espacements, bordures, typographie, couleurs), valeurs alignées sur une seule colonne. On le copie dans le projet, on le charge après `styles.css`, on change les valeurs voulues ; tel quel il ne change rien. Également téléchargeable depuis Storybook (_Spécifications → Thème & Système de Tokens_).

  Généré par `npm run docs:config`, **valeurs lues dans le CSS réellement compilé par Sass** : une expression comme `rem-calc(44px - 2 * $gutter)` y figure résolue (`2.25rem`), jamais sous sa forme SCSS. Les 19 hooks pour lesquels un réglage global n'a pas de sens (valeur dépendante de la variante rendue, propriété que le composant se pose lui-même) en sont volontairement absents — la colonne « Hook exposé » de chaque composant les donne.

  Le mot **« thème »** reste réservé au couple marque + clair/sombre (`ThemeService`, `BrandService`) : cette couche-ci s'appelle **hooks**, du nom des custom properties qu'elle règle, pour éviter trois sens différents du même mot.

- **Jetons sémantiques `navigation.highOutlined.*`** — la déclinaison _contour_ de la navigation (36 jetons : `content` / `surface` / `stroke` × 6 états, en clair et en sombre). `actions` avait la sienne depuis toujours, `navigation` n'exposait que `high` et `low`, c'est-à-dire uniquement des déclinaisons pleines. C'est ce que `ui-tabs` consomme désormais (voir `Changed`), et elle est disponible pour les autres composants de navigation.
- **`@4sh/ui-kit-schematics` embarque son `LICENSE` et ses README** (EN + FR). Le package déclarait `Apache-2.0` sans en fournir le texte, alors que la licence (§4a) demande qu'une redistribution en joigne une copie — d'autant plus ici que ce package existe pour que son contenu soit recopié ailleurs. Sa page npmjs, jusque-là vide, redirige maintenant vers `@4sh/ui-kit` : le compagnon ne s'installe jamais directement.

### Fixed

- **`ui-toggle` : le survol d'un interrupteur éteint virait au rose.** La piste lisait `--form-low-content-hover` (`secondary-400`) tant que l'interrupteur était éteint, alors que l'état allumé lisait `--form-high-content-hover` (`primary-400`) : survoler un toggle éteint le teintait dans la couleur secondaire, qu'aucun autre contrôle de formulaire n'emploie au survol. Les deux états partagent désormais le même jeton.
- **Un projet en sources copiées peut produire un build de production** (FSHSP-127). `ng add` câblait `styles` et `includePaths` dans `angular.json` mais laissait le budget `anyComponentStyle` d'`ng new` à sa valeur par défaut (4 kB d'avertissement, **8 kB d'erreur**). Ce seuil s'applique à _chaque_ composant, sur le CSS compilé : cinq composants du kit le dépassent — `ui-button` (55,7 kB), `ui-datepicker` (13,9), `ui-chip` (13,7), `ui-select` (12,6), `ui-autocomplete` (8,7). `ng serve` fonctionnait, les budgets ne vivant qu'en configuration `production` : c'est la CI qui tombait, sur un fichier que le consommateur n'a pas écrit et un seuil qu'il n'a pas choisi.

  Le schematic relève désormais ce budget à **128 kB d'erreur**, et retire l'avertissement — le laisser à 4 kB ferait remonter une quinzaine de composants à chaque build. 128 kB et pas au ras des 55,7 : les axes de `ui-button` sont multiplicatifs, un level de plus coûte 10,5 kB et une variante 17,2 kB, or les ajouter est précisément ce que le mode starter permet.

  Trois précautions pour ne pas défaire un réglage du projet : aucun budget n'est **ajouté** s'il est absent (sans seuil, rien ne casse), un seuil déjà plus haut n'est jamais **descendu**, et un seuil exprimé en pourcentage n'est pas touché.

  ⚠️ À savoir : `anyComponentStyle` ne peut pas être restreint à un fichier. Ce plafond s'applique donc aussi aux composants du projet, qui perd le garde-fou d'`ng new` sur son propre code. C'est un pare-chocs contre l'accident grossier, pas un garde-fou de performance — le poids de `ui-button` reste le vrai sujet.

- **`ui-file-upload` ne déclare plus `UiLink`** (FSHSP-124). Le composant l'avait dans ses `imports` sans jamais s'en servir — en mode `drag`, le lien est un `<span>` stylé à l'intérieur du `<label>`, un élément interactif y étant à éviter. Le build d'un projet en sources copiées remontait un `NG8113`, et `ui-file-upload` faisait recopier `ui-link` pour rien.
- **Les sources copiées n'importent plus depuis `node_modules`** (FSHSP-119). Un composant copié résolvait ses dépendances dans `node_modules/@4sh/ui-kit`, c'est-à-dire le code compilé : modifier le `ui-icon` copié n'avait aucun effet sur les composants qui l'utilisent, ce qui annulait la raison d'être du starter. Les 139 imports `@4sh/ui-kit/*` des sources sont désormais réadressés vers les copies voisines au moment de la copie.

  Un import passant par un barrel est résolu jusqu'au fichier qui porte réellement le symbole, et **scindé** si ses symboles sont dispersés (`{ UiIcon, UiIconType }` → `ui-icon.ts` + `ui-icon-families.ts`). Un symbole introuvable interrompt la copie en le nommant, plutôt que d'écrire un chemin faux.

  Effets de bord corrigés au passage : les dépendances copiées n'étaient utilisées par personne (copies mortes, suivies par `update` pour rien), le bundle embarquait le composant deux fois, et du code applicatif dépendait d'une `devDependency` — ce qui cassait en `npm ci --omit=dev`.

### Changed

- **`ui-tabs` passe sur les jetons `navigation.highOutlined.*`**, pour s'aligner sur le composant du UI Kit Figma. Les onglets lisaient `navigation.high.*`, la déclinaison _pleine_ : l'onglet actif était une pastille remplie (`surface.active` = `primary.500`, libellé blanc) **doublée** de la barre d'indicateur — deux marqueurs pour le même état, là où la maquette n'en montre qu'un.

  En `highOutlined`, `surface.active` est transparent et `content.active` prend la couleur d'accent : l'onglet actif se lit par son libellé coloré et la barre, comme dans Figma. Le survol passe de `primary.700` à `primary.500` sur le libellé (le fond `primary.100` est inchangé), et `default` / `disabled` sont identiques dans les deux déclinaisons.

  Les navigateurs de défilement de `ui-tab-list` suivent la même bascule, pour que la bande reste d'une seule pièce. Aucun changement de code ni d'API : seuls les noms de variables CSS changent. Les hooks exposés (`--ui-tabs-indicator-color`, `--ui-tabs-active-bar-color`) gardent leur nom ; seul leur défaut suit — désormais `var(--navigation-highoutlined-stroke-active)`.

  **En sombre**, `content.active` vaut `white.base` face à `grey.50` pour les onglets inactifs : l'écart est imperceptible, la barre d'indicateur porte seule la distinction. C'est la valeur du jeton Figma, conservée telle quelle.

- ⚠️ **Le parcours starter s'installe en une commande, et n'installe plus `@4sh/ui-kit`** (FSHSP-122) :

  ```bash
  ng add @4sh/ui-kit-schematics    # fondation + sélection des composants, enchaînées
  ```

  Auparavant il en fallait deux (`ng add @4sh/ui-kit` pour la fondation, puis `ng generate @4sh/ui-kit:add` pour les composants), et le kit restait dans `node_modules` : l'auto-complétion de l'IDE proposait ses imports alors que le projet doit utiliser ses copies locales. Les deux problèmes avaient la même cause — la porte d'entrée. Au moment du `ng add`, le compagnon n'était pas encore installé, d'où une `RunSchematicTask` différée, dans laquelle un prompt interactif ne tient pas.

  En entrant par le compagnon, le kit n'est plus installé du tout : plus rien à auto-importer, et le prompt fonctionne dans la foulée de la fondation. La version copiée est lue dans le compagnon lui-même, plus dans le `package.json` du consommateur.

  `--skip-components` pose la fondation seule, pour choisir les composants plus tard.

  **Le mode librairie est inchangé** : `npm i @4sh/ui-kit` puis `import { UiButton } from '@4sh/ui-kit/actions/ui-button'`. Seule la voie « sources copiées » change. `ng add @4sh/ui-kit` installe désormais le kit en `dependencies` (et non plus en `devDependencies`, qui n'avait de sens que pour piloter la CLI) et affiche laquelle des deux voies a été prise.

  **Migration** depuis `0.2.0` : désinstaller `@4sh/ui-kit` du projet (`npm rm @4sh/ui-kit`), les sources copiées n'en dépendant plus.

- ⚠️ **L'arborescence copiée est aplatie, et les fichiers transverses sortent de `components/`** (FSHSP-121). La copie reproduisait la structure d'`ng-packagr` (`ui-checkbox/src/lib/ui-checkbox.ts`, plus un barrel `src/public-api.ts`) : `src/` et `lib/` délimitent la surface publiée d'une librairie, et une fois le fichier chez le consommateur ils n'encadrent plus rien — ils enfouissaient chaque composant de deux niveaux. Elles disparaissent, le barrel avec :

  ```
  src/app/shared/
  ├── components/ui/{catégorie}/{ui-nom}/{ui-nom}.ts      ← uniquement des composants
  └── ui-core/{forms|motion|overlay|theming|types}/       ← directives de base, services, utilitaires, types
  ```

  Les bases partagées atterrissaient dans `components/ui/{catégorie}/_shared/`, c'est-à-dire des services (`theme.service`) et des utilitaires (`mask-engine`) rangés sous `components/`. Elles vivent maintenant dans `ui-core/`, en conservant leur regroupement par domaine. Les fichiers propres à un composant (`date-utils`, `ui-alert.types`, `ui-toast.service`…) restent à côté de lui : ils ne servent qu'à lui.

  **Mise à jour non triviale** pour un projet déjà installé en `0.2.0` : les chemins des fichiers copiés changent tous, donc `update` ne reconnaîtra pas les anciens. Déplacer les fichiers existants avant de lancer la commande, ou repartir d'un `add` propre.

- **Les constantes partagées de `_ui-config.scss` sont retouchables sans recompiler.** Ma migration n'avait hookifié que les variables _locales_ de chaque composant : il n'existait aucun levier de **catégorie** en mode package. Changer la taille de tous les contrôles de formulaire demandait soit de poser un hook par composant, soit de retoucher `--size-components-2xs`, qui déborde (il alimente aussi `ui-tag`, `ui-select`, `ui-autocomplete`, `ui-datepicker`).

  **16 constantes** exposées, nommées d'après leur variable SCSS (`$form-control-size` → `--ui-form-control-size`). Les composants les _interpolant_, la cascade se construit d'elle-même :

  ```css
  var(--ui-checkbox-box-size, var(--ui-form-control-size, var(--size-components-2xs)))
  /*      ↑ composant             ↑ catégorie                ↑ token                 */
  ```

  Une constante qui **référence une autre constante** (`$form-focus-ring-width: $focus-ring-width`) ne prend pas de nom propre : elle hérite de celui qu'elle vise, si bien que `--ui-focus-ring-width` couvre à lui seul les anneaux de focus du kit. Les quatre tailles d'avatar restent des nombres bruts (elles passent par `rem-calc()`), déjà atteignables par `--ui-avatar-size*`.

- **`ui-icon` : l'échelle et la surcharge d'exemplaire sont deux niveaux distincts.** `--ui-icon-size` était répété dans les cinq entrées de l'échelle ; il est désormais lu une seule fois, au-dessus d'une variable interne que chaque taille alimente. Comportement inchangé : `--ui-icon-size-{sm,md,default,lg,xl}` retouche un **pas de l'échelle** (donc toutes les icônes de cette taille), `--ui-icon-size` force **un exemplaire** quel que soit son `size` — c'est par là qu'un composant dimensionne l'icône qu'il projette.
- **`ui-checkbox` et `ui-select` exposent la taille de leur marqueur** (`--ui-checkbox-icon-size`, `--ui-select-checkbox-icon-size`). Rétrécir la case (`--ui-checkbox-box-size`) laissait la coche à la taille de l'échelle d'icônes, donc débordante. Le défaut de ces deux hooks pointe sur le pas d'échelle utilisé, si bien que retoucher l'échelle continue de traverser.
- **`--ui-card-padding` ne se relit plus sous ce nom : le miroir relisible est `--ui-card-gutter`.** Une custom property ne pouvant pas se référencer elle-même, le nom déclaré par la carte et le nom surchargeable devaient être distincts. `--ui-card-padding` devient le **point de surcharge** (gouttière de la carte), `--ui-card-gutter` la valeur à **relire** pour regouttiérer un contenu `contentFlush` : `padding-inline: var(--ui-card-gutter)`.
- **`--ui-sidebar-width`, `--ui-sidebar-rail-width`, `--ui-empty-state-media-size`, `--ui-empty-state-media-color`, `--ui-input-group-radius`, `--ui-input-group-overlap` et `--ui-skeleton-shine` sont désormais réellement surchargeables.** Ces sept custom properties étaient documentées comme des points de surcharge, mais le composant les **déclarait** sur son propre élément : une valeur posée par le consommateur sur `:root` était systématiquement écrasée (et, pour `--ui-skeleton-shine`, écrasée en mode sombre uniquement). Les déclarations passent sur un miroir privé, le nom public reste lu en amont. Mêmes valeurs par défaut.
- **`ui-tab-list` : `--ui-tabs-active-bar-color` / `--ui-tabs-active-bar-size` deviennent `--ui-tabs-indicator-color` / `--ui-tabs-indicator-thickness`** (le mot `active` se lisait comme un état alors que la convention met le modifieur en dernier). Les anciens noms restent honorés une version, en repli.
- **L'en-tête de traçabilité des fichiers copiés par `ng generate @4sh/ui-kit:add` porte la mention de licence** (`Apache-2.0 — Copyright 2026 4SH.`) en plus de son origine et de sa version. Une fois copié, le fichier vit dans le dépôt du consommateur, où plus rien n'indiquait sous quels termes il est fourni. Conséquence attendue : le premier `ng generate @4sh/ui-kit:update` suivant cette version affiche un diff d'une ligne en tête de chaque fichier installé.
- **L'en-tête de traçabilité des fichiers copiés tient sur une ligne et porte la mention de licence** :

  ```ts
  // @4sh/ui-kit@0.2.0 — Apache-2.0 — Copyright 2026 4SH.
  ```

  Il reprenait la catégorie, le nom du composant et le chemin du fichier — trois informations que le fichier copié porte déjà dans son emplacement. Ne reste que la version d'origine, et la licence : une fois copié, le fichier vit dans le dépôt du consommateur, où plus rien n'indiquait sous quels termes il est fourni. Conséquence attendue : le premier `ng generate @4sh/ui-kit-schematics:update` suivant cette version affiche un diff d'une ligne en tête de chaque fichier installé.

## [0.2.0] - 2026-08-14

Première version depuis la mise en package à porter de véritables changements de
composants — les deux précédentes (`0.1.1`, `0.1.2`) publiaient un tarball
inchangé. Elle est `MINOR` malgré la rupture qu'elle contient : tant que la
version commence par `0.x`, l'API est déclarée instable et une `MINOR` peut
casser, à condition de le documenter (voir [VERSIONING.md](./docs/VERSIONING.md)).

⚠️ **Mise à jour non triviale** : les 53 entry points sont renommés (réorganisation
par catégorie), donc **tous les imports de composants changent**. Voir la section
`Changed`.

### Added

- **Starter : installer les composants dans son code source plutôt que comme dépendance** (FSHSP-109). À la manière de shadcn/ui ou spartan-ng, un projet peut désormais **copier** les sources des composants chez lui et les posséder, au lieu de consommer `@4sh/ui-kit` en dépendance figée :

  ```bash
  ng add @4sh/ui-kit                    # fondation : styles, tokens, angular.json
  ng generate @4sh/ui-kit:add           # sélection interactive (a = tout)
  ng generate @4sh/ui-kit:add --all     # sans prompt
  ng generate @4sh/ui-kit:update        # diff par composant, appliquer/ignorer
  ```

  `add` résout les dépendances entre composants (`ui-select` entraîne `ui-icon`, `ui-field`, `ui-spinner`…) par analyse des imports, sans liste à maintenir. Chaque fichier copié porte son origine en en-tête, et `ui-kit.json` recense ce qui est installé. `ng add` embarque aussi la **chaîne de génération des tokens** (JSON, `tokens.config.json`, `tokens.build.mjs`) : un projet peut rebrander sans attendre une release du kit.

  Le kit gagne pour cela une façade de schematics (`collection.json` + `schematics/index.cjs`) qui ne contient aucune logique et délègue à un package compagnon, **`@4sh/ui-kit-schematics`**, publié en même temps et sous le même numéro (voir [`PUBLISHING.md`](./docs/PUBLISHING.md)). Les sources brutes restent hors du package compilé : un consommateur classique de `@4sh/ui-kit` ne les télécharge pas. Aucun changement pour lui.

- **`prevAriaLabel` / `nextAriaLabel` sur `ui-tab-list`** : les noms accessibles des navigateurs de défilement (mode `scrollable`) étaient écrits en dur dans le template, seuls libellés du kit qu'une application ne pouvait pas traduire. Ils deviennent des entrées, avec les textes actuels comme valeurs par défaut — aucun changement de rendu.

### Fixed

- **`dropdownOverlayPositions` ne repositionnait jamais horizontalement le panneau** (`ui-select`, `ui-autocomplete`, `ui-input-tags`, `ui-datepicker`) : `originX`/`overlayX` restaient figés à `start`, donc un champ proche du bord droit ouvrait un panneau à moitié caché au lieu de le basculer à gauche. Ajout des positions `end`-alignées en repli, sur le même principe déjà utilisé par `ui-tooltip`/`ui-popover`/`ui-context-menu` (FSHSP-112).

### Changed

- **BREAKING** : les 53 composants `ui-*` sont réorganisés par catégorie dans `projects/ui-kit/` (`actions/`, `forms/`, `informative/`, `layout/`, `navigation/`, `table/`, `base/` — mapping dans `storybook/docs/Overview.mdx`), pour restaurer le regroupement perdu lors de la migration vers le package npm (FSHSP-83). Conséquence inévitable : `ng-packagr` calcule le chemin public d'un entry point secondaire à partir de son chemin sur disque (aucune option pour le découpler), donc **chaque import change** — ex. `@4sh/ui-kit/ui-button` devient `@4sh/ui-kit/actions/ui-button`. Voir `storybook/docs/Overview.mdx` pour la liste complète des 53 renommages. Un projet qui consomme `@4sh/ui-kit` doit mettre à jour tous ses imports de composants.

## [0.1.2] - 2026-08-12

Version d'outillage et de CI : **aucun changement de code des composants**, le
contenu publié du package est identique à la `0.1.1`. Rien de ce qui suit ne part
dans le tarball — workflows, scripts, documentation et stories restent hors du
package.

Elle est publiée délibérément malgré cela : c'est la première version à emprunter
la chaîne `tag + release GitHub` introduite ici même, et le dépôt ne portait
jusqu'à présent **aucun tag** (`0.1.0` et `0.1.1` sont sur npm sans trace côté
GitHub). Éprouver ce dispositif sur une version sans enjeu fonctionnel vaut mieux
que de le découvrir lors d'une vraie livraison de composants.

### Added

- **CI de vérification sur les pull requests** ([`pr-checks.yml`](.github/workflows/pr-checks.yml)). Jusqu'ici **aucun workflow ne se déclenchait sur une PR** : `deploy-storybook` part sur un push `main`, `publish-ui-kit` est manuel. Rien ne vérifiait qu'une PR compile, lint ou type-check avant d'atterrir sur `main` — la branche depuis laquelle le package est publié. Deux jobs parallèles : `checks` (lint, typecheck de l'app de démo, `components:check`) et `storybook` (build du livrable public, isolé parce qu'il est de loin le plus lent). `npm ci` suffit à prouver que les 53 entry points compilent, son `postinstall` enchaînant `tokens:build` → `ui-kit:build` → `docs:config`. Nouveau script **`lint:check`** : `npm run lint` porte `--fix`, donc _répare_ au lieu de signaler — en CI il n'aurait jamais rien détecté. ⚠️ Un workflow vert ne bloque rien par lui-même : il reste à exiger ces statuts dans les règles de protection de `main`.
- **Release GitHub créée automatiquement à la publication npm.** Le dépôt ne portait **aucun tag** : `0.1.0` et `0.1.1` sont sur npm sans trace côté GitHub, l'étape `npm version` + `git push --follow-tags` de [`VERSIONING.md`](docs/VERSIONING.md) ayant sauté aux deux publications — et rien ne la rattrapait. Nouveau job `release` dans [`publish-ui-kit.yml`](.github/workflows/publish-ui-kit.yml), déclenché **après** un `npm publish` réussi : il pose le tag `vX.Y.Z` sur le commit publié et ouvre la release GitHub, dont le corps est la section `[X.Y.Z]` de ce fichier. L'accrochage à la publication est délibéré — la release ne peut pas mentir, elle n'existe que si le registre a accepté le tarball, et elle désigne le commit exact qui l'a produit, là où un tag posé à la main ne garantit ni l'un ni l'autre. La version est lue dans le `package.json` **du tarball** (`dist/ui-kit/`) et non dans `projects/ui-kit/` : le tag ne peut ainsi nommer que ce qui a réellement été accepté par le registre. Job **séparé de `publish`** parce qu'il réclame `contents: write`, droit qui n'a rien à faire dans celui qui porte déjà l'autorisation de publier sur npm. Idempotent : relancé seul après un échec passager, il constate la release existante au lieu d'échouer. Un tag poussé manuellement reste réutilisé tel quel, la voie `npm version` n'est pas cassée — seulement rendue inutile.
- **`scripts/changelog.section.mjs`** : extrait le corps de la section d'une version du CHANGELOG, et échoue en listant les sections présentes si elle manque (l'erreur la plus probable étant un décalage d'une version entre `package.json` et le CHANGELOG, invisible sans cette liste). Appelé **dès le job `verify`**, donc un lancement en `dry_run: true` signale une section absente ou vide — et affiche les notes telles qu'elles seront rendues — _avant_ la publication irréversible, au lieu de produire une release vide après coup.
- **Le job de publication trace ce que npm compare.** Deux runs ont été nécessaires pour diagnostiquer l'échec de la `0.1.1`, parce que l'échec d'authentification OIDC est **silencieux** : quand le registre ne trouve aucun Trusted Publisher correspondant, il ne renvoie pas d'erreur, npm continue simplement sans identifiant et meurt plusieurs étapes plus loin sur un `ENEEDAUTH` qui ne désigne pas sa cause. Le contrôle pré-publication affiche désormais les trois _claims_ comparés par npm (dépôt, nom du workflow, environment), à recopier littéralement dans le formulaire npmjs en cas de récidive, et échoue explicitement si GitHub n'émet aucun jeton OIDC pour le job (`ACTIONS_ID_TOKEN_REQUEST_URL` absent = permission `id-token: write` manquante).

### Changed

- **`docs/PUBLISHING.md` : le repli par token est retiré, pas remplacé.** La `0.1.1` ayant été publiée par OIDC, le token du compte de service a été révoqué et le secret `NPM_TOKEN` supprimé de GitHub — la procédure de repli décrivait donc un montage qui n'existe plus. Elle est remplacée par une consigne explicite : en cas de panne de l'OIDC, **ne pas remettre de token dans la CI** (c'est la tentation du moment de crise, et npm restreint de toute façon progressivement les tokens contournant la 2FA), mais attendre ou publier interactivement depuis un poste. La section « Publier depuis un poste » est corrigée en conséquence : elle affirmait qu'un token était nécessaire, alors qu'un `npm login` ouvre une **session** liée au poste — pas un secret partagé —, et elle précise désormais la contrepartie assumée (`--provenance=false` : hors CI, il n'y a aucun workflow vérifiable à attester).

### Fixed

- **`ng lint` échouait sur 29 erreurs, masquées par le `--fix`.** Toutes portaient sur `@angular-eslint/component-selector` dans les `*.stories.ts` du kit : les stories hébergent leurs propres composants de démonstration pour éprouver un scénario que le kit ne doit pas livrer, et leurs sélecteurs ne portent évidemment pas le préfixe `ui` — lequel réserve l'espace de noms public du kit. Quatre conventions coexistaient (`demo-*`, `toast-demo-*`, `sb-*`, `storybook-ui-*`) ; élargir la règle à ces quatre préfixes aurait gravé le désordre. Les 13 sélecteurs déviants sont normalisés en **`demo-*`** (usages dans les templates compris), et la règle est réécrite pour ce préfixe **sur les seuls `*.stories.ts`** — elle reste pleinement appliquée sur tout ce qui est packagé. Vérifié en Storybook headless sur les 14 stories concernées (`ui-toast`, `ui-alert`, `ui-progress-bar`, `ui-image`) : toutes rendent leur contenu, aucun élément inconnu laissé vide — un sélecteur renommé mais oublié dans un template ne produit aucune erreur de build, seulement un composant muet.
- **Storybook publié : les stories sans `template` échouaient sur `Cannot read properties of undefined (reading 'selector')`.** Les stories importent le kit depuis son package construit (`@4sh/ui-kit/*` → `dist/ui-kit`), dont les fesm sont en compilation _partielle_ : le linker Angular ne réémet `setClassMetadata()` qu'en mode JIT, si bien que dans `build-storybook` les classes arrivent avec leur `ɵcmp` mais **sans** `__annotations__`. Or `@storybook/angular` lit ces annotations pour fabriquer le template implicite d'une story qui ne déclare que `component` + `args` (`computesTemplateFromComponent`) et plantait sur le sélecteur manquant — toutes les stories sans `template` explicite, et donc les pages de doc qui les affichent, rendaient une erreur au lieu du composant. Nouveau décorateur global [`withComponentMetadata`](storybook/restore-component-metadata.ts) : il republie le sélecteur lu dans `ɵcmp` comme annotation avant le rendu (les inputs/outputs, eux, sont déjà lus dans `ɵcmp` par Storybook). Vérifié sur un build statique complet : 0 erreur de ce type sur les 587 stories, contre l'ensemble des stories sans template auparavant.
- **`ui-tooltip` : infobulle jamais attachée à son déclencheur dans le Storybook publié (`this.tooltipEvent is not a function`).** Même racine que l'entrée précédente, autre symptôme. Dans son `ngAfterViewInit`, `@storybook/angular` réassigne sur l'instance du composant de la story tous les args qu'il ne reconnaît pas comme input/output ; son repli (`ɵgetComponentDef`) ne lit que `ɵcmp`, donc pour une **directive** (`ɵdir`) sans annotations aucun input n'était reconnu : tous les args étaient écrits en dur sur l'instance et **écrasaient les champs de signaux** (`tooltipEvent = 'both'` à la place du signal). `bindTriggerEvents()` levait alors un `TypeError` et l'infobulle ne s'attachait jamais — aucune story de `ui-tooltip` ne fonctionnait sur le site publié. [`withComponentMetadata`](storybook/restore-component-metadata.ts) couvre désormais `ɵdir` et restitue aussi les **inputs / outputs** lus dans la définition ; l'annotation republiée est toujours un `Component`, Storybook n'inspectant que celles qui sont `instanceof Component` (`getComponentDecoratorMetadata`) — une annotation `Directive` serait purement ignorée.
- **Stories du chemin d'erreur de `ui-image` / `ui-avatar` : plus de dépendance DNS.** `Error_With_Fallback`, `Error_Placeholder`, `Error_SVG_Fallback` et `Broken_Image_Fallback` pointaient `https://example.invalid/…`. Le TLD réservé garantit bien l'échec, mais au prix d'une résolution DNS sortante et d'un `ERR_NAME_NOT_RESOLVED` en console — bruit permanent qui masque les vraies erreurs au balayage, et comportement dépendant du réseau. Elles visent désormais un chemin local inexistant (`./nope-missing.jpg`), servi en 404 par Storybook : même chemin d'erreur déclenché, sans requête externe.

## [0.1.1] - 2026-08-12

Version de documentation et d'outillage : **aucun changement de code des
composants**, le contenu publié du package est identique à la `0.1.0` à ses
fichiers de documentation près. Elle corrige le README affiché sur npmjs, qui
annonçait encore « pas encore publié sur un registre » sur la page du package
publié.

### Fixed

- **Publication `0.1.1` en échec `E404` : `registry-url` neutralisait l'OIDC.** L'input `registry-url` d'`actions/setup-node` écrit `//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}` dans le `~/.npmrc`. Depuis le retrait du token (FSHSP-90), cette variable n'existe plus : la ligne résolvait donc un jeton **vide**, npm se considérait authentifié, sautait entièrement l'échange OIDC, et le registre rejetait le `PUT` anonyme par un `E404 Not Found` trompeur (il masque un 403 sur les packages scopés — la provenance, elle, était bien signée, ce qui égarait le diagnostic). L'input est retiré du job `publish` : `registry.npmjs.org` est de toute façon le registre par défaut. Le contrôle pré-publication vérifie désormais qu'aucun `_authToken` n'est configuré, en lisant les `.npmrc` directement — `npm config get` refuse de retourner les options d'authentification.

### Changed

- **Publication npm en Trusted Publishing (OIDC) : le token disparaît.** L'étape _Publish_ de [`publish-ui-kit.yml`](.github/workflows/publish-ui-kit.yml) n'utilise plus `NODE_AUTH_TOKEN` — GitHub Actions présente le jeton OIDC du job, npm vérifie qu'il provient bien du dépôt `4sh/starter-angular` et du workflow `publish-ui-kit.yml` déclarés sur la page du package, puis délivre un droit de publication éphémère valable le temps du job. Plus aucun secret à stocker ni à faire tourner, plus de « bypass 2FA », et la provenance devient implicite (le drapeau `--provenance` reste explicite pour qu'une publication de repli par token soit attestée elle aussi). L'**environment protégé `npm-publish` reste indispensable** : l'OIDC garantit _d'où_ vient la publication, jamais _qui_ l'a décidée — et son nom fait désormais partie de la déclaration côté npm, donc le renommer casse l'authentification. Nouvelle étape de garde : Trusted Publishing exige **npm ≥ 11.5.1**, le `.nvmrc` (Node 24.15.0) embarque 11.12.1, et le workflow échoue désormais avec un message explicite si une descente de version repassait sous le seuil, au lieu d'un `401` obscur. [`docs/PUBLISHING.md`](docs/PUBLISHING.md) : la section « Authentification » décrit la configuration OIDC (dépôt, workflow, environment, _allowed actions_ limitées à `npm publish`), distingue explicitement **publier** (accessible à tout _required reviewer_ GitHub, sans compte npm ni 2FA) d'**administrer le package** (connexion au compte partagé, donc second facteur — d'où la discipline associée : codes de récupération en Vaultier, deux facteurs sur deux appareils, et bascule à terme vers des mainteneurs nominatifs), et rétrograde tout le montage token — compte de service `4sh-package-admin`, token granulaire, bypass 2FA, plages IP — en **solution de repli documentée mais non entretenue**.

### Added

- **`npm run components:check`** ([`scripts/components.check.mjs`](scripts/components.check.mjs)) : garde-fou sur les listes de composants écrites à la main. Le kit était énuméré à quatre endroits sans rien pour les relier — les deux README du package, [`components-index.md`](docs/components-index.md) et la page Storybook « Composants » — et ils avaient divergé, d'où un décompte annoncé faux. Le script confronte les trois listes aux entry points réellement présents sur le disque : table des familles des README == disque, tout entry point porteur d'une story présenté dans `Overview.mdx`, tout entry point coché ✅ dans l'index (l'index gardant le droit d'ajouter des ✅ pour un composant livré dans l'entry point d'un autre, et des ⬜ pour ceux à construire). Ces listes ne sont **pas générées** : le regroupement par famille et l'ordre pédagogique sont éditoriaux, pas déductibles du disque — elles sont donc validées, pas produites. Branché sur `npm run docs:config:check`, qui devient la vérification unique de la doc.

### Fixed

- **Doc dédupliquée : trois recouvrements supprimés.** La page Storybook « Package npm » recopiait le README du package (import à la carte, table des entry points, feuille de styles, socle `forms`) — soit trois copies de la même page d'installation depuis l'ajout du README anglais, et c'est par là que le décompte d'entry points avait divergé. Elle se réduit à ce qui lui est propre (le mécanisme du package unique, un exemple d'import, le renvoi aux pages de composants) et pointe vers le README publié, qui fait foi. [`docs/VERSIONING.md`](docs/VERSIONING.md) reproduisait par ailleurs la table des préfixes de branche et le format de commit de `.claude/rules/git-conventions.md`, en les qualifiant d'« optional » là où la règle les impose : il n'en garde que ce qui relève du versionnage (le préfixe annonce le bump attendu, `!` force un MAJOR) et renvoie pour le reste.
- **README racine remis en phase avec le dépôt.** C'est la page d'accueil du dépôt public, et elle décrivait encore l'arborescence d'avant la mise en package : les six chemins de la section « Structure » (`src/app/core/service`, `src/app/core/controlValueAccessor`, `src/app/shared/components/ui`, `src/app/shared/types`, `src/app/pages`, `src/styles/src/generated`) avaient tous disparu ou déménagé. La structure décrit désormais les deux moitiés réelles du dépôt — `projects/ui-kit/` (le package) et `src/` (l'application de démonstration) — la ligne « Tokens » du tableau Stack pointe la bonne sortie (`projects/ui-kit/styles/generated/`), la recette « Adding a new component » décrit le vrai layout d'un entry point (`ng-package.json` + `src/lib/` + story/MDX **hors** de `src/`, donc jamais packagées) et les globs Storybook cités correspondent à `storybook/main.js`. Ajout d'un lien vers le package npm et le Storybook publié en tête, d'une note sur le `ui-kit:build` préalable à `start`/`serve`/`build` (l'app de démo consomme le kit **construit**, comme un consommateur externe), d'une section sur la génération du `## Theming` par `docs:config`, et d'une table de liens vers `docs/VERSIONING.md`, `docs/PUBLISHING.md` et `AGENTS.md`.
- Le décompte des composants du kit annonçait **54** partout (les deux README du package et `AGENTS.md`) alors que le dépôt en contient **53** — la table des entry points du README en listait d'ailleurs bien 53.

### Changed

- **`@4sh/ui-kit` : README du package en anglais, version française conservée.** npmjs comme GitHub n'affichent que le `README.md` du tarball, sans négociation de langue : la seule mise en œuvre bilingue qui fonctionne des deux côtés est un fichier par langue relié par un sélecteur en tête de page. Le README d'origine devient [`projects/ui-kit/README.fr.md`](projects/ui-kit/README.fr.md) (contenu inchangé, `git mv` pour préserver l'historique) et `README.md` en porte la traduction anglaise — c'est donc l'anglais qui s'affiche désormais sur la page npm. `README.fr.md` est déclaré dans les `assets` de [`ng-package.json`](projects/ui-kit/ng-package.json) : `ng-packagr` ne copie automatiquement que `README.md` et `LICENSE`, le lien « Français » aurait sinon pointé vers un fichier absent du tarball. La `description` npm du package, elle aussi en français (affichée sous le titre sur npmjs et dans les résultats de recherche), passe en anglais.

## [0.1.0] - 2026-08-11

Première version publiée du package **`@4sh/ui-kit`** (voir la section
« Added » ci-dessous) et état initial du Design System : 54 composants
`ui-*` headless, pipeline de design tokens, documentation Storybook.

### Added

- **`@4sh/ui-kit`** : nouveau package npm (Angular Package Format, `ng-packagr`) pour consommer les composants du design system en dehors de ce repo, avec un _secondary entry point_ par composant (ex. `import { UiIcon } from '@4sh/ui-kit/ui-icon'`) pour un import à la carte et du tree-shaking. **Migration terminée : les 54 composants `ui-*` du kit vivent désormais dans le package**, un _secondary entry point_ chacun (`@4sh/ui-kit/ui-select`, `@4sh/ui-kit/ui-table`…), plus les entry points transverses **`forms`** (socle des champs : `BaseControlValueAccessor` — sorti de `src/app/core/` —, `BaseFieldControl`, `BaseFormField`, `dropdownOverlayPositions`, `mask-engine`, `option-resolver`, `format-label`, `warn-missing-accessible-name`), **`theming`** (`ThemeService`, `BrandService`), **`motion`** (directive `UiMotion`), **`overlay`** (`closeOnNavigation`) et **`types`**. `src/app/` ne contient plus que l'application de démonstration et ses composants `domain/`. **Le socle SCSS du design system (tokens générés, `utils`, `_ui-config`, couche de base, typographie, motion) a été déplacé de `src/styles/` vers `projects/ui-kit/styles/`** : le package ne dépend plus d'aucun fichier de l'application hôte et se build donc de façon autonome. Il **livre** désormais sa feuille globale compilée (`dist/ui-kit/styles.css` — tokens 3 marques × clair/sombre × responsive, base, typographie, presets de motion) **et** ses sources SCSS, pour qu'un consommateur puisse écrire ses propres composants avec `@use 'utils'`. `utils.scss` n'expose plus que de l'API (les classes utilitaires `_helpers.scss` en sont sorties : chaque `.scss` de composant étant une unité de compilation Sass distincte, elles étaient dupliquées dans les 54 composants). Documentation : `projects/ui-kit/README.md` (page npmjs) et page Storybook **« Package npm »**. Version initiale **0.1.0** (alignée sur le repo, API instable en `0.x` — cf. `docs/VERSIONING.md`), métadonnées npm complétées (`repository`, `homepage`, `keywords`, `bugs`, `author`) et **`peerDependencies` corrigées** : `@angular/cdk`, `@angular/platform-browser` et `rxjs` manquaient alors qu'ils sont réellement importés (17 usages du CDK pour l'overlay/focus-trap/a11y) — un consommateur aurait eu des erreurs de résolution. Nouveau script `npm run ui-kit:pack` : produit un tarball installable dans n'importe quel projet (`npm install /chemin/4sh-ui-kit-0.1.0.tgz`), ce qui permet de consommer le kit **avant** toute publication sur un registre. Publication outillée : registre **npm public** sous l'organisation **`4sh`**, via le workflow manuel [`publish-ui-kit.yml`](.github/workflows/publish-ui-kit.yml) (jamais automatique — une version publiée est irréversible), découpé en deux jobs : `verify` construit et affiche le contenu exact du tarball (`dry_run` à `true` par défaut, ni secret ni approbation requis), `publish` est rattaché à l'**environment protégé `npm-publish`** et **attend l'approbation d'un reviewer** avant même de pouvoir lire le secret. Publication avec `--provenance` (lien vérifiable tarball ↔ commit, ce qui restitue la traçabilité malgré le compte de service partagé) et token du compte `4sh-package-admin` lu depuis le secret GitHub `NPM_TOKEN` — il ne transite par aucun poste de développeur. `publishConfig.access: "public"` est figé dans le `package.json` : sans lui un package _scopé_ est `restricted` par défaut et la publication échoue en 402. Procédure complète : `docs/PUBLISHING.md`. Licence **Apache-2.0** (texte canonique intégral dans `projects/ui-kit/LICENSE` + `NOTICE`, tous deux livrés dans le tarball) : permissive comme MIT mais avec concession de brevets explicite, standard des design systems d'entreprise, et compatible avec l'intégration dans des applications clientes propriétaires (contrairement à une licence copyleft).
  - Les imports inter-composants **doivent** utiliser le nom réel du package (`@4sh/ui-kit/ui-icon`), jamais un raccourci (`ui-kit/ui-icon`) : ng-packagr n'enregistre une dépendance entre deux entry points que si l'import commence littéralement par le nom du package, sinon l'ordre de build reste indéterminé et le build échoue par intermittence (« Cannot find module … »). Les `paths` du `tsconfig.json` mappent donc `@4sh/ui-kit/*` → `dist/ui-kit/*`.
- `ui-datepicker` : **contrat de valeur `Date`/ISO explicite** — nouvel input **obligatoire, sans défaut, `valueType: 'date' | 'iso'`** (`input.required` — délibérément pas de valeur implicite, pour forcer chaque consommateur à choisir le mode correspondant au type réel de son modèle plutôt que d'hériter d'un défaut silencieux). `writeValue` accepte toujours `Date` **ou** string ISO `"yyyy-MM-dd"` (auto-détecté, mixable dans les tableaux `multiple`/`range`) quel que soit le mode ; ce qui est **émis** (`valueChange`, et donc ce qui atterrit dans le `FormControl`) suit strictement `valueType`, jamais un mélange silencieux. `'date'` garde un `Date` de bout en bout — normalisé à minuit quand aucune heure n'est affichée — pensé pour un DTO round-trippé par `class-transformer` (`@Type(() => Date)`) : le formulaire manipule directement le `Date` du DTO, la conversion vers le JSON du backend restant la responsabilité de `class-transformer`. `'iso'` émet une string `"yyyy-MM-dd"` (`"yyyy-MM-ddTHH:mm"` avec `showTime`/`timeOnly`) — pour un champ lu/écrit directement contre un `LocalDate` backend sans DTO entre les deux (ex. un filtre de recherche) ; toujours construite à partir des composants locaux du `Date` (`getFullYear()/getMonth()/getDate()`), jamais `date.toISOString()` (bascule en UTC, peut décaler le jour selon le fuseau). Story `IsoValueType`. **Masque de saisie clavier** — l'auto-formatage des « / » (comme `<input type="date">` natif) s'insère désormais pendant la frappe (`allowInput`), dès qu'un segment jour/mois est complet (même principe qu'un champ d'expiration de carte bancaire). Le moteur de masquage est extrait dans un nouvel utilitaire partagé **`mask-engine.ts`** (même dossier que `overlay-positions.ts`), consommé à la fois par `ui-input-mask` (refactor pur, zéro changement de comportement — vérifié en Storybook headless sur ses 9 stories) et `ui-datepicker`. `minDate`/`maxDate`/`disabledDates` acceptent désormais aussi des strings ISO `"yyyy-MM-dd"` en plus de `Date` (mixables dans `disabledDates`, indépendamment de `valueType`), pratique quand les bornes viennent déjà d'un backend au même format que la valeur ; une string malformée dégrade silencieusement en « pas de contrainte ». **Correctifs a11y** : `aria-label` de date complète sur chaque jour (« 8 juillet 2026 », y compris avec la cellule `#date` custom), `aria-live="polite"` sur le titre en vue multi-mois (déjà présent en mono-mois), en-têtes de semaine `role="columnheader"` + `aria-label` du nom complet du jour (texte visible resté abrégé) au lieu d'un `aria-hidden` qui les rendait muets pour un lecteur d'écran, indicateur de plage (`range`) complété d'une bordure haut/bas (`actions.high.stroke.default`) pour ne plus dépendre de la seule couleur sur les jours intermédiaires. Stories `MinMaxIso`, `DisabledDates`, `CustomFormat`, `AutoFormattedInputEnUs`
- `ui-select` : **`panelWidth`** — le panneau d'options reprenait systématiquement la largeur du champ, ce qui tronque les libellés dès que le champ est volontairement compact (un indicatif téléphonique dans un `ui-input-group`) alors que les options ne le sont pas. `panelWidth="auto"` dimensionne le panneau sur son contenu **en gardant la largeur du champ comme plancher** (`min-width`), une taille CSS (`panelWidth="320px"`) la fixe, et l'absence de valeur conserve le comportement d'origine. Story `Panel Width` + section MDX dédiée
- `ui-input-group` : **groupe champ + compléments** — nouveau couple `ui-input-group` (la rangée) + `ui-input-group-addon` (une cellule non interactive : texte, icône, `ui-checkbox`, `ui-radio`). Les contrôles (`ui-input`, `ui-input-number`, `ui-select`, `ui-button`…) se posent **directement** dans le groupe et gardent leur API, leurs états et leurs tokens : le groupe ne fait que la mise en rangée et le raccord visuel — coins arrondis aux **seules extrémités** et trait partagé **fusionné** (recouvrement égal à `--stroke-default`), pour que l'ensemble se lise comme un champ unique. Les enfants étant **projetés**, aucune règle scopée ne peut les atteindre : la mise en forme passe par les custom properties exposées par chacun (`--ui-field-radius` — **nouveau hook** sur `ui-field`, donc valable pour tous les champs qui en héritent —, `--ui-button-radius`, `--ui-input-group-item-radius`), qui traversent la frontière de composant sans `::ng-deep`. Un composant tiers rejoint le raccord dès qu'il lit `--ui-input-group-item-radius`. L'item qui prend le focus est remonté au-dessus de ses voisins, son anneau de focus restant entièrement visible malgré le recouvrement. Le `size` du groupe est transmis aux add-ons projetés (par DI), les contrôles gardant le leur. Aucun rôle ni attribut ARIA propre (enveloppes de présentation), aucune interaction clavier ajoutée. Stories `Basic`, `Multiple`, `Button`, `Checkbox & Radio`, `Select`, `Phone Number` (indicatif international avec drapeaux SVG locaux) et `Small`
- `ui-rating` : **demi-étoiles** — l'input **`allowHalf`** fait passer le pas de sélection à **0,5**. Le clic sur la moitié avant d'une étoile pose la demi-note, la moitié arrière l'étoile entière (le survol prévisualise la moitié pointée, `mousemove` et non plus `mouseenter`), et le `step` de l'`<input type="range">` sous-jacent suit : les flèches du clavier avancent aussi de 0,5 en 0,5 et le lecteur d'écran annonce « 3,5 ». `cancel` continue de porter sur la valeur exacte — re-cliquer la même moitié remet à `null`. Le remplissage partiel n'utilise **aucune icône « demi » dédiée** : l'icône « on » est superposée à l'icône « off » puis rognée en `clip-path` à la fraction voulue (miroir en RTL), donc la demi-étoile marche telle quelle avec n'importe quelle famille d'icônes **et** avec les templates projetés `#onIcon`/`#offIcon`, dont le contexte gagne `fill` (0 → 1). Une valeur hors pas (moyenne `4.3` poussée par l'hôte) est tronquée **à l'affichage** au pas courant, jamais réécrite dans le modèle
- Storybook : **sélecteur de marque dans la barre d'outils** — l'UI Kit s'affichait toujours en `brand1`, la bascule multi-marque n'existant que dans l'explorateur `Foundations → Colors`, où elle ne colore que sa propre palette. Le sélecteur **Brand** (`brand1`/`brand2`/`brand3`) rejoue `BrandService` : `[data-brand]` posé sur `<html>`, retiré pour `brand1` que `:root` sert déjà. Il s'applique donc à **toutes** les stories, et se combine avec le mode clair/sombre. Tout est isolé dans `storybook/brand-toolbar.ts` + trois lignes de `storybook/preview.ts` (import, `initialGlobals`, `withBrand`) : un projet mono-marque retire le fichier et ces trois lignes, rien d'autre n'en dépend. Vérifié en navigateur : les 3 marques changent bien la couleur rendue des composants (`primary.500` = `#9747ff` / `#107caa` / `#303be0`), en clair comme en sombre
- `ui-autocomplete` : **mode `multiple`** — le modèle devient un **tableau**, chaque suggestion choisie s'affiche par défaut comme une **`ui-chip` retirable** dans le champ (boîte `autoHeight`, saisie remise à zéro après chaque choix), la saisie libre ne modifiant jamais la sélection. **`unique`** (défaut) ignore les doublons (égalité par `dataKey` pour les objets) ; **`maxSelectedLabels`** replie le surplus derrière une chip informative dont le libellé vient d'**`overflowLabel`** (`{0}` = nombre masqué) ; `showClear` vide le tableau entier et `forceSelection` efface au blur toute saisie non reconnue sans toucher à la sélection. **Clavier** : `Retour arrière` sur saisie vide retire la dernière valeur ; `←` depuis le début de la saisie déplace le focus sur les chips par défaut (roving `←`/`→`, `Début`/`Fin`, suppression `Retour arrière`/`Suppr`) — chips dans une `role="listbox"` horizontale (`aria-setsize`/`aria-posinset`, suppression annoncée via `removeTagLabel`), panneau `aria-multiselectable="true"`. **Template `#selectedItem`** (contexte `$implicit`/`option`/`index`/`remove`) : rendu **entièrement personnalisé** des valeurs sélectionnées en remplacement des chips par défaut — la personnalisation passe par le template, **aucun input de style de chip** n'est exposé ; en mode templaté la listbox de chips et son roving sont retirés (a11y des valeurs déléguée au template consommateur, `Retour arrière` saisie vide toujours géré), `maxSelectedLabels`/`overflowLabel` restant actifs. Outputs `optionSelect`/`optionUnselect` par valeur ; story `Multiple` (défaut) + `Chips` (template) + section MDX dédiée
- `ui-image` : **prod-ready** — nouvel input `src` (URL distante/absolue via NgOptimizedImage, prioritaire sur `name`, sans loader custom), input `fallback` (asset local de repli) + placeholder tokenisé en état dégradé (`(error)` img + statut `httpResource` ; un `name` inconnu affiche le placeholder au lieu d'un rendu vide), output `loadFailed` (URL en échec), SVG inline réservé aux assets locaux (un `.svg` distant passe par `<img>` — pas de surface XSS), migration du fetch SVG `HttpClient.subscribe` → `httpResource` (cache module conservé, retry auto au changement de thème/marque/source) ; `name` devient optionnel ; `provideHttpClient(withFetch())` ajouté à `app.config.ts` (le composant ne fonctionnait qu'en Storybook)
- `ui-table` : **mode lazy serveur** — nouvel input `totalRecords` (total serveur pilotant le paginator) et nouvel output `sortChange` (état du tri après interaction d'en-tête) ; en mode `lazy`, la table n'applique plus ni tri ni découpe client : `value` est la page courante et le consommateur refetch sur `pageChange`/`sortChange` ; story `PaginationServer` (faux backend) + doc MDX « Lazy / serveur ». Note : `lazy` + `virtualScroll` conserve l'hydratation fenêtrée existante, mais le tri client y est désormais aussi désactivé (le consommateur possède l'ordre)
- **Outillage Claude Code** — `VERSIONING.md` déplacé dans `docs/` (nouveau dossier de doc repo) ; `.claude/` enrichi d'assets adaptés du projet _edfbs-safety-portal_ : 2 hooks de sécurité (`block-dangerous-commands.js`, `protect-secrets.js` + `settings.json`), 2 rules (`git-conventions.md`, `angular-patterns.md` path-scopée), le skill `/git-commit` (Conventional Commits + entrée CHANGELOG) et l'agent `design-system-expert` (modes FEATURE/AUDIT/REFACTOR sur le kit `ui-*`)
- `ui-read-only` : **affichage lecture seule** d'un couple label/valeur headless (catégorie _informative_, purement présentationnel — aucun état interactif, aucune participation au formulaire). Rendu comme une **liste de description native** `<dl>`/`<dt>`/`<dd>` : l'association label ↔ valeur est exposée gratuitement aux technologies d'assistance (aucun `aria-*`). La valeur provient de l'input `value` (converti en chaîne, **repli `fallback` atténué** « — » si `null`/`undefined`/vide) **ou** d'un **contenu riche projeté** (`<ng-content>` — badge, lien…) détecté après rendu via le motif `afterNextRender` de `ui-button`. **Dispositions flexibles** : `vertical` (label au-dessus) / `horizontal` (côte à côte) avec **colonne de label pilotable** — `labelWidth` (longueur CSS quelconque via la custom property `--ui-read-only-label-width`, vide = ajusté au contenu) et `labelAlign` (`left`/`right`) — ou **`grid` : contrôle total délégué** à un système de grille / classes utilitaires, le composant n'imposant **aucun display** et forwardant `rowClass` (rangée `<dl>`), `labelClass` (`<dt>`) et `valueClass` (`<dd>`) — ex. Gridaflex `flex-x` + `cell phone-24 desktop-auto` — (entrées additives dans toutes les dispositions). Tailles `default`/`small` (typographie), `multiline` (`white-space: pre-line`), mode `inline` (ajuste au contenu et s'insère dans le flux). **`matchFieldHeight`** : cale la zone de valeur sur la **hauteur d'un champ** (constantes partagées `utils.$form-field-height`/`-small`, valeur centrée verticalement, **sans padding horizontal**) pour un alignement propre en rangée avec un `ui-field`. **Libellé rendu par défaut via `ui-label`** (parité typographique exacte avec les champs — même police/graisse/couleur `--form-high-content-default`) : posé à côté d'un `ui-field`, un read-only s'aligne au pixel près (libellés, hauts/bas des valeurs/boîtes sur les mêmes lignes), l'**écart label→valeur reprenant celui du champ** (`utils.$form-field-gap` = `--units-sm` en `default`, `--units-xs` en `small`) ; personnalisable via `labelTemplate` (markup libre), `--ui-label-color` (héritée via `labelClass`) et `required` (marqueur `*`). Détail d'implémentation : le `line-height` de lecture est porté par la valeur (pas la racine) pour ne pas désaligner la hauteur du `ui-label` imbriqué. **A11y** : structure sémantique native `<dl>`/`<dt>`/`<dd>` **uniquement quand un label est présent** (sinon conteneur neutre — un `<dd>` sans `<dt>` serait invalide) ; **`emptyLabel`** annonce un texte explicite (« Non renseigné ») aux lecteurs d'écran via un span visuellement masqué pendant que le « — » passe en `aria-hidden` ; composant non focusable/non interactif (valeur cliquable = projeter un élément natif). Couleurs via tokens neutres `global.*` (libellé `global-low-content-default` atténué + `weight-bold`, valeur `global-high-content-default` + `weight-regular`). Stories Vertical/Horizontal/HorizontalFixedLabel/GridLayout/NextToField/Fallback/EmptyAccessible/Small/Multiline/ProjectedContent/Inline/Summary. 100 % tokens `global.*`/metrics/typography ; aucune **nouvelle** constante partagée (tailles de police locales `$sizes`, `matchFieldHeight` consomme les `$form-field-*` existantes de `ui-config`). **Divergences assumées** : composant adapté d'un composant maison (projet _edfbs-safety-portal_) reposant sur Transloco + grille Foundation — ici l'i18n est retirée (`label` chaîne brute au lieu de `labelKey`/`forceLabel` + pipe) et les classes de cellule Foundation figées (`labelCellClass`/`valueCellClass`) deviennent des passthroughs génériques `rowClass`/`labelClass`/`valueClass` compatibles n'importe quel système de grille ou utilitaire ; le marqueur de contenu projeté (`UiReadOnlyContent`) est remplacé par la détection `<ng-content>` façon `ui-button` ; **amélioration a11y** : structure sémantique `<dl>` au lieu de `div`/`span`. **Sans équivalent Figma** dédié pour l'instant (réutilise les tokens `global.*`) — un ComponentSet reste à créer dans le fichier UI Kit
- `ui-input-otp` : **saisie de code à usage unique / OTP** headless (catégorie _forms_, `ControlValueAccessor`) — une **rangée de cellules** d'un seul caractère backant une **valeur chaîne** jointe. Chaque cellule est un `<input maxlength="1">` natif ; le groupe porte `role="group"` et un **tabindex roving** (une seule tabulation : `Tab` entre/sort du champ, `←`/`→` déplacent le focus entre cellules, `Retour arrière` efface et recule). La **frappe avance automatiquement**, un **collage** de code est **réparti** sur les cellules (avec troncature à `length`). Modèle = **chaîne** pilotée en CVA → compatible `[(ngModel)]`, **Reactive Forms** et futurs **Signal Forms** (`[field]`, Angular 22) sans réécriture (interop `BaseControlValueAccessor` : contrôle sans flux `events` géré). **Options** : `length` (nombre de cellules, 4 par défaut), **`mask`** (masque les caractères → `type="password"`), **`integerOnly`** (chiffres uniquement, `inputmode="numeric"`, non-chiffres rejetés à la frappe **et** au collage), tailles **`small`/`default`/`large`**, `autofocus`, `disabled`, `readonly`, `invalid`. **Template `#input`** : remplace la cellule par défaut par une UI personnalisée — contexte `value` (`$implicit`)/`index`/`tabindex` (roving à reporter)/`events` (`input`/`keydown`/`focus`/`blur`/`paste` à brancher pour conserver la mécanique). **A11y** : `role="group"` nommé (`ariaLabel`/`ariaLabelledBy` **obligatoire**, avertissement dev sinon), cellules `<input>` natives nommées « <nom> n », `:focus-visible` distinct du `:hover`, `disabled` natif, couleurs via tokens. Outputs `valueChange` (à chaque changement) et **`completed`** (émis une fois toutes les cellules remplies — auto-soumission). Stories Basic/Controlled/Mask/IntegerOnly/Sizes/Disabled/Invalid/Template/Sample (écran d'authentification composé avec `ui-button` + séparateur central). 100 % tokens `form.*`/metrics/typography (réutilise les constantes structurelles `$form-field-*` de `ui-config`, aucune nouvelle constante partagée). **Divergences assumées** : booléens de contenu → présence d'input / template projeté (idiome Angular) ; états interactifs (`hover`/`focus`/`disabled`) → pseudo-classes CSS pilotées par tokens, jamais des props ; le composant n'embarque pas de shell `ui-field` (pas de label/helper : c'est un groupe de cellules à la manière de `ui-segment-control`, nom accessible via `ariaLabel`) ; ajout de l'output non-PrimeNG `completed` (motif OTP d'auto-soumission). **Sans équivalent Figma** dédié pour l'instant (réutilise les tokens `form.*`)
- `ui-sidebar` : **barre latérale applicative** headless (catégorie _navigation_) — chrome de navigation composé de trois pièces : le conteneur `ui-sidebar`, le menu déclaratif `ui-sidebar-menu` et la directive `[uiSidebarTrigger]`. **Deux présentations partageant le même intérieur** : **statique** (`mode="static"`, défaut) — la barre vit dans le flux et **pousse le contenu**, repliable en **rail d'icônes** (`collapsed` two-way, `collapsible`), avec `openOnHover` qui **fait flotter** un rail replié au survol / focus sans décaler le contenu ; **overlay** (`mode="overlay"`) — offcanvas **flottant** piloté par `visible` two-way, avec `backdrop`, fermeture `dismissable` (clic sur le fond) + `Échap` (`closeOnEscape`), **piège de focus** + restauration (`@angular/cdk/a11y`, `aria-modal`), **blocage du scroll** de fond (ref-compté), bouton de fermeture optionnel (`showCloseButton`), et `contained` pour embarquer l'overlay dans une région bornée (`position: absolute`, sans lock du body). **Responsive** : `responsive` bascule automatiquement une barre statique en overlay **sous `breakpoint`** (via `matchMedia`, défaut `1024px`, à aligner sur l'échelle `$breakpoint-*`) et la redonne en rail au-dessus. Bord `side` `left`/`right` (le glissement de l'overlay en dérive via le système de motion). Slots `#header` / `#footer` + corps projeté scrollable. L'**état replié résolu** (`effectiveCollapsed`) est exposé aux descendants **par injection de dépendances** : déposer un `ui-sidebar-menu` à l'intérieur l'adapte au rail sans câblage. **`ui-sidebar-menu`** : modèle déclaratif `UiSidebarMenuItem[]` rendu en **landmark `<nav>`** enveloppant des conteneurs `role="list"` / `role="listitem"` (sémantique préservée malgré `list-style:none`) ; **sections** libellées (navigation groupée), **groupes repliables** (`toggleable` — défaut faux au 1ᵉʳ niveau, vrai imbriqué) révélant des **sous-arbres profonds** (technique `grid-rows` 0fr↔1fr + fondu, indentés) avec état d'expansion (`expandedKeys` two-way) ; **suivi d'état actif** par item (RouterLink `routerLinkActive` pour les liens de route, `active` + `aria-current` pour la navigation manuelle — un groupe contenant un descendant actif **se déplie d'office**) ; `badge` de fin, `icon`, `separator`, `disabled`, familles `navigation.high`/`low`, densités `default`/`small` ; en mode rail, libellés / en-têtes / badges / chevrons masqués et icônes centrées avec `title`/`aria-label` = libellé. **`[uiSidebarTrigger]`** : bascule une barre depuis n'importe quel élément, reflète `aria-controls` (id du panneau) + `aria-expanded` (état résolu), `Entrée`/`Espace` natifs sur `<button>`. **A11y** : landmark nommé (`ariaLabel`/`ariaLabelledBy`, repli « Navigation » + avertissement dev), clavier natif (`Tab`/`Maj+Tab` parcourent, `Entrée`/`Espace` activent, `Échap` ferme l'overlay), `:focus-visible` distinct du `:hover`, `disabled` natif, couleurs via tokens (l'entrée active **paire** `surface-active` + `content-active`, seul contraste lisible). Largeurs pilotables par variables CSS locales (`--ui-sidebar-width` 280px / `--ui-sidebar-rail-width` 72px / `--ui-sidebar-backdrop-background`) — aucune constante partagée `ui-config` ajoutée. Stories Variants (playground) / WithMenu / Responsive / DualSidebar / NestedMenu. 100 % tokens `navigation.*`/`global.*`/metrics/typography. **Réutilise** les mécaniques éprouvées de `ui-drawer` (scrim, motion directionnel, piège de focus, scroll lock) portées dans le composant pour partager exactement le même intérieur entre présentations statique et overlay. **Divergences assumées** : booléens de contenu → présence d'input / templates projetés (idiome Angular) ; états interactifs (`hover`/`focus`/`active`/`disabled`) → pseudo-classes CSS pilotées par tokens, jamais des props ; le rail replié affiche les icônes de 1ᵉʳ niveau (pas de flyout latéral au survol dans cette itération) ; `breakpoint` par défaut `1024px` (hors échelle `$breakpoint-*` stricte 900/1200 — configurable, recommandation d'alignement documentée). **Sans équivalent Figma** dédié pour l'instant (réutilise les tokens `navigation.*`)
- `ui-input-tags` : **saisie multi-valeurs / tags** headless (catégorie _forms_, `ControlValueAccessor`) — édition d'une **liste de valeurs** sur le shell partagé `ui-field` (label + boîte `autoHeight` + helper). La boîte héberge une liste de **tags** — rendus **par défaut avec [`ui-chip`](#)** retirable en taille `small` (couleur réglable via `chipLevel`/`chipSubLevel`/`chipRounded`, sans template) — et un `<input>` en ligne : `Entrée` ajoute la valeur saisie, `Retour arrière` (input vide) retire le dernier. Un template `#item` (contexte `value`/`label`/`index`/`onRemove`) permet un rendu de tag **entièrement personnalisé** en remplacement du `ui-chip`. Modèle = **tableau** (`string[]` par défaut, `T[]` en typeahead objet) piloté en CVA → compatible `[(ngModel)]`, **Reactive Forms** et futurs **Signal Forms** (`[field]`, Angular 22) sans réécriture (interop `BaseControlValueAccessor` : contrôle sans flux `events` géré). **Options** : `max` (plafond de tags, bloque la saisie au-delà), `allowDuplicate` (doublons refusés par défaut), **`delimiter`** (string/RegExp : découpe la saisie et le texte collé en plusieurs tags), `addOnBlur`/`addOnTab`/`addOnPaste`, `maxLength` (longueur d'un tag). **`typeahead`** : liste de suggestions dans un **overlay CDK** alimentée par le parent via `completeMethod` (anti-rebond `delay`, seuil `minLength`, `completeOnFocus`) — suggestions primitives/objets (`optionLabel`/`optionValue`/`optionDisabled`, `dataKey`), **groupes** (`group`/`optionGroupLabel`/`optionGroupChildren`), focus `autoOptionFocus`/`focusOnHover`, `scrollHeight`, `autoFlip`, `panelStyleClass` ; les valeurs déjà présentes sont marquées (coche) et non re-sélectionnables (sauf `allowDuplicate`). **A11y** : liste de tags `role="listbox"` + `aria-orientation="horizontal"`, chaque tag `role="option"` avec `aria-label`/`aria-selected`/`aria-setsize`/`aria-posinset` et **focus roving** (`←`/`→` entre tags, `→` depuis le dernier revient à l'input, `Suppr`/`Retour arrière` retire), input en `role="combobox"` (typeahead) avec `aria-autocomplete`/`aria-controls`/`aria-activedescendant` ; action de suppression `<button tabindex="-1">` (retrait clavier via `Suppr`/`Retour arrière` sur le tag focalisé), `:focus-visible` distinct du `:hover`, garde-fou dev si aucun nom accessible. Outputs `tagAdd`/`tagRemove`/`valueChange`/`completeMethod`/`optionSelect`/`opened`/`closed`/`inputFocus`/`inputBlur`. Templates `#item` (tag, contexte `value`/`label`/`index`/`onRemove`), `#option`, `#group`. Stories Basic/Delimiter/AllowDuplicate/Max/Template/Typeahead/Events/Invalid/Disabled/Sizes + section « Formulaires » (ngModel/Reactive/Signal Forms). 100 % tokens `form.*`/`global.*`/metrics/typography (coque `overlay-panel` partagée, tags `global.high.surface.hover`). **Divergences assumées** : booléens de contenu → présence d'input / templates projetés (idiome Angular) ; états interactifs → pseudo-classes CSS pilotées par tokens, jamais des props ; le tag `ui-chip` **est** l'option roving (`role="option"` + tabindex roving sur son hôte), son action de suppression étant rendue **non focusable au Tab** via le nouvel input `removeTabindex="-1"` de `ui-chip` — motif `listbox`/`option` à un seul arrêt de tabulation préservé (retrait clavier via `Suppr`/`Retour arrière` sur le tag focalisé). **Sans équivalent Figma** dédié (source PrimeNG indisponible en v21 — conçu depuis les bonnes pratiques WAI-ARIA « listbox de tags » ; réutilise les tokens `form.*` de `ui-autocomplete`)
- `ui-autocomplete` : **champ de suggestions** headless (catégorie _forms_, `ControlValueAccessor`) — saisie assistée bâtie sur le shell partagé `ui-field` (label + boîte + helper) et un panneau de suggestions token-stylé en **overlay CDK**. Déclencheur = `<input>` natif suivant le motif **WAI-ARIA combobox** (`role="combobox"` + `aria-autocomplete="list"`/`aria-haspopup`/`aria-expanded`, relation `aria-controls` ↔ liste `role="listbox"`, focus visuel des options via `aria-activedescendant` — le focus ne quitte jamais l'input). **Recherche pilotée par le parent** : la frappe émet `completeMethod` (`{ originalEvent, query }`) **anti-rebondie** (`delay`, 300 ms) et gatée par `minLength` (1) ; le parent renseigne `suggestions`. Suggestions **primitives ou objets** (`optionLabel`/`optionValue`/`optionDisabled` en chemins pointés, `dataKey` pour l'égalité), **groupes** (`group` + `optionGroupLabel`/`optionGroupChildren`). **`dropdown`** ajoute un bouton déclencheur (`dropdownMode` `blank` → requête vide / `current` → texte courant). **`forceSelection`** valide la saisie au blur (effacée si elle ne correspond à aucune suggestion — le modèle ne porte qu'une suggestion réelle). **`showClear`** (icône d'effacement), **`loading`** (spinner à la place du déclencheur pendant la requête async, composé via `ui-spinner`), **`completeOnFocus`** (requête à la prise de focus). Focus configurable **`autoOptionFocus`**/**`selectOnFocus`**/**`focusOnHover`**. **Virtual scroll** CDK (`virtualScroll`/`virtualScrollItemSize`) + mode `lazy`/`lazyLoad`. Clavier : `↑`/`↓` (naviguer, ouvre si fermé), `Début`/`Fin`, `Entrée` (sélectionner + fermer), `Échap` (fermer), `Tab` (fermer + laisser filer le focus). Outputs `completeMethod`/`valueChange`/`optionSelect`/`opened`/`closed`/`cleared`/`dropdownClick`/`lazyLoad`/`inputFocus`/`inputBlur`. Templates `#item`/`#group`/`#header`/`#footer`/`#empty`, `panelStyleClass`, positionnement auto-flip. Avertissement dev si aucun nom accessible. Stories Basic/Dropdown/CustomOption/Group/ForceSelection/ClearIcon/Loading/Sizes/Disabled/Invalid/FocusBehavior/Objects/VirtualScroll. 100 % tokens `form.*`/`global.*`/metrics/typography (coque `overlay-panel` partagée), rendu du panneau aligné sur `ui-select`. **Divergences assumées** : booléens de contenu → présence d'input / templates projetés (idiome Angular) ; états interactifs (`hover`/`focused`/`disabled`) → pseudo-classes CSS pilotées par tokens, jamais des props ; mode `multiple`/chips ajouté ensuite dans ce même cycle (voir l'entrée dédiée « mode `multiple` » ci-dessus). **Sans équivalent Figma** dédié pour l'instant (réutilise les tokens `form.*` de `ui-select`)
- `ui-popover` : **panneau flottant ancré** headless (catégorie _layout_) — surface flottante rattachée à un déclencheur, pilotée **impérativement** via une variable de référence de template (`#op`) et les méthodes publiques `show(event?, target?)` / `hide()` / `toggle(event?, target?)` ; signal en lecture seule `visible`. Rendu dans un **overlay CDK connecté** (`cdkConnectedOverlay`) : positionnement, **retournement automatique** sur les bords du viewport et recalage au scroll délégués au CDK (aucune géométrie manuelle). L'ancre est déduite du `currentTarget` de l'événement, ou **passée explicitement en 2ᵉ argument** (mode contrôlé : n'importe quel bouton peut piloter l'état en gardant le panneau ancré au même élément). **Contenu projeté** quelconque (`<ng-content>`). **`position`** (`top`/`bottom`/`left`/`right`, défaut `bottom`) définit le côté préféré ; la **flèche** (`showArrow`, défaut vraie) suit le côté effectif via l'output `positionChange` du CDK. **A11y** : `role="dialog"` (`role` surchargeable) + `aria-modal="true"` (le focus reste piégé), **piège de focus** `cdkTrapFocus` (`@angular/cdk/a11y`), focus du **premier élément focusable** ou de l'élément portant `autofocus` à l'ouverture (`focusOnShow`), `Échap` ferme (`closeOnEscape`) et **rend le focus au déclencheur**, `aria-expanded`/`aria-controls` **reflétés impérativement sur le déclencheur** (nettoyés à la fermeture), garde-fou dev si panneau ouvert sans nom accessible (`ariaLabel`/`ariaLabelledBy`). **`dismissable`** (défaut) ferme au clic extérieur, en ignorant un clic sur le déclencheur (son propre gestionnaire possède la bascule). **Motion** : entrée `zoom` via le système de motion (`motion`, respecte `prefers-reduced-motion` et `data-motion="off"`). Stories Basic (accès par référence) / Controlled (ancre en 2ᵉ argument) / Positions / RichContent. Coque partagée `overlay-panel` (constantes `$overlay-panel-*` de `ui-config`, surchargeables par instance via `--ui-overlay-*`), 100 % tokens `global.*`/metrics/typography, constantes SCSS locales `$popover-padding`/`$popover-max-width`/`$popover-arrow-size`. **Sans équivalent Figma** pour l'instant. **Divergences assumées** : booléens de contenu → présence d'input / contenu projeté (idiome Angular) ; états interactifs → pseudo-classes CSS ; **animation de sortie instantanée** (l'overlay se détache à la fermeture, comme `ui-context-menu` — seule l'entrée est animée)
- `ui-stepper` : **assistant de progression / wizard** headless (catégorie _navigation_) — guide l'utilisateur au fil d'étapes numérotées. API de **composition** alignée sur `ui-tabs` : conteneur `ui-stepper` (`[(value)]` two-way, `stepChange`, méthodes `next()`/`prev()`), `ui-step-list` (bandeau `role="tablist"`) de `ui-step` (en-têtes `<button role="tab">`), `ui-step-panels`/`ui-step-panel` (`role="tabpanel"`), et `ui-step-item` (enveloppe verticale propageant le `value` à son step + panel). **Orientations** `orientation` : `horizontal` (en-têtes + connecteurs, panneaux en-dessous) et `vertical` (panneaux dépliés sous chaque en-tête le long d'un rail). **Mode `linear`** : les étapes situées après l'étape active sont désactivées (impossible de sauter en avant), on progresse en pilotant `value` ; les étapes terminées restent cliquables pour revenir. **État dérivé** (à venir / actif / terminé) calculé depuis la position par rapport à l'étape active — aucune prop d'état à gérer ; marqueur numéroté (ou `icon` par étape, ou `completedIcon` global comme `check` sur les étapes terminées). **`lazy`** (groupe ou panneau, template `#content`) comme `ui-tabs`. **Mode « étapes seules »** : `ui-step-list` sans panneaux pour un simple indicateur de progression. **A11y** : `role="tablist"` (sur `ui-step-list` en horizontal, sur l'hôte `ui-stepper` en vertical) + `aria-orientation`/`aria-label` ; en-têtes `role="tab"` + `aria-controls`/`aria-selected`/`aria-current="step"` ; panneaux `role="tabpanel"` + `id`/`aria-labelledby` ; navigation **clavier native** — `Tab` traverse les en-têtes activables (désactivés sautés), `Entrée`/`Espace` activent ; marqueur `aria-hidden` (le sens passe par le titre) ; `:focus-visible` distinct du `:hover` ; `disabled` natif. **Motion** : le panneau actif se **déploie** et les autres se **replient** (technique `grid-rows` 0fr↔1fr + fondu, comme `ui-accordion`), animation pilotée par `[motion]` et neutralisée sous `prefers-reduced-motion`, `data-motion="off"` et `[motion]="false"` (panneaux inactifs `inert`, retirés du tab order et des lecteurs d'écran). Stories Basic (horizontal) / Vertical / Linear / StepsOnly / Template / Controlled. 100 % tokens `navigation.*`/`informative.*`/`global.*`/metrics/typography. **Divergences assumées** vs YAML Figma : les props Figma `state` (Default/Active/Past) et `interaction` (Default/Hover/Disabled) → état **dérivé** de l'étape active + pseudo-classes CSS (idiome Angular headless, pas de props/variants) ; les composants Figma `_stepper-number`/`_stepper-separator` → éléments internes du `ui-step`/rail du `ui-step-item` (pas de composants enfants dédiés) ; ajout des modes `linear`, `vertical`, `lazy`, du `completedIcon` et des méthodes `next()`/`prev()` (comportement wizard non dessiné dans le YAML, purement visuel)
- `ui-progress-bar` : **barre de progression** headless (catégorie _informative_) — indicateur d'état de processus. **Modes** `mode` : `determinate` (piste remplie jusqu'à `value` %, borné 0–100) et `indeterminate` (balayage en boucle, sans valeur). **Libellé** de valeur affiché à droite (`valuePosition="right"`), sous la barre (`bottom`) ou **dans le remplissage** (`inside` — barre haute `size-components-xs`, texte centré contrasté `informative.highlightHigh.content`, rogné si le remplissage est trop étroit), masquable (`showValue=false`), avec **unité** `unit` (défaut `%`) ou **formateur libre** `valueTemplate` (contexte `{ $implicit: value }`). **Mode `steps`** : découpe la barre en N segments discrets (nombre de segments remplis dérivé de `value`). **Tailles** `size` (`default` 8px / `small` 4px). **Couleur** de remplissage surchargeable par instance via l'input `color` ou la custom property `--ui-progress-bar-color` (défaut `informative.highlightHigh.surface.default` ; piste `global.high.stroke.default`). **A11y** : piste `role="progressbar"` + `aria-valuemin`/`aria-valuemax`/`aria-valuenow` (omis en indéterminé), `aria-valuetext` « n / total » en mode steps, `ariaLabel`/`ariaLabelledBy`. La transition de largeur (mode déterminé) et l'animation indéterminée sont neutralisées sous `prefers-reduced-motion: reduce` et `data-motion="off"` (via `:host-context()`). Stories Basic/Dynamic/Template/Indeterminate/AsSteps + positions & tailles. 100 % tokens `informative.*`/`global.*`/metrics. **Divergences assumées** vs YAML Figma : le composant Figma `_ui-progress-bar-item` (5 instances) → segments internes générés par `@for` (idiome Angular headless, pas de composant enfant dédié) ; la prop Figma `Progress` (0/20/40/60/80/100) → input continu `value` (0–100) ; la prop Figma `Position` (None/Right/Bottom) → `valuePosition` + `showValue` ; ajout des modes `indeterminate` et `steps` et de l'override `color` (inspirés de PrimeNG ProgressBar) ; segments espacés de `units.2xs` pour rendre les étapes discernables (vs contiguës dans le YAML) ; états interactifs → aucun (composant non interactif)
- `ui-alert` : **message inline** headless (catégorie _informative_, sibling non-flottant de `ui-toast`) — retour informatif/succès/avertissement/erreur inséré dans le flux de la page (bandeau de formulaire, résumé d'erreurs, statut). Anatomie Figma : icône de tête · titre/texte + `<ng-content>` · bouton de fermeture. **Niveaux** `level` (`default`/`highlight`/`success`/`warning`/`error`) × **sous-niveau** `subLevel` (`high` soutenu / `low` discret) → couleurs + icône par défaut ; **tailles** `size` (`default`/`large`, pilotant typo titre/texte + taille des icônes). **Icône** `icon` : nom FontAwesome pour surcharger, `false` pour masquer, `true` (défaut) pour l'icône du niveau. **Fermeture** `closable` (`<button>` natif + `closeIcon`/`closeAriaLabel`, clavier <kbd>Entrée</kbd>/<kbd>Espace</kbd>) : l'alerte se masque et émet `close`. **Auto-disparition `life` (ms)** via `setTimeout` (browser-only, robuste en onglet throttlé, nettoyé au destroy). **A11y** : racine `role="alert"` (`aria-live="assertive"` + `aria-atomic="true"` implicites), `ariaLabel`/`ariaLabelledBy`, `:focus-visible` distinct du `:hover`, couleur jamais seul vecteur de sens. Stories dédiées Basic/Level/Icon/Variant/Sizes + **Dynamic** (bloc `@for`, retrait à la fermeture), **Life** (auto-disparition) et **Forms** (erreurs de validation en sévérité `error`). 100 % tokens `informative.*`. **Divergences assumées** vs YAML Figma : booléen Figma `icon` → présence/valeur de l'input `icon` (idiome Angular) ; états interactifs → pseudo-classes CSS ; ajout de `life` (auto-disparition, inspiré de PrimeNG Message) non dessiné dans le YAML
- `ui-slider` : **curseur de sélection de valeur** headless (catégorie _forms_, `ControlValueAccessor`) — choix d'une valeur numérique (ou d'un intervalle) en glissant une poignée le long d'une piste. Bâti sur le **motif WAI-ARIA « slider »** : chaque poignée est un élément `role="slider"` exposant `aria-valuemin`/`aria-valuemax`/`aria-valuenow`/`aria-orientation`, entièrement pilotable au **clavier** (flèches ± `step`, Page↑/↓ ± 10 pas, Début/Fin → `min`/`max`). Interactions pointeur (souris/tactile/stylet) unifiées via `PointerEvent` + pointer capture (drag de poignée **et** clic sur la piste). API : `min`/`max`/`step`, **`range`** (deux poignées, le modèle devient `number[]`), **`minStepsBetweenHandles`** (écart minimal conservé entre poignées), **`marks`** (un repère par pas, borné à 100 pas), `orientation` (`horizontal`/`vertical`), `disabled`/`readonly`/`required`/`invalid`, `name`/`ariaLabel`/`ariaLabelledBy`/`inputId`/`tabindex`. Outputs **`sliderChange`** (émis en continu pendant le déplacement) et **`slideEnd`** (émis une fois le glissement/clic terminé), plus `sliderFocus`/`sliderBlur`. Style erreur automatique quand le contrôle attaché est invalide et touched/dirty (`invalid` pour forcer) ; états `hover`/`:focus-visible`/`pressed`/`disabled` en pseudo-classes sur les tokens `actions.*`/`form.error.*` (piste `global.low.surface`, remplissage/poignées `actions.high.surface`, repères `informative.defaulthigh`/`actions.high.content`). **Hooks structurels** CSS custom properties surchargeables par instance : `--ui-slider-thickness`, `--ui-slider-handle-size`, `--ui-slider-length` (vertical). Avertissement dev si aucun nom accessible. Compatible autonome, `[(ngModel)]`, Reactive Forms et **Signal Forms** (`[field]`, prêt pour la migration Angular 22). **Divergences assumées** vs YAML Figma : la prop `Type` (Default/Marks/Range) est décomposée en inputs fonctionnels `range` + `marks` ; la prop `Level` (1-4, largeurs de piste fixes) devient une **largeur fluide** (fill container) conforme aux règles Auto Layout ; taille des repères portée de 3px à `rem-calc(6)` pour la lisibilité. Co-localisé, 100 % tokens
- `ui-skeleton` : **placeholder de chargement** headless (catégorie _informative_) — boîte unique affichée à la place du contenu pendant son chargement. Trois **formes** (`shape`) avec dimensions par défaut par forme × taille (`size` `default`/`small`) : `text` (224×20, `--radius-xl` ; small hauteur 12), `circle` (100×100, `--radius-full` + `aspect-ratio:1` pour rester rond avec une seule dimension surchargée ; small 64×64), `rectangle` (100×100, `--radius-default` ; small 64×64). **Surcharges CSS brutes** `width`/`height`/`borderRadius` (valeurs libres pour composer cartes, grilles, listes, tableaux) appliquées en styles inline (priment sur les défauts pilotés par tokens). **Couleur de fond** personnalisable par instance via la custom property `--ui-skeleton-background` (défaut `--global-low-surface-default`). **Animations** (`animation`) : `wave` (défaut — reflet clair large et doux qui glisse de gauche à droite via un `::after` en dégradé, teinte pilotable par `--ui-skeleton-shine`, atténuée en thème sombre), `pulse` (pulsation d'opacité), `none`. **A11y** : racine `aria-hidden="true"` (état purement visuel, jamais annoncé comme du contenu ; regrouper sous un conteneur `aria-busy="true"`), aucun élément interactif. Motion figée sous `prefers-reduced-motion: reduce` et `data-motion="off"`. Hôte transparent (`display:contents`) pour que la boîte s'insère naturellement dans les mises en page flex/grid. Co-localisé, 100 % tokens `global`/metrics. **Divergence assumée** vs YAML Figma : la prop `Type` (Text/Circle/Rectangle) devient l'input **`shape`** (aligné sur la propriété de style documentée), et l'animation/les surcharges de dimensions étendent le composant au-delà des variants dessinés
- `ui-table` : doc **Skeleton** — exemple de rendu de lignes de substitution remplies de `ui-skeleton` pendant le chargement d'une requête (story + section MDX)
- `ui-paginator` : **pagination autonome** (catégorie _table_) — extraction de la barre embarquée dans `ui-table`, fidèle aux composants Figma `_paginator-button-controls`/`_paginator-button-numbers` : contrôles premier/précédent/suivant/dernier et numéros de page en **`<button>` natifs** stylés directement par les tokens (carrés arrondis 40px `--radius-default`, famille `actions.low`, page courante `actions.high` + `aria-current="page"`, états `hover/focused/pressed/disabled` en pseudo-classes sur les tokens `-hover/-focused/-pressed/-disabled`, anneau `:focus-visible` `actions.*.stroke.focused`), icônes en instances **`ui-icon`**, sélecteur « lignes par page » en instance **`ui-select`** (le booléen Figma `selectPage` devient la présence de `rowsPerPageOptions`). API : `totalRecords`, two-way **`rows`** et **`first`** (pagination programmatique), fenêtre de numéros `pageLinks` recentrée sur la page courante, **mode compact `ellipsis`** (`1 2 3 … 30 31 32` : `boundaryCount` pages à chaque bord + voisinage de la page courante, `…` décoratifs `aria-hidden` non focusables, un trou d'une page affiché plutôt qu'élidé), `disabled`, output `pageChange {first, rows, page, pageCount}`, labels d'accessibilité surchargeables (`ariaLabel`, `pageAriaLabel` à motif `{page}`, `firstPageAriaLabel`…). **Rapport de page courante** `showCurrentPageReport` + motif `currentPageReportTemplate` (placeholders `{first}`/`{last}`/`{rows}`/`{page}`/`{pageCount}`/`{totalRecords}`), barre modulable `showFirstLastIcon`/`showPageLinks` (mode compact rapport + précédent/suivant). **Templates par élément** : contenus libres **embarqués** `#start`/`#end` et rapport riche `#report` (contexte = état de pagination `{first, last, rows, page, pageCount, totalRecords}`), `#firsticon`/`#previcon`/`#nexticon`/`#lasticon` (contenu des contrôles) et `#pagelink` (contexte `{ $implicit: numéro, page, active }`) — les attributs a11y restent portés par les boutons. Landmark `<nav>` nommé, butées désactivées nativement. Constantes SCSS locales `$button-size`/`$button-radius`/`$bar-gap`/`$blocks-gap`/`$numbers-gap`. Consommée par `ui-table` (`paginator`)
- `ui-table` : **réordonnancement de lignes** par glisser-déposer (`uiReorderableRow` + output `rowReorder {dragIndex, dropIndex, value}` avec copie réordonnée à re-binder, indicateur de bord de dépose token-stylé), **column toggle** documenté (pur templating avec `ui-select multiple`), **`loading`** (masque `--ui-table-mask-background` + instance `ui-spinner`, `aria-busy`), **virtual scroll** fenêtré maison préservant la sémantique `<table>` (`virtualScroll` + `virtualScrollItemSize`, lignes de cale, 10 000 lignes rendues en quelques dizaines de `<tr>` ; mode **`lazy`** → output `lazyLoad {first, last}`), **état vide** rendu en instance `ui-empty-state` (`emptyMessage`/`emptyIcon`, template `#emptymessage` prioritaire), **clavier de sélection complet** (roving tabindex — `Tab` atteint la première ligne sélectionnée —, `Début`/`Fin`, `Maj + flèche` étend, `Maj + Espace` plage depuis l'ancre, `Ctrl/Cmd + A` tout sélectionner, `Ctrl/Cmd + Maj + Début/Fin` jusqu'aux bords)
- `ui-table` : **tableau de données** headless (nouvelle catégorie _table_) — le composant orchestre le **pipeline de données** (tri → pagination), la sélection, l'expansion, le scroll et la pagination ; le **markup des lignes reste au consommateur** via les templates `#header`/`#body`/`#footer`/`#caption`/`#expandedrow`/`#emptymessage` (vrais `<tr>/<th>/<td>`, contexte typé `{ $implicit, rowIndex, even, selected, expanded, frozen }`). **Tailles** `default`/`small`/`large` (densité de padding), `showGridlines` (bordures entre cellules), `stripedRows` (fonds alternés `table.body.*.even/odd`), `rowHover`. **Tri** par directive `uiSortableColumn` + indicateur décoratif `ui-table-sort-icon` : simple (cycle asc → desc → non trié), **multiple** (Ctrl/Cmd + clic compose), presort `sortField`/`sortOrder`/`multiSortMeta`, **`customSort`** (tri délégué via l'output `sortFunction`), activation clavier `Entrée`/`Espace`, état `aria-sort`. **Sélection** two-way `[(selection)]` : `single`/`multiple` sur lignes `uiSelectableRow` (avec `metaKeySelection` : clic remplace, Ctrl/Cmd bascule, Maj étend la plage), **checkbox** (`ui-table-checkbox` + `ui-table-header-checkbox` tout-sélectionner avec indéterminé — instances `ui-checkbox`), **radio** (`ui-table-radio` — instance `ui-radio`), **clavier complet** (`↑`/`↓` de ligne en ligne, `Espace`/`Entrée` basculent, `Maj + flèche` étend), events `rowSelect`/`rowUnselect`/`headerCheckboxToggle`, lignes `tabindex="0"` + `aria-selected`, clics sur descendants interactifs ignorés. **Pagination intégrée** (`paginator`, `rows`, modèle two-way `first` pour le pilotage programmatique, `rowsPerPageOptions`, `pageLinks` fenêtrés, output `pageChange`) — barre **composée** de `ui-button` (contrôles circulaires via le hook `--ui-button-radius`, page courante `high` + `aria-current="page"`) et `ui-select` (lignes par page), calquée sur le Figma `ui-paginator`. **Scroll** : `scrollable` + `scrollHeight` fixe (en-tête et pied **sticky**) ou **`'flex'`** (le viewport suit le parent flex), horizontal via `tableStyle` min-width. **Colonnes figées** `uiFrozenColumn` (+`alignFrozen` left/right, plusieurs par bord, décalages auto-mesurés via ResizeObserver), **lignes figées** `frozenValue` (tbody sticky sous l'en-tête, hauteur mesurée). **Expansion** `uiRowToggler` (aria-expanded) + template `#expandedrow`, état two-way `expandedRowKeys` (par `dataKey`), events `rowExpand`/`rowCollapse`. **Resize de colonnes mode fit** `uiResizableColumn` (poignée pointer, la colonne voisine absorbe le delta, largeurs verrouillées au drag, output `colResize`). Styles **globaux sous `.ui-table`** (`ViewEncapsulation.None` — seul moyen de styler le markup projeté), 100 % tokens `table.*` (head/body/footer × surface/content/stroke × états), sélection en famille `informative.highlightLow` (pas d'état `selected` dans `table.*`), focus `:focus-visible` en anneau intérieur `global.high.stroke.focused`, constantes SCSS locales `$sizes`/`$stroke-width`/`$paginator-gap`/`$resizer-hit-width`. API inspirée d'une _data table_
- `ui-select` : **liste déroulante** headless (catégorie _forms_) — bâtie sur le shell partagé `ui-field` (label + boîte + helper + états) et la base `BaseFormField` (CVA + validation auto), panneau d'options token-stylé dans un **overlay CDK** (coque partagée `overlay-panel` en famille `form.*`, largeur calée sur le champ, retournement auto `autoFlip`, entrée `slide-down` du système de motion). Déclencheur **natif** suivant le **motif WAI-ARIA combobox** : `<button role="combobox">` (ou **`<input>` en mode `editable`** — la frappe fixe directement la valeur), `aria-haspopup/expanded/controls` + **`aria-activedescendant`** (le focus DOM ne quitte jamais le déclencheur ou le filtre, les options ne sont focalisées que visuellement). **Options** primitives ou objets avec mapping de champs en chemins pointés (`optionLabel`/`optionValue`/`optionDisabled`), **groupes** (`group` + `optionGroupLabel`/`optionGroupChildren`, en-têtes custom `#group`, groupes vidés par le filtre masqués), égalité objet par `dataKey`. **Sélection multiple** (`multiple`, modèle tableau, panneau maintenu ouvert) avec indicateur **`checkmark`** (coche fin de ligne) ou **`checkbox`** (case purement visuelle `aria-hidden`, l'état accessible restant `aria-selected` sur l'option), affichage du déclencheur personnalisable via `#selectedItem` (rendu **par valeur sélectionnée**, contexte avec callback **`remove`** — motif **chips retirables** avec `ui-chip`, rendues à côté du bouton combobox pour rester du HTML interactif valide ; `Retour arrière` retire le dernier élément), repli **`maxSelectedLabels`** (au-delà, indicateur `overflowLabel` « (+n autres) »), **passage à la ligne** des valeurs en largeur contrainte (boîte `ui-field` en `autoHeight`). **Filtre intégré** (`filter`, insensible casse/accents, `filterBy` multi-champs, `autofocusFilter`, `resetFilterOnHide`, messages `emptyMessage`/`emptyFilterMessage`), effacement `showClear` (bouton natif nommé, affordance discrète intégrée au champ), état **`loading`** (`ui-spinner` en suffixe). En **`editable`**, la frappe ouvre le panneau et **filtre les suggestions** façon autocomplete (sélection = libellé, fermeture = texte conservé). **Clavier complet** : `↓`/`↑` ouvrent puis naviguent (sans boucle, options désactivées sautées), `Début`/`Fin`, `Entrée`/`Espace` sélectionnent, `Échap`/`Tab`/`Alt+↑` ferment, **type-ahead** avec tampon 500 ms ; comportement du focus configurable `autoOptionFocus`/`selectOnFocus`/`focusOnHover`. **Virtual scroll CDK** (`virtualScroll` + `virtualScrollItemSize`, `aria-setsize`/`aria-posinset`, en-têtes de groupe alignés sur la hauteur de ligne) et **chargement paresseux** (`lazy` → output `lazyLoad {first,last}` sur la plage rendue). Templates `#item`/`#selectedItem`/`#group`/`#header`/`#footer`/`#empty`, `panelStyleClass`, `scrollHeight`. **Sécurité/perf** : aucun `innerHTML` (interpolation partout), listes dérivées en `computed()`, DOM borné par le viewport virtuel. Garde-fous dev : nom accessible manquant, `editable`+`multiple`, `checkbox` sans `multiple`. 100 % tokens `form`/`global`/metrics/typography, constantes SCSS locales `$sizes`/`$panel-*`/`$option-*`/`$checkbox-size`. API inspirée d'un _select_
- `ui-empty-state` : **état vide** headless (catégorie _informative_) — pile verticale centrée communiquant l'absence de contenu (liste vide, aucun résultat, boîte à jour…) et proposant une issue. Régions optionnelles rendues uniquement si remplies (aucun conteneur vide, aucun espacement fantôme) : **visuel** (`showMedia`, `true` par défaut) via une **illustration projetée** (`uiEmptyStateMedia` — image/SVG/`ui-image`, l'emporte sur le raccourci) **ou** un **raccourci `icon`** FontAwesome rendu en grand par `ui-icon` (décoratif), **titre** (`title`) + **description** (`description`), **corps libre** projeté (contenu par défaut, détecté après rendu) et **slot d'actions** `uiEmptyStateActions` (un ou plusieurs `ui-button`/liens). Tailles `default`/`small` (typographie titre/texte + taille du visuel adaptées). **A11y** : visuel décoratif (`aria-hidden`), titre rendu en paragraphe (aucun niveau de titre imposé pour ne pas casser le plan du document), **région nommée** `role="region"` ajouté uniquement si `ariaLabel`/`ariaLabelledBy` est fourni (pas de landmark anonyme). Réglages par instance via custom properties `--ui-empty-state-media-size` / `--ui-empty-state-media-color`, constantes SCSS locales `$sizes`/`$text-gap`/`$actions-gap`/`$max-width`. Co-localisé, 100 % tokens `global`/metrics/typography. **Divergences assumées** vs YAML Figma : le booléen `action` devient la **présence** d'un slot d'actions (idiome Angular) ; le visuel unifie illustration **et** icône derrière un `showMedia` + deux voies de contenu
- `ui-file-upload` : **téléverseur de fichiers** headless (catégorie _forms_) — deux dispositions : champ compact **`field`** (résumé de sélection + bouton « Browse ») et **zone de glisser-déposer `drag`** (icône `cloud-arrow-up`, lien « Click to upload » + invite, indice `hint`). La sélection s'appuie sur un **`<input type="file">` natif** masqué mais **focusable**, enveloppé d'un `<label>` : sélecteur système, tabulation et activation clavier natifs, toute la surface accepte le **dépôt** (`dragover`/`drop`, surbrillance `_dragging`, garde contre le `dragleave` des enfants). `multiple`, filtre `accept` (MIME/extension, ré-appliqué au dépôt), **validations** `maxFileSize` / `fileLimit` (messages agrégés dans une région `role="alert"` `aria-live="assertive"`). Téléversement **automatique** (`auto`), à la demande, ou **délégué** (`customUpload` → sortie `uploadHandler` avec callbacks `setProgress`/`markUploaded`/`markError`) ; l'uploader intégré utilise **`XMLHttpRequest`** (progression réelle via `upload.onprogress`, `url`/`method`/`fieldName`/`withCredentials`, aucun provider HTTP global requis, abort au retrait/vidage). **Templates** `file` (ligne), `content` (section de contenu, reçoit les fichiers), `toolbar` (barre d'actions) — contextes typés avec callbacks `remove`/`clear`/`choose`/`upload`. Tailles `default`/`small`, `disabled`, labels personnalisables, sorties `onSelect`/`filesChange`/`onUpload`/`onError`/`onRemove`/`onClear`, API publique `choose()`/`upload()`/`remove()`/`clear()` + signal `files` en lecture seule. **A11y** : nom accessible obligatoire (`ariaLabel`, garde-fou dev), anneau de focus sur la zone via `:focus-visible` de l'input, boutons de suppression `<button>` nommés. **Sécurité** : validation client = confort UX (revalider serveur), object URLs des aperçus **révoqués** (retrait/vidage/destruction), noms de fichiers échappés (interpolation, jamais `innerHTML`), `withCredentials` off par défaut. Co-localisé, 100 % tokens `form`/`actions`/`global`/`informative`/metrics/typography ; réutilise les constantes partagées `$form-field-*`. API inspirée d'un _file upload_
- `ui-file-upload-list` : **ligne de fichier** headless — l'unité répétée d'une liste de téléversement (miroir 1:1 du composant Figma). Présentationnelle : **vignette** (images, via object URL) ou icône de fichier, nom (tronqué) + taille formatée / état, bouton de suppression `<button>` nommé. Pendant le téléversement, le marqueur bascule sur **`ui-spinner`** (taille `small`) et une **barre de progression** `role="progressbar"` apparaît ; état d'erreur stylé via les tokens `informative.error*`. Tailles `default`/`small`, `removable`, `removeLabel`, sortie `remove`. Co-localisé, 100 % tokens `form`/`informative`/metrics/typography, constante SCSS locale `$sizes`
- `ui-spinner` : **indicateur de chargement indéterminé** headless (catégorie _informative_). Rend par défaut un **cercle SVG lissé** (arc croissant/décroissant + rotation), coloré par les tokens via `currentColor` (`--global-high-content-default`, surchargeable par instance avec `--ui-spinner-color`). **Marqueur entièrement remplaçable** par **présence d'input**, dans l'ordre de priorité : `spinnerTemplate` (`TemplateRef` — contenu libre) › `image` (URL, affichée telle quelle car un GIF/SVG animé porte sa propre motion) › `icon` (nom FontAwesome, mis en rotation par le composant via `ui-icon`) › cercle SVG par défaut. Réglages du cercle : `strokeWidth` (échelle du viewBox 0–50), `fill` (`none` = anneau seul), `animationDuration` (durée d'une rotation, propagée au marqueur icône via la custom property `--ui-spinner-duration`). Tailles `default`/`small` (marqueur 32/20px, libellé `text/bold/md`/`sm`) + réglage fin via `--ui-spinner-size`. **Libellé** visible optionnel (`label`) et `orientation` `vertical`/`horizontal` (marqueur au-dessus / à côté). **`delay` (ms)** : délai de grâce anti-flash — le spinner n'est monté que si l'attente dépasse le délai (`setTimeout`, pas `requestAnimationFrame`, pour survivre à un onglet throttlé ; nettoyé sur re-run/destruction). **A11y** : racine `role="status"` + `aria-live="polite"` + `aria-busy="true"` (apparition annoncée sans voler le focus), **nom accessible toujours présent** (`ariaLabel`, sinon le `label` visible lu tel quel, sinon repli « Chargement »), marqueur **décoratif** (`aria-hidden` — le sens vient du texte, jamais de la seule animation) ; `prefers-reduced-motion: reduce` **et** `data-motion="off"` figent la rotation et affichent un **anneau plein atténué** (indicateur intentionnel, pas un spinner « bloqué »). Co-localisé, 100 % tokens `global`/metrics/typography, constante SCSS locale `$sizes`
- `ui-button-split` : **bouton d'action + déroulant** headless — un bouton d'action principal accolé à un déclencheur qui révèle une collection d'options additionnelles. **Composition** (zéro style dupliqué) de deux **`ui-button`** (action + déclencheur icon-only au chevron, tokens `actions.*`) et d'un **`ui-menu` en popup** (panneau, tokens `navigation.*`) — les options sont pilotées par le même **modèle déclaratif `model` (`UiMenuItem[]`)** : commandes (`command`/`itemClick`), navigation `routerLink`/`url`, **sous-menus imbriqués**, séparateurs, icônes. `level` (`high`/`low`/`success`/`warning`/`error`) et `size` (`default`/`small`) appliqués aux deux boutons (+ densité du menu) ; icône d'action `icon`/`iconPos`, chevron personnalisable `dropdownIcon`. **Désactivation indépendante** : `disabled` (les deux), `buttonDisabled` (action seule), `menuButtonDisabled` (déclencheur seul) — les états dérivés en `computed`, jamais dupliqués. Les deux boutons sont **soudés visuellement** (coins intérieurs carrés + chevauchement de la bordure partagée = `$stroke-width`, sans doublement du trait ; le bouton survolé/focus passe au-dessus via `z-index`) — collage réalisé via le **hook `--ui-button-radius`** exposé par `ui-button` (custom property héritée à travers la frontière de composant, **sans `::ng-deep`**). Sorties `buttonClick`/`dropdownClick`/`menuShow`/`menuHide`/`itemClick`, méthodes publiques `focus()`/`hide()`. **A11y** : deux `<button>` natifs, déclencheur `aria-haspopup="menu"` + `aria-controls` + `aria-expanded` reflétant l'ouverture, nom accessible du déclencheur icon-only `menuButtonAriaLabel` (défaut « Plus d'options »), clavier roving et retour de focus hérités de `ui-menu`. Co-localisé, 100 % tokens (via les composants composés), constante SCSS locale `$stroke-width`. API inspirée d'un _split button_
- `ui-context-menu` : **menu contextuel** headless ouvert au **clic droit** (ou tout `triggerEvent` custom), **positionné aux coordonnées du pointeur** dans un overlay CDK (origine `Point`, repli automatique sur les 4 bords du viewport). Attachement à un élément via **`target`** (variable de référence de template, listener re-câblé réactivement via `effect` + cleanup) ou au **document entier via `global`** ; clic droit répété sur la zone attachée → **repositionnement** du menu ouvert, interaction extérieure (clic ou clic droit hors zone) → fermeture. Le panneau est un **`ui-menu` embarqué** (composition, zéro duplication) : même modèle déclaratif `UiMenuItem[]` (**sous-menus imbriqués en panneaux latéraux flyout** par défaut — `submenus="inline"` pour l'accordéon —, `command`, `routerLink`/`url`), même clavier roving WAI-ARIA, mêmes tokens `navigation.*` et coque `overlay-panel` — **densité `small` par défaut** (menu d'actions contextuel). Fermeture extérieure **à la presse du pointeur** (`pointerdown` document, hors overlays) et non au relâchement : le clic droit d'ouverture (et son `auxclick`) ne referme jamais le menu. **Ancré au document, pas à l'écran** : l'ancre est mémorisée en coordonnées de page et re-projetée dans le viewport à chaque scroll — le menu défile avec le contenu (comme un nœud du DOM) au lieu de rester figé à l'écran. Méthodes publiques `show(event)`/`hide()`/`toggle(event)` (câblage manuel), sorties `opened`/`closed`/`itemClick` (fermeture auto après activation). **A11y** : focus placé sur le **premier item** à l'ouverture, `Échap` ferme et **rend le focus à la cible**, apparition `zoom` (système de motion, reduced-motion respecté). Templates `#item`/`#submenuheader` **transmis à travers la frontière de composant** via les nouveaux inputs template de `ui-menu`. SCSS local réduit à l'enveloppe d'overlay (plafonnement viewport) — le visuel vit dans `ui-menu`
- `ui-menu` : **mode `submenus` `inline`/`flyout`** — en `flyout`, chaque groupe devient un **panneau latéral en cascade** (menu imbriqué récursif dans un overlay CDK ancré à l'item parent, retournement auto sur les bords) : ouverture au survol/clic (le survol d'une autre entrée du niveau referme le panneau frère), **clavier** `→` ouvre + focus le premier item de l'enfant / `←`+`Échap` referment + rendent le focus au parent, parents `aria-haspopup="menu"`+`aria-expanded` avec chevron `›` et état visuel `_sub-open` maintenu, activation d'un item enfant remontée (`itemClick`) et fermeture en cascade. Nouveau modifier SCSS **`._floating`** (plafond viewport + scroll **interne** au panneau — partagé avec `._popup` : l'ombre du panneau n'est jamais rognée par un wrapper). Inputs **`itemTemplate`/`submenuHeaderTemplate`** (`TemplateRef`, prioritaires sur les templates projetés `#item`/`#submenuheader` — permettent à un composant hôte comme `ui-context-menu` de faire suivre ses propres templates projetés, y compris aux panneaux imbriqués) + méthodes publiques **`focusFirst()`** / **`closeFlyouts()`**. Robustesse : les mises au focus différées utilisent des **macrotâches et non `requestAnimationFrame`** (rAF ne tire jamais dans un onglet throttlé/arrière-plan) ; la fermeture extérieure du popup ignore les interactions dans les overlays (panneaux flyout) ; **suivi du scroll** : popup et flyouts se re-ancrent sur leur déclencheur/item à chaque scroll (ré-application manuelle des positions, différée d'une macrotâche pour laisser le panneau parent se re-ancrer d'abord — la stratégie `reposition` du CDK s'est montrée inopérante en zoneless)
- `ui-breadcrumb` : **fil d'Ariane** headless — montre la position courante dans une hiérarchie de navigation. Modèle déclaratif `items` (`UiBreadcrumbItem[]`) : destination **externe** (`url`/`target`) ou **interne** (`routerLink`), callback `command` (`{ originalEvent, item }` + sortie `itemClick`), icône FontAwesome optionnelle (élément **icon-only** pour l'accueil, garde-fou a11y hérité de `ui-link`), `disabled`/`visible`/`ariaLabel`/`styleClass` par élément. Chaque élément est une **instance `ui-link`** (composition, zéro style dupliqué — le Figma instancie de même) ; le dernier élément porte **`aria-current="page"`**. **Séparateur** personnalisable : chaîne `separator` (défaut `/`) ou template `#separator` (ex. chevron `ui-icon`) ; contenu d'élément personnalisable via le template **`#item`** (contexte `$implicit` + `last`). **Repli des fils longs** : au-delà de `maxItems`, le milieu se replie derrière un **bouton ellipsis accessible** (`<button>` natif nommé `ellipsisAriaLabel` + `aria-expanded`) qui révèle les éléments masqués et **déplace le focus** sur le premier révélé — remplace le variant Figma booléen `Collapsed` par un repli dérivé du contenu, les pages intermédiaires restant atteignables (clavier + lecteur d'écran). Tailles `default`/`small` (liens `ui-link` + séparateur alignés sur la convention du kit : texte `default`/`md`). **A11y** : landmark `<nav>` nommé (`ariaLabel`, défaut « Fil d'Ariane »), liste ordonnée `<ol>`/`<li>`, séparateurs `aria-hidden`, aucune interaction clavier spécifique (séquence de tabulation naturelle). Co-localisé, 100 % tokens `global`/`actions`/metrics/typography, constantes SCSS locales `$sizes`/`$item-gap`/`$focus-ring-width`
- `ui-drawer` : **panneau glissant** headless ancré à un bord de l'écran, inspiré de PrimeNG `p-drawer`. Surface stylée par tokens rendue au-dessus d'un masque de viewport optionnel, pilotée par le modèle **two-way `visible`**. **4 positions** (`left`/`right`/`top`/`bottom`) + mode **`fullScreen`** (plein viewport). **Glissement directionnel câblé sur le système de motion** : le preset d'entrée/sortie est **dérivé de `position`** (`slide-right`/`slide-left`/`slide-down`/`slide-up`) avec l'amplitude étirée à `100 %` de la taille propre du panneau (glissement complet depuis le bord), le plein écran passant en fondu ; masque en fondu (`animate.enter`/`animate.leave` + `[uiMotion]`), coupable par `motionDisabled` / `prefers-reduced-motion` / `data-motion="off"`. Trois régions composables : `header` (chaîne) ou template `#header`, corps `<ng-content>` (**scroll interne**), pied via template `#footer` (+ `#closeicon`), et un template **`#headless`** qui prend la main sur **tout l'intérieur** du panneau (le drawer conserve la mécanique d'overlay). **Mode modal** (défaut) : masque qui assombrit (`--primitives-black-50`, surchargeable via `--ui-drawer-mask-background`) et **bloque le scroll de fond** (compteur partagé + compensation de la gouttière de scrollbar) ; `dismissableMask` (défaut **activé**) ferme au clic extérieur. **Mode non-modal** (`modal=false`) : arrière-plan interactif, `blockScroll` optionnel. Mode **`contained`** (scope à l'ancêtre positionné en `position: absolute`, sans blocage du scroll — pour embarquer/prévisualiser), `drawerStyle`/`styleClass`/`maskStyleClass`, empilement `autoZIndex`/`baseZIndex`. **A11y** : `role="dialog"` + `aria-modal`, nom accessible via `aria-labelledby` (titre) sinon `ariaLabel`/`ariaLabelledBy` (garde-fou dev), **piège de focus + capture/restauration** (`@angular/cdk/a11y` `cdkTrapFocus`/`cdkTrapFocusAutoCapture`), fermeture `Échap` (`closeOnEscape`+`closable`), bouton de fermeture `<button>` natif. Sorties `onShow`/`onHide`. Rendu **en place** (masque `position: fixed`, non téléporté vers `body` — cohérent avec `ui-modal`/`ui-toast`). Structure **positionneur (glissement) + panneau (surface)** pour préserver l'animation native. 100 % tokens `global`/metrics, bordure logique sur le bord intérieur, constantes SCSS locales `$drawer-z-index`/`$mask-background`/`$size-side`/`$size-edge`/`$panel-padding`
- `ui-menu` : menu de **navigation / commandes** headless, statique ou **popup**, inspiré de PrimeNG `p-menu` (+ le repli de `p-panelmenu`). Modèle déclaratif `items` (`UiMenuItem[]`) : **groupes à en-tête** (label + séparateur plat entre sections), **sous-menus imbriqués repliables** — repliables **par défaut au-delà du premier niveau**, surchargeable par item via `toggleable` (forcer un groupe racine repliable / figer un groupe imbriqué ouvert) —, entrées `command` (`{ originalEvent, item }`), navigation **router** (`routerLink`/`queryParams`/`routerLinkActive` stylé par les tokens `-active`) et **URL externes** (`url`/`target`, `rel` sécurisé auto), `disabled`/`visible`/`title`/`ariaLabel`/`styleClass` par item. État d'ouverture **contrôlable** via `expandedKeys` (two-way `model`, objet `{ [id]: boolean }` indexé sur l'`id` des items). Mode **`popup`** : panneau dans un **overlay CDK** ancré à l'élément déclencheur (`toggle(event)`/`show(event)`/`hide()`), retournement auto au-dessus (`autoFlip`), fermeture clic extérieur/`Échap`/activation avec **retour du focus au déclencheur**, entrée animée `slide-down` (système de motion). Repli des sous-menus par la technique **grid-rows** animée (chevron rotatif), coupée par `[motion]="false"` / `prefers-reduced-motion` / `data-motion="off"`. **Clavier roving WAI-ARIA menu** : tab stop unique, `↑`/`↓` en boucle, `Début`/`Fin`, `→`/`←` ouvre/ferme un groupe, `Entrée`/`Espace` active, `Échap` ferme. A11y : `role="menu"`/`menuitem` sur de vrais `<a>`/`<button>`, groupes `role="group"` + `aria-labelledby`, toggles `aria-expanded`+`aria-controls`, contenu replié `inert`, focus ring `--global-high-stroke-focused` (la famille `navigation` n'a pas d'état `focused`). **Templates** `#item` (contenu du menuitem, `$implicit`), `#submenuheader`, slots `#start`/`#end`. Familles de couleur `level` `high`/`low` (items 100 % tokens `navigation.*`), **densités `size` `default`/`small`** (map SCSS `$sizes` — rendu compact pour les menus d'actions « … »). Panneau **sans bordure** (élévation par l'ombre partagée) au **fond opaque `global`** (le token `navigation.*-surface-default` est transparent par design), coque via le **mixin partagé `overlay-panel`**, constantes SCSS locales `$sizes`/`$panel-surface`/`$item-radius`/`$group-indent`
- **Recette partagée `overlay-panel`** pour les panneaux flottants (dropdowns) : groupe de constantes structurelles `$overlay-panel-*` dans `_ui-config.scss` (radius, padding, ombre, épaisseur de trait, offset déclencheur↔panneau — catégorie **global**) + mixin `utils.overlay-panel($surface, $stroke)` (`src/styles/src/utils/_overlay.scss`). La **structure** est partagée entre tous les panneaux flottants (menu, calendrier du datepicker, futurs select/multiselect…), les **couleurs** restent le choix de la famille de tokens de chaque composant (menu → `navigation.*`, forms → `form.*`) — re-styler une famille n'impacte pas les autres ; échappatoire par instance via les custom properties `--ui-overlay-*`
- `ui-nudger` : **incrémenteur numérique** headless (`[−] valeur [+]`) — deux `ui-button` icon-only (`−`/`+`) encadrant la valeur courante, sans champ de saisie (cas « stepper » distinct de `ui-input-number`). Valeur `number` via `ControlValueAccessor` (`BaseControlValueAccessor`) : autonome, `[(ngModel)]`, **Reactive Forms** et **Signal Forms**. Bornes `min`/`max` + pas `step` : le bouton concerné est **désactivé** dès la borne atteinte — les états Figma `Min`/`Max` sont **dérivés** de la valeur, jamais des props (idiome Angular). Tailles `default`/`small` (valeur en typo `title/xl`/`title/lg`), familles de couleur `level` (tokens `actions.*`, transmises aux boutons), `disabled`/`readonly`/`invalid`, icônes et libellés a11y des boutons personnalisables (`decrementIcon`/`incrementIcon`/`decrementLabel`/`incrementLabel`), formateur d'affichage optionnel `formatValue` (unité/symbole), `defaultValue` (valeur de repli / autonome). **A11y** : racine `role="group"` avec nom accessible obligatoire (garde-fou dev sur `ariaLabel`/`ariaLabelledBy`), boutons `<button>` natifs opérables au clavier, valeur en **région live** (`aria-live="polite"`) annoncée à chaque changement, `emitTouch` au `focusout` réel (hors va-et-vient entre les deux boutons). Sortie `valueChange`. Co-localisé, 100 % tokens `form`/`actions`/metrics/typography, `$sizes` en constante SCSS locale. Sans équivalent direct PrimeNG
- `ui-tabs` : onglets **headless** en **API de composition** (`ui-tabs` conteneur + `ui-tab-list` / `ui-tab` + `ui-tab-panels` / `ui-tab-panel`), inspiré de PrimeNG `p-tabs`. Le conteneur pilote l'état actif (`value` two-way `model`), l'`orientation` (`horizontal` / `vertical`), le `scrollable` (zone défilante + navigateurs précédent/suivant `showNavigators` gated sur le débordement), le `lazy` (défaut de groupe, surchargeable par panneau) et le `selectOnFocus` (activation automatique au focus). Chaque onglet est un `<button role="tab">` **natif** (hôte `ui-tab` en `display: contents` → bouton directement dans le `role="tablist"`), avec **navigation clavier roving WAI-ARIA** (`←`/`→` ou `↑`/`↓` selon l'axe en boucle, `Début`/`Fin`, `Entrée`/`Espace` pour activer), **tab stop unique** (roving `tabindex` sur l'onglet actif / premier disponible), `disabled` natif (sauté au clavier), `icon`/`iconPos` optionnels. Structure ARIA complète : `role="tablist"` (`aria-orientation`) → `role="tab"` (`aria-selected` + `aria-controls`) → `role="tabpanel"` (`aria-labelledby` + `tabindex="0"`), panneaux **conservés dans le DOM** mais `hidden` quand inactifs (état préservé) ou rendus paresseusement (template `#content` pour n'initialiser qu'à l'activation). **Indicateur d'onglet actif glissant** mesuré (ResizeObserver + `afterRenderEffect`, offset/longueur du bouton actif) cadencé par le **système de motion** partagé, **personnalisable** via les variables CSS exposées `--ui-tabs-active-bar-color` / `--ui-tabs-active-bar-size` ; panneaux en **fondu** à l'activation (keyframes du système de motion). Mode **menu de navigation** (onglets seuls sans panneaux, contenu fourni par un `router-outlet` via `[(value)]`). Sortie `tabChange`. Coupure de motion par `[motion]="false"`, `data-motion="off"` et `prefers-reduced-motion`. 100 % tokens `navigation` (liste/onglets) + `global` (contenu/trait), constantes SCSS locales `$tab-min-height`/`$bar-thickness`/`$track-thickness`/`$nav-size`
- `ui-link` : **lien textuel inline** headless — un vrai `<a>` destiné au fil du texte (paragraphes) et à la navigation, stylé avec la famille sobre `actions.low.content.*` (light **et** dark). Destination **externe** (`href`) **ou interne** (`routerLink`, directive Angular) ; `external` force `target="_blank"` + `rel="noopener noreferrer"` sûr, `rel`/`target` surchargeables. Icônes optionnelles de chaque côté via **présence d'input** (`iconLeft`/`iconRight`, noms FontAwesome — idiome Angular équivalent aux booléens `Icon Left`/`Icon Right` de Figma), mode **icon-only** déduit (sans `label`, avec garde-fou a11y dev sur `ariaLabel`). Tailles `default`/`small`. **Souligné par défaut** (a11y : la couleur n'est jamais le seul repère d'un lien inline — ajout intentionnel au spec Figma qui n'encode que la couleur), retiré en icon-only. États interactifs (hover/focus/pressed/disabled) **pilotés en CSS par les tokens**, jamais en props ; `disabled` propre à l'ancre (retrait de `href`/`routerLink`, `aria-disabled`, hors tabulation). Sorties `linkClick`/`linkFocus`/`linkBlur` + méthode `focus()`. Co-localisé, 100 % tokens `actions`, constantes SCSS locales `$sizes`/`$focus-ring-width`
- `ui-modal` : fenêtre modale **headless** — surface flottante stylée par tokens rendue au-dessus d'un masque de viewport, pilotée par le modèle **two-way `visible`**. Entrée/sortie via le **système de motion** (`animate.enter`/`animate.leave` + `[uiMotion]` : fondu du masque + `zoom` du dialogue par défaut, preset surchargeable via `motion`/`motionDisabled`). Trois régions composables : `header` (chaîne) ou template `#header`, corps `<ng-content>` (**scroll interne** au-delà de `max-height: 90vh`), pied via template `#footer` (+ `#closeicon`). **Mode modal** (défaut) : masque qui assombrit (`--primitives-black-50`, surchargeable via `--ui-modal-mask-background`) et **bloque le scroll de fond** (compteur partagé + compensation de la gouttière de scrollbar → pas de saut de mise en page) ; `dismissableMask` ferme au clic extérieur. **Mode non-modal** (`modal=false`) : arrière-plan interactif et scrollable (masque traversant), `blockScroll` peut tout de même figer le scroll. **Déplaçable** (`draggable`, poignée d'en-tête, offset via la propriété CSS indépendante `translate` pour ne pas heurter l'animation `zoom`, `keepInViewport`/`minX`/`minY`), **agrandissable** (`maximizable`, plein viewport ↔ restauration, `aria-pressed`), **redimensionnable** (`resizable`, poignée au coin bas-droit façon `textarea` — pointeur uniquement, tailles minimales `minWidth`/`minHeight`, sortie `onResizeEnd`), **9 positions** (`center`/`top`/`bottom`/`left`/`right` + 4 coins), largeurs **par palier** (`breakpoints`, feuille de style runtime scoping `data-modal-uid`), mode **`contained`** (scope à l'ancêtre positionné en `position: absolute` + pas de blocage du scroll — pour embarquer/prévisualiser une modale dans un conteneur), `dialogStyle`/`styleClass`/`maskStyleClass`, empilement `autoZIndex`/`baseZIndex`. **A11y** : `role="dialog"` + `aria-modal`, nom accessible via `aria-labelledby` (titre) sinon `ariaLabel`/`ariaLabelledBy` (garde-fou dev), **piège de focus + capture/restauration** (`@angular/cdk/a11y` `cdkTrapFocus`/`cdkTrapFocusAutoCapture`, `focusTrap`/`focusOnShow`), fermeture `Échap` (`closeOnEscape`+`closable`), boutons d'en-tête `<button>` natifs, respect de `prefers-reduced-motion` / `data-motion="off"`. Sorties `onShow`/`onHide`/`onMaximize`/`onDragEnd`. API inspirée de PrimeNG `p-dialog`. Rendu **en place** (masque `position: fixed`, non téléporté vers `body` — cohérent avec `ui-toast`). 100 % tokens `global`/metrics, constantes SCSS locales `$modal-*`/`$dialog-*`/`$mask-background`
- `ui-segment-control` : contrôle segmenté headless (équivalent d'un _select button_) — choix d'**une** valeur (mode simple) ou de **plusieurs** (`multiple`, le modèle devient un tableau) parmi une courte liste de segments inline. Chaque segment est un `<button>` natif ; le groupe porte la sémantique **WAI-ARIA** (`radiogroup`/`radio` + `aria-checked` en simple avec **navigation aux flèches et sélection au déplacement** — motif radio ; `group`/`aria-pressed` en `multiple`), **roving tabindex** (un seul segment tabbable, flèches ←/→ ou ↑/↓ selon `orientation` + Début/Fin). Options **polymorphes** : primitives, objets `{ value, label?, icon?, disabled?, ariaLabel? }`, ou objets métier via mapping de champs (`optionLabel`/`optionValue`/`optionDisabled`/`optionIcon`) et `dataKey` pour l'égalité des valeurs objet. En mode simple, **indicateur glissant** mesuré (ResizeObserver + `afterRenderEffect`) qui glisse sous le segment actif, cadencé par le **système de motion** (`utils.motion-transition` + garde `motion-reduce`, opt-out `[motion]="false"`, `prefers-reduced-motion` et `data-motion="off"`) ; en mode `multiple`, chaque segment coché peint sa surface. Tailles `default`/`small`, `fluid` (pleine largeur, segments à parts égales), `orientation` `horizontal`/`vertical`, **template de segment** via `[itemTemplate]` (contexte `{ $implicit, option, selected, index }`), `allowEmpty` (autorise le vidage / empêche de vider le dernier en `multiple`), `disabled`/`readonly`/`invalid`. Sorties `selectionChange` et `optionClick`. Valeur via `ControlValueAccessor` (`BaseControlValueAccessor`) : compatible autonome, `[(ngModel)]`, **Reactive Forms**, et **Signal Forms** (`[field]`, via l'interop CVA) ; style **erreur automatique** quand le contrôle est invalide et touché/modifié. Garde-fous a11y en dev (nom accessible du groupe, segment icon-only sans `ariaLabel`). 100 % tokens `actions`/`global` (bordure de piste en `box-shadow` inset → math d'offset exacte), gouttière interne `$gutter` (4px) rendant les coins de l'indicateur concentriques au track (radius-md 12 = 4 + radius-sm 8), constantes SCSS locales `$sizes`/`$gutter`. API inspirée d'un _select button_
- `ui-toast` : notification transitoire headless en **trois pièces** — la carte présentational **`UiToast`** (icône · titre/texte · fermeture, 100 % tokens `informative`, niveaux `default`/`highlight`/`success`/`warning`/`error` × `subLevel` `high`/`low`, mode bannière `expanded`), le service **`UiToastService`** (`add` retournant l'id / `addAll` / `clear(key?)` / `remove(id)`, store à base de **signals**, `providedIn: 'root'`, inspiré de PrimeNG `MessageService`), et le conteneur **`ui-toast-container`** ancré dans le viewport. Le conteneur lit le service, filtre par `key`, empile les toasts sur **7 positions** (`top`/`bottom` × `left`/`center`/`right` + `center`), anime l'entrée/sortie via le **système de motion** (`animate.enter`/`animate.leave` + `[uiMotion]`, preset choisi selon la position, surchargeable via `motion`/`motionDisabled`), et gère l'**auto-fermeture** (`life`, `sticky` pour la désactiver) avec **mise en pause au survol et au focus clavier**. Messages `UiToastMessage` : `title`/`text`/`level`/`subLevel`/`icon` (nom FA, `true` = icône du niveau, `false` = masquée)/`closable`/`sticky`/`life`/`key`/`data`/`styleClass`. Contenu **personnalisable** via l'input `template` (contexte `{ $implicit: message, closeFn }`) pour les toasts custom et à **actions** (`ui-button`). Positionnement `fixed` par défaut, `contained` (`position: absolute`) pour scoper la pile à un ancêtre positionné. A11y : région live par toast (`role="alert"`+`aria-live="assertive"` pour `error`/`warning`, `role="status"`+`polite` sinon, `aria-atomic`), bouton de fermeture `<button>` natif avec `aria-label`, icône décorative, respect de `prefers-reduced-motion` / `data-motion="off"`. Parité API PrimeNG côté conteneur : `stackVisibleLimit` (plafond de toasts visibles, les autres en file mais avec compte à rebours actif), `stackGap` (écart runtime entre cartes), `preventDuplicates` (fusion des messages de contenu identique), `autoZIndex`/`baseZIndex` (empilement `z-index` automatique/plancher). `breakpoints` volontairement non repris (largeur déjà responsive, styles par palier via `styleClass` + `@media`). API inspirée de PrimeNG `p-toast`. Constantes SCSS locales `$toast-*` (z-index, largeurs, gouttières)
- **Système de motion** partagé, piloté par les tokens et sans dépendance runtime `@angular/animations` : (1) presets d'entrée/sortie globaux `ui-motion-<preset>-enter|leave` (`fade`, `slide-up`/`down`/`left`/`right`, `zoom`, `collapse`) consommés par l'API Angular native `animate.enter`/`animate.leave` (`src/styles/src/base/_motion.scss`, keyframes + classes) ; (2) directive **`[uiMotion]`** (`src/app/shared/motion/ui-motion.ts`) qui sélectionne un preset, expose `enter()`/`leave()` et applique les surcharges de timing (`motionDuration`/`motionEasing`/`motionDelay`/`motionDistance`/`motionScale`/`motionDisabled`) ; (3) mixin SCSS **`utils.motion-transition(...)`** + garde **`utils.motion-reduce`** pour les transitions d'état (`src/styles/src/utils/_motion.scss`). Personnalisation par les custom properties `--ui-motion-*` (défauts = groupe de tokens partagés `$motion-*` dans `_ui-config.scss`). Désactivation par élément (`motionDisabled`), globale (`data-motion="off"` sur `<html>`, réinitialise animations + transitions), et respect automatique de `prefers-reduced-motion`. Documenté dans **Foundations → Motion** (démos live par preset)
- `ui-accordion` : accordéon **headless** en **API de composition** (`ui-accordion` conteneur + `ui-accordion-panel`), inspiré de PrimeNG `p-accordion`. Le conteneur pilote l'état d'ouverture (`value` two-way `model`, modes `single`/`multiple`), la **navigation clavier roving** entre en-têtes (`↑`/`↓` en boucle, `Début`/`Fin`), `selectOnFocus`, et les défauts de groupe (`separator`, `control`, `expandIcon`/`collapseIcon`, `motion`) surchargeables par panneau. En-tête entièrement `<button>` (motif **WAI-ARIA Accordion** : `aria-expanded` + `aria-controls`, cible de clic large), chevron dans une pastille circulaire (Figma 40×40, tokens `actions.low`) réagissant à l'état de l'en-tête ; en-tête riche via le slot `uiAccordionHeader` (ou raccourci `header`). Contenu (`role="region"` + `aria-labelledby`) **conservé dans le DOM** mais `inert` et de hauteur nulle quand fermé (état de formulaire préservé), replié par la technique **`grid-template-rows`** animée via le système de motion (coupable par `[motion]="false"`, reduced-motion et `data-motion="off"`). Panneaux `disabled` (ignorés au clavier), sorties `onOpen`/`onClose`. 100 % tokens `informative`/`actions`, réutilise `ui-separator` et `ui-icon`
- Groupe de tokens de timing partagés `$motion-*` dans `_ui-config.scss` (durées `fast`/`base`/`slow`, easings `standard`/`enter`/`leave`, `distance`, `scale`, catégorie **global**), consommé par le système de motion
- `ui-card` : conteneur **flexible** headless composant des régions optionnelles — **média** pleine largeur (clippé aux coins arrondis), **en-tête** (titre + sous-titre), **contenu** (slot par défaut) et **pied** (actions/tags alignés à droite). Système de slots à double niveau : raccourcis `header` / `subheader` (aucun import) + **directives marqueurs** exportées (`UiCardMedia`, `UiCardTitle`, `UiCardSubtitle`, `UiCardFooter`, forme attribut `[uiCardX]` ou élément `ui-card-x`) pour du contenu riche. Chaque région n'est rendue que si elle porte du contenu (détection via `contentChild` + `afterNextRender` pour le slot par défaut) : aucun wrapper vide ni espacement fantôme. Le slot **titre** centre nativement ses adornments (tag / icône) sur la ligne de texte. Mode **`contentFlush`** : retire la gouttière du contenu (pleine largeur, edge-to-edge) tout en gardant en-tête / pied en retrait — la gouttière reste ré-applicable via la variable CSS exposée `--ui-card-padding` (ex. bande média/couleur sans gutter + paragraphe gouttiéré). Le pied s'aligne à droite par défaut mais accueille la grille **Gridaflex** (`flex-x` / `flex-gap-x` / `align-justify`) pour l'espacement et la répartition (tags à gauche / action à droite). Variantes de surface `outlined` / `elevated` / `flat`, promotion en `role="region"` via `ariaLabel` / `ariaLabelledBy`. API inspirée de PrimeNG `p-card`, 100 % tokens `global` (light/dark), personnalisation via variables SCSS locales `$card-*`
- `ui-textarea` : zone de texte **multiligne** headless — bâtie sur le shell `ui-field` (mode `multiline`) + un `<textarea>` natif. Label + helper, niveaux `default`/`success`/`error` (bordure + message), tailles `default`/`small`, `rows`, `maxlength`, `placeholder`, `required`/`disabled`/`readonly`/`invalid`. Redimensionnement via la poignée **native** logée dans le coin bas-droit — axe configurable `resize` (`none`/`vertical`/`horizontal`/`both`, `vertical` par défaut) — ou `autoResize` (la hauteur suit le contenu). Compteur de caractères optionnel (`showCount`, affiche `saisi / max` avec `maxlength`). Gestion d'erreur **dynamique** via la validation du contrôle (`showError` = invalide + touché/modifié). Valeur via `ControlValueAccessor` (`BaseFormField`) compatible reactive forms et `[(ngModel)]`. API inspirée de PrimeNG `pTextarea` (`autoResize`, `rows`). Co-localisé, 100 % tokens `form`.
- Mode `multiline` du shell `ui-field` : boîte haut-alignée sans padding (le contenu porte son inset, poignée de resize dans le coin), hauteur automatique avec `min-height`, et **ligne de pied de champ** (`hasFooter` + slot `[uiFieldFooter]`) pour héberger un compteur (support de `ui-textarea`)
- Groupe de constantes partagées `$form-textarea-*` dans `_ui-config.scss` (min-height + padding vertical, catégorie **forms**)
- `ui-rating` : notation étoiles headless — configuration du nombre d'étoiles, annulation (toggle off au clic), support natif du clavier et des lecteurs d'écran via un `<input type="range">` invisible superposé (pattern WAI-ARIA slider), hover interactif sur les étoiles SVG (`ui-icon`). Valeur via `ControlValueAccessor` compatible avec les formulaires réactifs et template-driven, gestion des états `disabled` / `readonly` / `invalid`. Co-localisé, 100 % design tokens `form`.
- `ui-datepicker` : sélecteur de **date / mois / année** (et d'**heure** optionnelle) headless — champ déclencheur `ui-input` en lecture seule ouvrant un calendrier dans un overlay Angular CDK (ou rendu **inline**). Sélection `single` / `multiple` / `range` (`selectionMode`, valeur `Date | Date[] | null`), **vues à tiroir** jour → mois → année (clic sur l'entête) + modes `MonthPicker`/`YearPicker` (`view`), **plusieurs mois** côte à côte (`numberOfMonths`, séparateur + titres alignés sur les flèches), ouverture en overlay avec **retournement automatique** au-dessus quand la place manque (désactivable via `autoFlip`), navigation mensuelle, **focus roving** clavier complet (flèches / Début-Fin / Page↑↓ ±année / Entrée-Espace / Échap, motif WAI-ARIA), bornes `minDate`/`maxDate`, `disabledDays`/`disabledDates`, `showTime` (steppers verticaux HH:MM façon PrimeNG, pattern ARIA `spinbutton` pilotable au clavier, `hourFormat` 12/24h + AM/PM), `timeOnly` (icône horloge), effacement `showClear` (croix dans le champ), barre de boutons `showButtonBar` (Aujourd'hui/Effacer), `firstDayOfWeek`, `locale` + `dateFormat` custom, `closeOnSelect`. **Templates** projetés `#date` (cellule de jour, contexte `$implicit`+`selected`) et `#buttonbar` (contexte `todayCallback`/`clearCallback`). Valeur via `ControlValueAccessor` sur `BaseFormField` (label/helper/erreur/niveaux/états partagés). 100 % tokens `form`/`actions`, overlay ancré sur la boîte de champ (placement correct même avec helper), personnalisation via `panelStyleClass` + variables SCSS locales. API inspirée de PrimeNG `p-datepicker`
- `ui-chip` : jeton compact **interactif** headless — relative actionnable de `ui-tag`. Libellé + **icône de tête** (`icon`) **ou image façon avatar** (`image`, prioritaire, repli automatique sur icône/label en cas d'échec + output `imageError`), **action de suppression** optionnelle (`removable`) rendue en `<button>` natif (focusable au `Tab`, déclenchable `Entrée`/`Espace` natif + `Retour arrière`/`Suppr`, output `remove`), et **contenu projeté** libre pour du contenu personnalisé (feature Template). Mode **sélectionnable** (`selectable`) : la racine devient un `<button aria-pressed>` focusable au clavier, un clic (ou `Entrée`/`Espace`) bascule l'état `selected` (two-way `model`), sélectionné le chip prend le remplissage soutenu `high` de sa famille et affiche l'icône `selectedIcon` (`check` par défaut) en tête — motif choice/filter chip pour la sélection d'entités (exclusif de `removable`, avertissement dev). Familles de couleur `level` (`default`/`highlight`/`success`/`warning`/`error`) × intensité `subLevel` (`high`/`low`, défaut `low` discret), tailles `default`/`small`, forme `rounded` (pilule) ou rectangle arrondi, `disabled` (désactive nativement la suppression / sélection). A11y : `label` exposé en `aria-label` par défaut, surchargeable par `ariaLabel`/`ariaLabelledBy` ; garde-fou `isDevMode()` si un chip icône/image seule n'a pas de nom accessible ; anneau de focus distinct du survol sur l'action de suppression. 100 % tokens `informative`, états interactifs en pseudo-classes CSS (jamais des props)
- `ui-tag` : étiquette informative (pilule) headless — libellé + icône optionnelle de chaque côté (`iconLeft`/`iconRight`), familles de couleur `level` (`default`/`highlight`/`success`/`warning`/`error`) × intensité `subLevel` (`high`/`low`), tailles `default`/`small`, forme `rounded` (pilule) ou rectangle arrondi. Composant statique inspiré de l'API PrimeNG `Tag` (`severity` → `level`×`subLevel`), 100 % tokens `informative`, garde-fou a11y icon-only (`ariaLabel`)
- `ui-tooltip` : infobulle headless via la **directive** `[uiTooltip]` (Angular CDK Overlay) — placement 4 côtés (`top`/`bottom`/`left`/`right`) avec retournement automatique dans le viewport (`fitContent`, désactivable pour verrouiller le côté) et repositionnement au scroll, déclenchement `hover`/`focus`/`both` (focus clavier par défaut), `showDelay`/`hideDelay`/`life`, contenu texte / HTML assaini (`escape`) / `TemplateRef` (`tooltipContext`), `autoHide` (infobulle survolable), `hideOnEscape`, `showOnEllipsis`, offsets `positionTop`/`positionLeft`. API inspirée de PrimeNG `pTooltip`. Panneau visuel co-localisé consommant directement les tokens `informative`, personnalisation ponctuelle via `tooltipStyleClass`. `aria-describedby`/`role="tooltip"` + prise en charge `prefers-reduced-motion`
- Feuille CSS `@angular/cdk/overlay-prebuilt.css` ajoutée aux cibles de build (`angular.json`) pour le positionnement des overlays CDK
- `ui-avatar` : avatar utilisateur headless (modes image / initiales / icône déduits des inputs, tailles `small`/`default`/`large`, forme `circle`/`square`, badge de statut projeté via `[avatarBadge]`, repli automatique sur image cassée + output `imageError`)
- `ui-avatar-group` : empilement d'avatars avec chevauchement (débordement « +N » via un avatar Label), inspiré de l'API PrimeNG avatargroup
- `ui-separator` : séparateur visuel et sémantique (`role="separator"`, horizontal/vertical, trait solid/dashed, épaisseur `default`/`small`, libellé optionnel aligné start/center/end)
- Groupe de tokens partagés `$avatar-*` dans `_ui-config.scss` (diamètres + chevauchement, catégorie **informative**)
- Storybook : addon d'accessibilité `@storybook/addon-a11y`
- Socle du Design System Angular 21 **headless** (standalone + signals), sans librairie UI propriétaire ; Angular CDK au besoin
- Pipeline de design tokens : sources DTCG (`src/design-tokens/*.json`) → Style Dictionary (`scripts/tokens.build.mjs`, `tokens.config.json`) → variables SCSS/CSS générées
- Architecture de tokens : primitives, semantics, metrics, typography, responsive, transitions
- Multi-marque (brand1/2/3) et clair/sombre via `[data-brand]` / `[data-theme]`, pilotés par `BrandService` et `ThemeService`
- Styles composites (`src/design-tokens/styles.json`) : ombres en variables `--shadow-{catégorie}-{taille}` **et** classes utilitaires `.shadow-{catégorie}-{taille}`
- `ui-config` (`src/styles/src/settings/_ui-config.scss`) : constantes structurelles partagées (focus ring, bordure, taille des contrôles, transition, champ `$form-field-*`) sur 3 niveaux — global / catégorie / composant — + mixin `control-transition()`
- Polices auto-hébergées (variable fonts DM Sans + Inter) via `src/styles/src/vendors/_fonts.scss`
- Composants UI génériques : `ui-button` (patron de référence), `ui-icon`, `ui-image`
- Composants de formulaire headless (`ControlValueAccessor` via `BaseControlValueAccessor` ; input natif invisible superposé ; erreur auto invalide + touché) :
  - `ui-label` (marqueur requis, tailles, hook `--ui-label-color`)
  - `ui-checkbox` (`indeterminate`, `trueValue`/`falseValue`)
  - `ui-radio` (groupe natif par `name`)
  - `ui-toggle` (interrupteur, `role="switch"`, `trueValue`/`falseValue`)
- Famille de champs de saisie sur un **shell partagé** `ui-field` + base `BaseFormField` (label + boîte + helper + états + `errorText`) :
  - `ui-input` (text/password/email/tel/url/search ; icône gauche, unité, **zone d'action** droite pour reveal/clear)
  - `ui-input-number` (`min`/`max`/`step`, spinner ±, flèches clavier, clamp au blur, formatage `Intl` locale/devise/groupement, `role="spinbutton"`)
  - `ui-input-mask` (tokens `9`/`a`/`*`, littéraux auto-insérés, gestion fine du caret, `unmask`)
- Composants informatifs : `ui-helper` (texte d'aide/feedback, niveaux + icône déduite, `ariaLive`), `ui-badge` (compteur/statut, `level` × `subLevel`, point)
- Types partagés de niveaux (`UiLevel`, `UiSubLevel`, `UiFeedbackLevel`) dans `src/app/shared/types/ui-level.ts`
- Storybook 10 (addon-designs Figma, dark-mode) : stories & MDX **co-localisées** par composant dans `src/`, documentation globale dans `storybook/docs/` ; pages `Overview` (galerie par catégorie), `Foundations`, `Spécifications`
- Skill de génération de composant `ui-*` depuis un YAML Figma (`.claude/skills/`)
- Documentation projet (`CLAUDE.md`, `AGENTS.md`) et workflow de versioning (`VERSIONING.md`)

### Changed

- `ui-datepicker`! : **`valueType` requis sur chaque instance** — voir l'entrée `Added` dédiée. `dateSelect` est également retypé `Date` → `Date | string` (suit le même `valueType` que `valueChange`). **Breaking pour tout consommateur existant** : le compilateur Angular (`strictTemplates`) rejette toute instance de `ui-datepicker` sans `[valueType]`/`valueType` explicite (`NG8008`) — `demo-orders` migré dans ce même commit (`valueType="date"`, ses signaux étaient déjà en `Date`)
- `ui-breadcrumb` : **les éléments ne passent plus par `ui-link`** — chaque item rend directement **l'élément natif correspondant à sa sémantique**, ce qui rend l'ancre pilotable item par item au lieu de subir la surface d'API de `ui-link` : `routerLink` → `<a>` + directive `RouterLink` (nouveaux passthroughs **`queryParams`** et **`fragment`**, `target` posé sur l'input de la directive et non plus en `[attr.target]`, sans quoi `RouterLink` `preventDefault` un `_blank`), `url` → `<a href>` (nouveau **`rel`**, avec repli `noopener noreferrer` dès `target="_blank"`), et le `href` passe par une **liaison de propriété** donc par la sanitisation URL d'Angular. **Corrections d'accessibilité** au passage : un item **sans destination** (cas courant du dernier crumb) ou **`disabled`** était rendu en `<a>` **sans `href`** — ni focusable ni annoncée comme lien, mais stylée comme telle ; c'est désormais un `<span class="ui-breadcrumb-text">` (`._current` sur le dernier, `._disabled`), coloré `--global-text-default` au lieu de la couleur de lien. Un item porteur d'une **`command` seule** (action, pas navigation) devient un **`<button type="button">`** : il était jusqu'ici cliquable à la souris mais inatteignable au clavier. Le garde-fou dev « icône sans nom accessible », hérité de `ui-link`, est réimplémenté dans le composant. La restauration du focus après ouverture de l'ellipsis ne cible plus `a` en aveugle mais le premier descendant réellement focusable des crumbs révélés (robuste au template `#item`). Styles co-localisés (`&-link`, `&-text`, `&-icon`, `&-label`) : parité visuelle exacte avec l'ancien rendu `ui-link`, la taille de police étant déjà héritée de `.ui-breadcrumb-list` avec le même mapping `default`/`small`. Story + section MDX « Liens »
- **⚠️ Tokens sémantiques `global` : nouvelle nomenclature `--global-[axe]-[occurrence]`** (rupture, aucun alias de compatibilité) — les 21 tokens `--global-{default|high|low}-{content|surface|stroke}-{default|hover|focused}` sont remplacés par **14** tokens `--global-{background|text|border}-{default|muted|subtle|brand|focus}[-hover]`. Motifs : (1) `high`/`low` désignait une **hiérarchie d'intention** dans `actions`/`form`/`navigation` mais un **niveau de contraste** dans `global` — deux sens pour un mot, principale source de confusion ; (2) le niveau `default` doublonnait `high` (valeurs identiques sur 5 rôles / 6), ce qui avait déjà produit une dérive : `ui-card` peignait son fond avec `default.surface.default` et `ui-menu`/`ui-popover`/`ui-sidebar` le leur avec `high.surface.default`, **même valeur, deux tokens** ; (3) `high.content.focused` valait `primary.500` — ce n'était pas un état de focus mais du texte à la couleur de marque, d'où le renommage en `text.brand` ; (4) l'axe `content`/`surface`/`stroke` est renommé d'après les propriétés CSS qu'il cible (`color`/`background-color`/`border-color`). L'état est **omis au repos** (`--global-text-muted`, pas `…-muted-default`) — d'où trois segments dans le cas courant. **Migration iso-valeur** : 33 des 34 couples (token × mode) rendent exactement la couleur d'avant, vérifié dans le navigateur sur Storybook headless en light **et** dark ; unique écart assumé, `ui-spinner` en dark passe de `white.base` (#ffffff) à `grey.50` (#f6f7f9) suite à la fusion `default.content.default` → `text.default`. Table de correspondance complète (indexée par identité Figma réelle — `localId`, `key`, scopes et comptages de bindings relevés en lecture seule) : **`docs/token-migration-global.json`** ; procédure Figma détaillée, à exécuter **après merge** : **`docs/figma-migration-global.md`**. Correspondance : `high|default.content.default` → `text-default` · `low.content.default` → `text-muted` · `high.content.hover` → `text-default-hover` · `low.content.hover` → `text-muted-hover` · `high.content.focused` → `text-brand` · `high|default.surface.default` → `background-default` · `high.surface.hover` → `background-default-hover` · `low.surface.default` → `background-muted` · `low.surface.hover` → `background-muted-hover` · `high.surface.focused` → `background-brand` · `high|low.stroke.default` → `border-default` · `default.stroke.default` → `border-subtle` · `high.stroke.hover` → `border-default-hover` · `high.stroke.focused` → `border-focus`. Nouvelle section « Anatomie du domaine `global` » dans `Spécifications / Design tokens`
- `ui-datepicker`!, `ui-input-number`!, `ui-nudger`!, `ui-file-upload-list`! : **convention de nommage des inputs de nom accessible** — un input qui alimente un `aria-label` brut porte désormais toujours le suffixe `*AriaLabel` (jusque-là mélangé avec `*Label` selon le composant) : `ui-datepicker` (`prevLabel`→`prevAriaLabel`, `nextLabel`→`nextAriaLabel`, `incrementHoursLabel`/`decrementHoursLabel`/`incrementMinutesLabel`/`decrementMinutesLabel`→`*AriaLabel`, `toggleMeridiemLabel`→`toggleMeridiemAriaLabel`), `ui-input-number` et `ui-nudger` (`incrementLabel`/`decrementLabel`→`incrementAriaLabel`/`decrementAriaLabel`), `ui-file-upload-list` (`removeLabel`→`removeAriaLabel`). Inchangés car hors périmètre : le texte visible de bouton (`todayLabel`/`clearLabel`) et les templates `{0}` interpolés via `formatLabel` (`removeTagLabel`, `overflowLabel`, `uploadingLabel`, `progressLabel`, `failedLabel`…). Convention documentée dans `AGENTS.md`. **Breaking** pour tout consommateur liant ces inputs par leur ancien nom
- **Doc de theming générée depuis le SCSS** — les tables « Theming » des 45 docs composants étaient écrites à la main : elles figeaient le binding (`var(--radius-md)`) et la valeur résolue (`12px`), deux informations qui deviennent fausses dès qu'un projet consommateur retune une variable (et deux valeurs l'étaient déjà : `$form-field-radius` documenté 4px pour 8px réels, `$form-field-height` 48px pour 44px). Elles sont désormais **produites depuis les `.scss`** par `npm run docs:config` (`scripts/docs.config.mjs` → `storybook/generated/ui-config.json`, gitignoré, régénéré par `postinstall` et avant `storybook`/`build-storybook` ; `docs:config:check` pour la CI) et rendues par `<ConfigTable of="ui-<nom>" />` (`storybook/blocks/config-table.js`). Le **contrat** (nom de la variable + rôle) vient d'un commentaire **`///` en fin de déclaration** dans le `.scss` (246 lignes documentées : 232 variables `$` + 14 custom properties exposées) ; le **binding** et la chaîne d'indirections vers `ui-config` sont déduits (badge « partagé » + lien vers la page de groupe) ; la **valeur** est **mesurée dans le navigateur** via une sonde de custom property, donc conforme au thème, à la marque et au viewport actifs (fin des annotations manuelles type « 48px (44px mobile) »). Les maps SCSS sont dépliées ligne par ligne, les hooks `var(--ui-x, <défaut>)` affichent le défaut qui s'applique réellement. Conséquence : **rebinder une variable dans un projet ne demande plus aucune reprise de doc**. Conventions ajoutées à `AGENTS.md`, `CLAUDE.md` et au skill `generate-ui-component` ; section « Theming » systématiquement en bas de page ; mentions de Figma retirées des commentaires SCSS des composants
- **Dé-duplication des formulaires** (aucun changement de comportement) — (1) nouveau `option-resolver.ts` partagé : la résolution d'options (`optionValue`/`optionLabel`/`optionDisabled` en dot-path, égalité par `dataKey`, normalisation en entrées) était copiée à l'identique dans `ui-select`, `ui-autocomplete` et `ui-input-tags` (~60 lignes ×3) ; un correctif s'applique désormais en un seul endroit. (2) nouveau `overlay-positions.ts` : ancrage du panneau déroulant (dessous + flip) partagé par select/autocomplete/tags/datepicker. (3) `base-form-field.ts` scindé : nouvelle base `BaseFieldControl` (uid/ids, `modelValue`, `writeValue`, `isDisabled`/`isInvalid`, inputs aria/état) dont héritent désormais les 8 contrôles non-box (`ui-checkbox`, `ui-radio`, `ui-toggle`, `ui-slider`, `ui-rating`, `ui-nudger`, `ui-segment-control`, `ui-input-otp`) qui re-déclaraient chacun cette plomberie ; `BaseFormField` (champs « box ») l'étend et garde son API inchangée. Effet de bord assumé : quelques inputs de plomberie hérités inertes en plus sur certains contrôles (ex. `readonly` sur `ui-radio`), et suffixes numériques des ids auto-générés désormais issus d'un compteur commun (unicité préservée)
- **i18n des formulaires : toutes les chaînes utilisateur/a11y sont désormais des inputs surchargeables** (pattern `{0}` à la `ui-select.overflowLabel`) — `ui-file-upload` : messages de validation (`invalidTypeMessage`/`invalidSizeMessage`/`limitReachedMessage`), erreurs d'upload (`uploadErrorMessage`/`networkErrorMessage`/`httpErrorMessage`), résumé multi-fichiers (`filesSummaryLabel`), et défauts anglais unifiés en français (`Parcourir`/`Choisir un fichier…`/`Téléverser`/`Effacer`…) ; `ui-file-upload-list` : `failedLabel`/`uploadingLabel`/`progressLabel` ; `ui-datepicker` : panel (`panelAriaLabel`), flèches (`prevAriaLabel`/`nextAriaLabel`) et steppers horaires (`hoursAriaLabel`, `incrementHoursAriaLabel`, `toggleMeridiemAriaLabel`…) ; `ui-input-number` : `incrementAriaLabel`/`decrementAriaLabel` ; `ui-input-otp` : `charAriaLabel` ; `ui-input-tags` : `removeTagLabel`. Une app non francophone peut désormais localiser le kit sans fork
- `ui-input` : **inputs `value` (affichage seul, hors CVA) et ARIA popup (`ariaHasPopup`/`ariaExpanded`/`ariaControls`)** — permet à un hôte composite de piloter le texte visible et la sémantique de déclencheur de popup de l'input natif ; `ui-datepicker` n'utilise plus `[ngModel]`/`FormsModule` en interne (le trigger est alimenté par `[value]`, suppression de la machinerie template-driven embarquée)
- **Migration Angular 22.0.8 / TypeScript 6** — `ng update` + Storybook 10.5.4, Node 22.23.1 ; `ChangeDetectionStrategy.OnPush` généralisé (défaut v22), outputs renommés (`no-output-on-prefix`/`no-output-native`), lint `angular-eslint` 22 + règles a11y template réactivées
- **Zoneless** : `provideZonelessChangeDetection()` remplace `provideZoneChangeDetection` ; `zone.js` retiré des dépendances et des polyfills (le code étant 100 % signals + OnPush)
- **Signal Forms stables (Angular 22)** : chaque composant de formulaire (16 champs CVA) dispose désormais d'une story « Signal Forms » (`@angular/forms/signals` : `form()` + `required()` + directive `[formField]`) et sa doc MDX présente les trois APIs (Signal Forms / Template-driven / Reactive) au présent — l'interop passe par la couche CVA existante, sans réécriture des composants. NB : le sélecteur stabilisé est `[formField]` (les entrées historiques ci-dessous mentionnent l'expérimental `[field]`)
- `ui-image` : migration des derniers `@Input()` décorateurs vers les signal inputs (`input()`/`input.required()`) ; les deux `[ngStyle]` restants (`ui-drawer`, `ui-modal`) deviennent des bindings natifs `[style]`
- `ui-rating`! : **« non noté » devient `null`** (type `number | null`) — le `cancel` (re-clic sur la note courante) et la remise à zéro au clavier émettent `null` (ex-sentinelle `0`), `writeValue(null)` est préservé au lieu d'être converti en `0` ; `required` rejette désormais naturellement l'absence de note (le workaround `Validators.min(1)` devient inutile). Un `0` explicitement écrit reste `0` (affiché 0 étoile). **Breaking** pour les consommateurs typés `number`
- `ui-input-number` : typé honnêtement `BaseFormField<number | null>` — suppression du cast `as number` qui masquait le `null` émis au vidage du champ (aucun changement runtime, le composant émettait déjà `null`)
- `ui-input-number` : **spinner ± mieux intégré** — les boutons haut/bas étaient de petites icônes flottant dans le padding de la boîte (zone de clic étroite, intégration approximative). Ils forment désormais une **colonne pleine hauteur collée au bord droit** de la boîte (padding droit annulé via marge négative), **scindée en deux boutons demi-hauteur** (haut = coin supérieur droit, bas = coin inférieur droit) avec **icônes centrées**, séparateur vertical depuis l'input + séparateur horizontal entre les deux, coins droits arrondis suivant la boîte (`overflow:hidden`), et **surface de survol** `form.high.surface.hover` sur toute la demi-cellule. Résultat : zone de clic nettement plus large (~30×20 au lieu d'une icône) et meilleure lisibilité. Implémenté en généralisant le mixin partagé `utils.field-spinner($btn, $width)` (flush + plein hauteur + split, largeur paramétrable — `ui-input-number` passe une locale `$spinner-width: 30px`) ; 100 % tokens `form.*`, boutons toujours `tabindex="-1"` (clavier via ↑/↓ sur l'input `role="spinbutton"`), désactivation aux bornes inchangée
- `ui-table` : **état de chargement repensé** — plus de masque translucide au-dessus de l'en-tête : pendant `loading`, les lignes du corps sont remplacées par une **ligne d'attente** (`ui-spinner` centré, colspan complet), l'en-tête et le pied restant nets et en place ; les lignes apparaissent une fois les données chargées (a11y inchangée : `aria-busy` racine + spinner `role="status"` nommé ; la custom property `--ui-table-mask-background` disparaît au profit de la constante locale `$loading-padding-y`)
- `ui-table` : **anneau de focus des lignes** — l'indicateur `:focus-visible` encadre désormais la `<tr>` entière (outline intérieur) au lieu d'un box-shadow inset par cellule (qui dessinait quatre bordures par `<td>`) ; **pagination déléguée** au nouveau composant autonome `ui-paginator` (rendu et API `pageChange` inchangés)
- `ui-chip` : **input `removeTabindex`** — permet de sortir l'action de suppression de l'ordre de tabulation (`-1`) quand le chip vit dans un conteneur à focus roving (consommé par `ui-input-tags` pour préserver le motif `listbox`/`option`). Non renseigné = ordre de tabulation naturel (comportement inchangé)
- `ui-field` : **mode `autoHeight`** — la boîte grandit avec un contenu qui passe à la ligne (min-height = hauteur fixe, padding vertical `xs`, centrage conservé ; rendu identique sur une seule ligne). Consommé par `ui-select` en mode `multiple` pour le wrap des chips/valeurs
- `ui-icon` : **hook `--ui-icon-size`** — la taille rendue est désormais lue via `var(--ui-icon-size, <px du preset>)` sur chaque classe de taille. Un composant hôte peut ainsi rendre une icône **au-delà du preset `xl` (32px)** — ex. une icône hero dans `ui-empty-state` — en posant `--ui-icon-size` sur le `<ui-icon>` ou un ancêtre : les custom properties héritent **au travers de la frontière d'encapsulation**, ce qui évite tout `::ng-deep`. Comportement par défaut inchangé (repli sur la valeur du preset `size` quand le hook n'est pas défini)
- `ui-button` : **hook `--ui-button-radius`** — le rayon des coins est désormais lu via `var(--ui-button-radius, var(--radius-sm))`, permettant à un composant hôte de reformer les coins sans percer l'encapsulation (`::ng-deep`). Consommé par `ui-button-split` pour souder ses deux boutons ; comportement par défaut inchangé (repli sur `--radius-sm`)
- `ui-link` : nouvel input `ariaCurrent` — pose `aria-current` sur l'ancre (ex. `"page"` pour l'élément courant d'un fil d'Ariane ; consommé par `ui-breadcrumb`)
- `ui-datepicker` : la coque du panneau consomme désormais les constantes partagées `$overlay-panel-*` (`$panel-radius`/`$panel-padding`/`$panel-shadow` retargetées — valeurs identiques, zéro changement visuel)
- `ui-button` : **hôte polymorphe** — quand `href` ou `routerLink` est fourni, le composant rend un **vrai `<a>`** stylé comme un bouton (au lieu du `<button>`), pour couvrir les CTA qui **naviguent** tout en préservant le comportement natif de l'ancre (Cmd/Ctrl+clic, clic-molette, « ouvrir / copier le lien ») — aucune navigation JS bricolée dans un `(click)`. Nouveaux inputs `href`/`routerLink`/`target`/`rel` (rel `noopener noreferrer` par défaut si `target="_blank"`) ; `disabled` en mode lien retire `href`/`routerLink`, pose `aria-disabled` et sort de la tabulation (pas de `disabled` natif sur `<a>`). Contenu, icônes, tailles et niveaux inchangés. `role="button"` conservé sur l'ancre. Le choix de l'élément suit la **sémantique** (naviguer ⇒ `<a>`, agir ⇒ `<button>`), pas l'apparence ; pour un lien **inline**, utiliser `ui-link`
- Doc de configuration (`Components/Configuration`) refondue : la page principale devient « fonctionnement + sommaire », les groupes de variables sont éclatés en sous-pages (`Global UI` / `Forms` / `Actions` / `Informative`) et chaque composant reçoit une section « Configuration (SCSS) » listant ses variables locales
- `ui-toggle` : ajout d'un template de curseur personnalisé `handle` (contexte `{ checked }`, façon PrimeNG) et de `labelPosition` (`before`/`after`, label cliquable des deux côtés)
- `BaseControlValueAccessor` complété (setDisabledState, état `showError`, désabonnement automatique)
- `ui-icon` : boîte **carrée adaptative** (`aspect-ratio: 1`) pour un encombrement prévisible quelle que soit l'avance du glyphe FontAwesome
- Explorateur `Foundations/Colors` : bascule marque + mode en direct, y compris en mode clair
- **Tokens `actions`** : 10 nouveaux niveaux dédiés aux variantes du bouton — `highOutlined`/`lowOutlined`/`successOutlined`/`warningOutlined`/`errorOutlined` et les 5 `*Ghost` équivalents (300 tokens : 10 niveaux × `content`/`surface`/`stroke` × 5 états × light/dark). Même contrat que les niveaux existants, alias primitives uniquement, et c'est **le token qui porte la transparence** (`surface-default` = `transparent` pour les deux variantes, tout le `stroke` sauf `focused` pour `ghost`) : les composants n'ont plus à coder de cas particulier. Repos accentué (texte `*.700` clair / `*.200` sombre, bordure `*.600` / `*.300`), fond teinté au survol/focus (`*.50` / `*.800`) et à la pression (`*.100` / `*.700`), `low` volontairement un cran plus discret. Contrastes WCAG AA vérifiés sur les 300 combinaisons × 3 marques (pire cas : texte 4,83:1, bordure 3,32:1). Les variables Figma correspondantes restent à créer (les tokens n'ont pas encore de `variableId`)
- `ui-button` : nouvel axe **`variant`** (`filled` défaut/`outlined`/`ghost`), composable avec `level`. `outlined` (bordure) et `ghost` (texte seul) consomment chacun leur jeu de tokens `--actions-<level><variant>-*` : transparents au repos, **fond légèrement teinté** au survol/focus/pression — ils ne diffèrent que par la bordure, absente en `ghost` sauf au focus où elle porte l'anneau. Côté SCSS, un seul mixin `action-colors($set)` sert tous les jeux ; `filled` n'émet aucune classe
- `ui-button` : nouvel axe **`onColor`** (`'dark' | 'light' | null`, défaut `null`) pour un bouton **posé sur un fond de couleur** (bandeau, carte teintée, image). Remplace la variante `contrast` (jamais publiée, ajoutée dans ce même cycle) : `contrast` était une **valeur de `variant`**, donc mutuellement exclusive avec `outlined`/`ghost` — or le cas d'usage réel est précisément « outlined **sur** un fond de couleur ». `onColor` est un **troisième axe orthogonal** : il se combine avec les 5 `level` et les 3 `variant`. Il déclare la **luminosité du fond** (la seule chose que l'appelant connaisse de façon fiable) et le bouton épingle son chrome : `dark` ⇒ blanc, `light` ⇒ sombre. Un principe unique le régit — la polarité de la surface est épinglée, le contenu suit sa surface : en `outlined`/`ghost` le fond porte la couleur (jeux niveau-indépendants), en `filled` la pastille est un neutre opaque et le **label garde la teinte du `level`** (un « Supprimer » blanc reste rouge). Deux polarités et non un booléen : elles **partitionnent la palette** (60/60 fonds couverts pour les marques 1 et 2, 59/60 pour la 3), là où un booléen épinglé au blanc n'en couvrait que 20 sur 47 et dégradait notamment les bandeaux `success`/`warning`, dont les surfaces par défaut (`green.500`/`orange.500`) sont claires et exigent `onColor="light"`. Forwardé par `ui-button-split` et `ui-nudger`
- **Tokens `actions`** : 6 nouveaux jeux `onDark`/`onLight` (420 tokens) adossés à `onColor` — `onDark.outlined`, `onDark.ghost`, `onDark.filled.<level>` et leurs symétriques `onLight`. Seule famille du DS dont les valeurs `modeLight` et `modeDark` sont **volontairement identiques** : un bandeau violet est violet dans les deux thèmes, la polarité ne doit donc pas basculer avec `data-theme` (c'était l'autre défaut de l'ex-`contrast`, qui roulait sur les tokens `--global-high-*`, eux réactifs au thème). Les teintes de survol se déplacent toujours à l'opposé du contenu épinglé — `black.20`/`black.30` en `onDark`, `white.30`/`white.40` en `onLight` — de sorte que survol et pression ne peuvent qu'améliorer le contraste. Contrastes vérifiés sur les 6 265 combinaisons jeu × état × fond légitime × 3 marques (pire cas 4,51:1, à la frontière même du domaine de validité)
- Doc `Spécifications → Design Tokens` : anatomie des noms **corrigée** — la page décrivait un segment `[domaine]` (`token`/`global`) et donnait tous ses exemples sous la forme `--token-actions-high-surface-hover`, or **aucune variable `--token-*` n'existe** (les tokens sémantiques sont générés sans préfixe, la famille ouvre le nom : `--actions-high-surface-hover`). Les 5 exemples de la table des familles pointaient donc vers des variables introuvables, qui résolvent silencieusement à rien. Ajout par ailleurs : règle de casse (camelCase concaténé en minuscules, `informative.defaultHigh` → `--informative-defaulthigh-*`), table des cas où `[niveau]` fusionne un second axe (emphase dans `informative`, variante et polarité dans `actions`), encadré sur l'invariance au thème des jeux `onDark`/`onLight`, et note sur la famille `effects`, plate et hors anatomie. Même formulation obsolète (`token.{modeLight|modeDark}.…`) à corriger dans `CLAUDE.md`
- Doc Storybook : **tableaux homogénéisés sur la charte** `<table className="doc-table">` (bordures, en-têtes, zébrures, hover, `code` stylé, définie dans `storybook/preview-head.html`). 3 tableaux markdown convertis dans `ui-button.mdx`, 2 dans `ui-input-otp.mdx` et `ui-motion.mdx` ; 10 `<table>` nus — donc non stylés — alignés dans `ui-button`, `ui-button-split`, `ui-link`, `ui-checkbox`, `ui-radio`, `ui-breadcrumb`, `ui-image`. Les 65 pages `.mdx` ne comportent plus aucun tableau markdown ni `<table>` nu (102 tableaux, tous en `doc-table`). Note d'écriture : un `*` brut dans du JSX est interprété comme de l'emphase markdown et peut traverser une balise `</code>` (erreur de build `Expected the closing tag </code>`) — utiliser la forme déjà en usage dans le dépôt, `<code>{'*.50'}</code>`
- `ui-button-split` : forwarde le nouvel axe `variant` de `ui-button` aux deux boutons internes (action + trigger de menu), qui portent déjà 100 % de la couleur du composant
- `ui-nudger` : forwarde le nouvel axe `variant` de `ui-button` aux deux boutons `[−]`/`[+]`
- **Champs « boîte » : le contrôle natif épouse désormais la boîte sur les deux axes** — l'inset horizontal était un `padding` porté par `.ui-field-box`, et le contrôle n'occupait que la hauteur de sa ligne de texte : dans un champ de 44 px, l'`<input>` mesurait **19,5 × 232** au centre d'une boîte de **44 × 260**, soit ~55 % de la surface visible. Quatre conséquences, toutes corrigées d'un coup : (1) la moitié de la boîte n'était pas la cible du clic — `ui-field` rattrapait le trou en JS (`onBoxMouseDown` → `control.focus()`), ce qui place le caret **en fin de valeur** au lieu du point cliqué et ne démarre aucune sélection au glissé ; (2) le fond d'autofill Chrome (`:-webkit-autofill`) se peignait sur la boîte de l'`<input>`, donnant une bande colorée flottant au milieu du champ ; (3) la zone de glissé-sélection était limitée à la ligne de texte ; (4) toute outline posée sur l'élément focalisé encadrait ce rectangle intérieur, pas le champ (cf. `Fixed`). **Le contrat s'inverse** : la boîte n'a plus de `padding`, chaque enfant direct porte l'inset du côté où il touche la bordure — nouveau mixin partagé **`utils.field-inset-edges($mode, $inset)`**, en `padding` pour les surfaces interactives (contrôle natif, trigger, wrapper de chips : la zone de clic grandit avec elles) et en `margin` pour les affixes de taille fixe (icône, unité, croix ronde, spinner de chargement : la boîte reste serrée, l'anneau de focus d'une croix reste un cercle). Le mixin `field-native-input($inset)` ajoute `align-self: stretch` + `border-radius: inherit` et applique l'inset par défaut ; il accepte `false` (contrôle imbriqué dans un wrapper qui le porte déjà : `ui-autocomplete`, `ui-input-tags`) ou une longueur (`ui-textarea`, filtre de `ui-select` à `units-sm`). Les deux **marges négatives** qui annulaient le padding de la boîte pour coller le bouton d'action et le spinner ± au bord (`field-action`, `field-spinner`) disparaissent : « collé au bord » est devenu le comportement par défaut. Même traitement appliqué à la ligne de filtre du panneau `ui-select` (l'input de recherche remplit sa ligne de bord à bord). **Rendu inchangé au pixel** : l'inset reste `units-md` (`units-sm` pour le filtre) et se lit au même endroit — seule la boîte du contrôle a grandi sous le texte. Vérifié en Storybook headless sur les **225 stories du dossier `forms`** (aucune anomalie : contrôle plein cadre, rien qui débordait, inset ≤ 12 px partout) ; caret et glissé-sélection contrôlés au clic à 5 px du bord (caret 0 en haut à gauche, sélection `0..5` au glissé, là où le JS renvoyait systématiquement la fin de valeur). `onBoxMouseDown` est conservé : il ne couvre plus que les zones réellement mortes (gouttières du `gap`, icônes décoratives), où renvoyer le focus au contrôle reste le bon geste
- **ui-popover**: le panneau n'est plus modal par défaut — `aria-modal` et le piège de focus (`cdkTrapFocus`) sont maintenant conditionnés par le nouvel input `modal` (défaut `false`), pour rejoindre le patron WAI-ARIA « non-modal dialog » ([FSHSP-53])

### Fixed

- `ui-input-otp` (FSHSP-64): `Home`/`End` keyboard navigation was entirely missing from the cell `keydown` handler — only `ArrowLeft`/`ArrowRight` moved focus, contradicting the ticket checklist's explicit "←/→ move focus, Home/End" requirement. Added both cases (jump to the first/last cell) — verified in Storybook: `End` from cell 2 focuses the last cell (roving `tabindex` updates accordingly), `Home` from the last cell focuses the first
- `ui-input-number` (FSHSP-63): the `unit` suffix rendered unconditionally whenever set, even when `currency` was also set — the ticket checklist and the component's own docstring ("Ignored when `currency` is set") both specify `unit` should be dropped in that case, since the formatted value already carries the currency symbol (e.g. showing both "kg" and "3,00 €" side by side). Added a `displayUnit` computed (`currency() ? null : unit()`) — verified in Storybook: setting `unit` and `currency` together now shows only the currency-formatted value
- `ui-tabs` (FSHSP-40): same defect as `ui-stepper` (FSHSP-38) in the sibling composition — `UiTabPanel.lazy` was OR'd with the group-level `UiTabs.lazy` default, so a panel could only opt into laziness, never opt out of it when the group default was `true`, contradicting the ticket checklist and the MDX's own "surcharge locale du défaut de groupe". Same fix: `lazy` is now an optional `input<boolean>` so an explicit per-panel value overrides the group default (`this.lazy() ?? this.tabs.lazy()`)
- `ui-stepper` (FSHSP-38): `UiStepPanel.lazy` was OR'd with the group-level `UiStepper.lazy` default (`stepper.lazy() || this.lazy()`), so a panel could only ever _opt into_ laziness, never opt out of it when the group default was `true` — contradicting both the ticket checklist and the component's own MDX ("surcharge locale du défaut de groupe"). `lazy` is now an optional `input<boolean>` (`undefined` by default) so an explicit per-panel value truly overrides the group default (`this.lazy() ?? this.stepper.lazy()`), matching the override-input pattern already used in `ui-table`
- `ui-sidebar` (FSHSP-36): four targeted fixes found during the kit QA audit. (1) The static (in-flow rail) presentation's `<aside>` panel had no `tabindex`, so the public `focus()` method silently no-oped in that mode (a plain non-interactive element with no `tabindex` cannot receive programmatic focus) — added `tabindex="-1"`, matching the overlay presentation's panel. (2) With `responsive` enabled, the two-way `visible` model was never reset when the sidebar left the overlay presentation (viewport back above `breakpoint`): closing the offcanvas that way still left `visible` at `true` internally, so crossing back below `breakpoint` later silently reopened it as an overlay without any user action — `visible` is now reset to `false` whenever the sidebar is no longer presented as an overlay. (3) `ui-sidebar-menu`'s `routerLink` leaves relied on `routerLinkActive` alone, which does **not** set `aria-current` on its own (needs the `ariaCurrentWhenActive` input) — the active router-linked entry carried no `aria-current` at all, unlike its `url` (`aria-current="page"`) and manual-`active` (`aria-current="true"`) siblings. Added `[ariaCurrentWhenActive]="'page'"`. (4) `[uiSidebarTrigger]` reflected `aria-controls`/`aria-expanded` on its own host element via a static host binding — when placed on a component wrapper (`ui-button`, as demonstrated in the component's own `Responsive`/`WithMenu` stories), that host is the custom element, not the actual native `<button>` a screen reader exposes as interactive, so the ARIA state was effectively invisible to assistive tech. Now resolves and reflects onto the real interactive descendant (`button`/`a[href]`/`[role=button]`, falling back to the host) via `afterRenderEffect`, matching `ui-popover`'s trigger-resolution convention
- `ui-input-mask` (FSHSP-60): the `Error` story's shared render template never bound `[errorText]` at all — the input reached every story except this one, so no story could ever exercise it. The `Error` story itself also mimicked `ui-input`'s plain `level='error'` + no-`invalid` shape while supplying an `errorText` — by design (see `ui-input`'s `Error` vs `ErrorText` twin stories), `errorText` only surfaces via the form-invalid path (`invalid`/touched+dirty), never from `level` alone, so it would have stayed silently dropped even with the binding fixed. Added the missing `[errorText]` binding + `errorText` `argTypes` entry, and set `invalid: true` on the `Error` story so it actually demonstrates the message (now reaches `aria-describedby`, previously `null`)
- `ui-textarea` (FSHSP-59): the character counter (`showCount`) was `aria-hidden="true"`, making it completely invisible to screen readers — a sighted user sees "17 / 280" but an assistive-tech user gets no indication of the limit at all. Now given an id and wired into the textarea's `aria-describedby` (combined with the helper/error message id when both are shown), so it's read like the rest of the field's description
- `ui-select` (FSHSP-49): in `editable` mode, `clear()` reset the model value but not the internal `editableQuery` signal — after typing (which opens the panel and filters by the typed text) and then clicking the clear (×) action, the trigger went visually empty but the panel stayed filtered on the stale query instead of showing the full option list again. Now `clear()` also resets `editableQuery`, matching the reset already done on option selection and on close
- `ui-nudger` (FSHSP-39): `tabindex` was forwarded only to the increment button, not the decrement one — the two buttons are functionally symmetric (no "primary" one, unlike `ui-button-split`'s action/trigger pair), so a consumer setting `tabindex="-1"` to pull the whole control out of the tab order only skipped `+`, leaving `−` still tabbable. Now forwarded to both
- `ui-button-split` (FSHSP-26): the `Nested` story's model didn't set `toggleable: true` on its top-level groups — since `ui-menu` only makes nested groups toggleable by default at `depth > 0`, "Fichier"/"Partager" rendered as plain, always-expanded section headers instead of the collapsible submenus the story name and MDX ("groupes repliables") both claim to demonstrate. Fixed in the story's model, no component change needed
- `ui-image` (FSHSP-66): MDX was missing the mandatory `## Theming` section — added
- `ui-modal` (FSHSP-52): `aria-modal="true"` was a static attribute, applied even when `modal=false` (a non-modal, embedded dialog) — now `[attr.aria-modal]="modal() ? 'true' : null"`, correctly reflecting the input
- `ui-toast` (FSHSP-78): no `role`/`aria-live` on its own root — unlike its documented sibling `ui-alert`, which self-announces. A standalone `ui-toast` (its own JSDoc: "drop `<ui-toast>` anywhere") was silently invisible to screen readers outside `ui-toast-container`. Moved the role/aria-live logic (already correct in the container) onto the card itself; the container no longer duplicates it
- `ui-toggle` (FSHSP-67): the track had no `:hover` style at all — `--form-{low,high}-content-hover` tokens exist but were never referenced, so hovering (checked or not) looked identical to the resting state
- `ui-checkbox`, `ui-radio` (FSHSP-31, FSHSP-41): the shared `form-control-palette` mixin never applied the `-checked` surface token on `._checked`/`._indeterminate` — only the border color changed, even though `--form-{high,low,success,error}-surface-checked` all exist. Both controls' box now fills with the checked-state color, not just its border
- `ui-file-upload-list` (FSHSP-48): `iconSize` returned the invalid/inverted `'small'`/`'md'` pair instead of `UiIconSize`'s `'sm'`/`'md'` (no `._small` class exists on `ui-icon`, so both the file/trash icons silently fell back to the ambient font-size instead of scaling with the row); now typed `computed<UiIconSize>` and matches the `ui-chip` pattern (`'small'` row → `'sm'` icon, `'default'` → `'md'`). MDX was also missing the `## Theming` section — added
- `ui-label` (FSHSP-37): no co-located `.mdx` at all despite having stories — added, and documented `--ui-label-color` (missing `///`) which drives the label state from parent form components (`ui-checkbox`/`ui-radio`/`ui-toggle`…)
- `ui-separator` (FSHSP-74): MDX was missing the mandatory `## Theming` section — added
- `ui-field` (FSHSP-35): `--ui-field-box-cursor` custom property (already consumed cross-component by `ui-select`/`ui-autocomplete`/`ui-input-tags`) had no `///` doc comment — undocumented despite being a real public contract; documented alongside `--ui-field-radius`
- `ui-icon` (FSHSP-65): `_lg`/`_xl` used hardcoded pixel fallbacks (`24px`/`32px`) and `_sm`/`_md`/`_default` read the text typography scale instead of the dedicated `--size-typography-icon-*` tokens — all five sizes now reference the correct token family
- `ui-icon` (FSHSP-65): dev-mode warning when `decorative=false` without `ariaLabel` (previously rendered `role="img"` with no accessible name, silently)
- `ui-icon` (FSHSP-65): MDX doc was missing the mandatory `## Theming` section — added, documenting the `--ui-icon-size` override hook
- `ui-autocomplete` (FSHSP-27): with `completeOnFocus` **and** `dropdown` both enabled, clicking the dropdown trigger emitted `completeMethod` **twice** for a single click — `onDropdownClick`'s own `focus()` call fires a synchronous `focus` event on the input, which `completeOnFocus`'s `onInputFocus` branch answered with its own query, on top of the dropdown's explicit one. Now guarded with the existing `queryDirty` flag, set before focusing, so a dropdown click always emits exactly one query
- `ui-datepicker` (FSHSP-33): time steppers (`stepHours`/`stepMinutes`/`toggleMeridiem`) no longer act when `disabled`/`readonly`; the chevron buttons and the AM/PM spinbutton now reflect that state (native `disabled`, `aria-disabled`, `tabindex` removed from the tab order) with a matching disabled style
- `ui-datepicker` (FSHSP-33): typing a date with `allowInput` + `showTime` no longer truncates the hour/minute (and AM/PM) digits — the auto-formatting mask now covers the time segments, not just day/month/year
- `ui-datepicker` (FSHSP-33): the weekday header row (`role="row"`) is now nested inside `role="grid"` as its first child instead of being a sibling — fixes an invalid ARIA grid structure
- `ui-datepicker` (FSHSP-33): `MonthPicker`/`YearPicker` grids now support full keyboard navigation (arrows/Home/End/Page Up/Page Down, roving tabindex), previously only the day grid did
- `ui-datepicker` (FSHSP-33): `selectMonth`/`selectYear` now also respect `!showTime()` before auto-closing the panel, consistent with `selectDay`/`selectToday`
- `ui-datepicker` (FSHSP-33): `allowInput` is now inert in `timeOnly` mode (trigger forced back to read-only) instead of silently mis-parsing a bare time string
- `ui-datepicker` (FSHSP-33): MDX doc no longer contradicts itself on the trigger being always read-only — reworded to reflect `allowInput`
- `ui-datepicker` (FSHSP-33): story `argTypes` now cover `showIcon`, `disabledDays`, `stepMinute`, `todayLabel`, `clearLabel`, `panelStyleClass`, `inputFocus` and `inputBlur`
- **Overlays : un panneau ouvert à la destruction de l'application restait affiché en haut à gauche de la page suivante** — symptôme visible en changeant de story dans Storybook (`ui-select`, `ui-menu` popup, `ui-context-menu`, `ui-popover`, `ui-autocomplete`, `ui-input-tags`) : le panneau réapparaissait par-dessus la story suivante, collé au coin haut-gauche, et la story elle-même échouait à s'afficher (`NG0205`). Cause : le panneau portait `[animate.enter]`, qui **inscrit le nœud dans la file d'animations d'Angular** ; cette file est dénouée _via l'injecteur_ quand le nœud disparaît, et l'injecteur est déjà détruit à ce moment-là — `removeAnimationsFromQueue` lève `NG0205` **au milieu de la séquence de destruction**, qui s'interrompt. Tout ce qui suit est sauté, dont le `ngOnDestroy` de `CdkConnectedOverlay` et son `OverlayRef.dispose()` : le host de l'overlay reste dans le document (popover natif jamais refermé), les nœuds du panneau restent dans le pane — le `ViewContainerRef` déjà détruit ne les retrouve plus — et la stratégie de position ayant réinitialisé ses styles, l'ensemble s'affiche à l'origine du viewport. `ui-datepicker`, seul panneau sans `uiMotion`, n'a jamais eu le problème : c'est ce qui a mis sur la piste. Correctif : nouvelle entrée **`motionOnInsert`** sur la directive `uiMotion` — la classe du preset est posée par la directive elle-même, sans passer par `animate.enter` (les presets sont de simples animations CSS, aucun hook framework n'est nécessaire pour jouer une entrée). Les six panneaux d'overlay du kit l'utilisent ; les composants rendus dans une vue normale (`ui-modal`, `ui-drawer`, `ui-sidebar`, `ui-toast`) gardent `animate.enter`/`animate.leave`, qui leur apporte en plus l'animation de sortie. Animation, `motionDisabled`, `data-motion="off"` et `prefers-reduced-motion` sont inchangés (tout est porté par la même classe CSS). Vérifié story par story : panneau ouvert → changement de story → zéro overlay résiduel, zéro erreur console, story suivante rendue
- **Overlays : un panneau ouvert survivait au changement de page et réapparaissait en haut à gauche** — `ui-menu` (popup + sous-menus flottants), `ui-context-menu`, `ui-popover`, `ui-select`, `ui-autocomplete`, `ui-input-tags`, `ui-datepicker` et `uiTooltip` posent leur panneau dans un overlay CDK, donc dans `body`, **hors de la vue routée** : rien ne le détache tant que son composant hôte vit. Un composant déclaré dans un shell persistant (header, sidebar, layout) survivant précisément à la navigation, le panneau restait affiché par-dessus la page suivante — et dès la première re-position (un simple scroll) il se recollait dans le coin **haut-gauche**, son ancre ayant disparu avec la page précédente : un élément détaché mesure `0×0` à l'origine du viewport (reproduit : `x:248,y:68` → `x:0,y:8`). Tous ces composants passent désormais par le primitif partagé **`closeOnNavigation()`** (`src/app/shared/overlay/close-on-navigation.ts`) : il écoute `NavigationStart` et referme le panneau, ce qui couvre `routerLink`, `router.navigate()` et les retours navigateur (un clic extérieur ne fermait que le premier cas). Seuls les **changements de page** ferment : une navigation qui ne réécrit que la query string ou le fragment (filtres, pagination, état deep-link poussé par le panneau lui-même) laisse le panneau ouvert. Sans `Router` dans l'injecteur (kit hors application routée, Storybook), le primitif est inerte. Filet complémentaire pour les deux composants dont l'ancre est **capturée sur l'événement** et peut donc mourir sans eux : `ui-menu` et `ui-popover` referment le panneau au lieu de le re-positionner quand l'ancre n'est plus dans le document (`isConnected`), ce qui couvre aussi la disparition hors navigation (ligne de liste retirée, `@if` fermé). `uiTooltip` en profite pour un cas qui n'avait rien à voir avec le routage : une navigation retire le déclencheur **sans jamais émettre `mouseleave`**, l'infobulle restait donc collée à l'écran. `ui-modal` et `ui-drawer` ne sont pas concernés : ils ne sont pas des overlays CDK et leur visibilité appartient au consommateur (`[(visible)]`)
- **Anneau de focus global : le pointillé `!important` écrasait tous les anneaux du kit** — `_base.scss` posait `*:focus-visible { outline: … dashed 1px !important }` sur **tout** descendant focusable. Le `!important` gagnait contre chaque `outline: none` du kit (une vingtaine de composants qui dessinent leur propre anneau : `ui-button`, `ui-select`, `field-action`, `field-native-input`…), avec deux effets visibles : (1) dans un champ, le pointillé encadrait l'**`<input>`** — donc, l'input n'occupant alors que sa ligne de texte, un rectangle flottant au milieu de la boîte, à l'intérieur de l'anneau légitime dessiné par `.ui-field-box:focus-within` ; (2) ailleurs, il s'empilait sur les anneaux conçus (bordure + halo) et encadrait même la coque des conteneurs focalisés par programme (`ui-modal`, `ui-drawer`, `ui-popover`, `ui-tab-panel`, `ui-step-panel`, `ui-sidebar`), qui posent `outline: none` volontairement. La règle devient **`:where(:focus-visible)`** — spécificité **0**, sans `!important` : n'importe quelle règle de composant la supplante naturellement, et elle reste le filet de sécurité des éléments focusables sans anneau propre (`ui-rating`, `ui-nudger`, `ui-context-menu`…). Ajout d'un `outline-offset` de 2 px pour que le repli se dessine **hors** de l'élément. Un pointillé 1 px était de toute façon un mauvais indicateur au regard de WCAG 1.4.11 / 2.4.11 : les anneaux du kit (bordure 2 px + halo) sont plus lisibles. Vérifié en Storybook headless : sur 12 combinaisons champ × niveau, le contrôle natif n'a plus d'outline et l'anneau est bien celui de la boîte ; `ui-button`/`ui-nudger` gardent leur seul anneau (plus d'empilement) ; `ui-rating` conserve le repli. Les contrôles de sélection (`ui-checkbox`, `ui-radio`, `ui-toggle`) sont inchangés : leur `<input>` natif est en `opacity: 0`, qui masque aussi son outline
- `ui-input` : **le bouton de la zone d'action dessinait une seconde bordure à l'intérieur du champ au focus** — `field-action:focus-visible` posait `box-shadow: inset 0 0 0 2px var(--form-high-stroke-focused)`, soit un rectangle plein-hauteur encadré au ras du bord droit, _dans_ l'anneau que la boîte dessine déjà en `:focus-within` : deux contours emboîtés pour un seul focus. Le focus est désormais **une surface, jamais une bordure** : `background-color: var(--form-high-surface-focused)` + `color: var(--form-high-content-focused)`, rayon suivant déjà le coin droit de la boîte. `box-shadow` sort de la liste de transitions. Mesuré : au repos fond transparent / icône `grey-500`, au focus fond `primary-50` / icône `primary-500`, plus aucune `box-shadow` ni `outline`, anneau de champ présent. **Limite connue** : `--form-high-surface-focused` et `--form-high-surface-hover` pointent sur la même primitive (`primary-50` clair, `grey-800` sombre), focus et survol ne se distinguent donc plus que par la teinte de l'icône (`primary-500` vs `primary-400`) ; et le remplissage seul ne pèse que **1,16 → 1,28:1** contre le fond du champ selon la marque, sous les 3:1 attendus par WCAG 1.4.11 pour un indicateur d'état — c'est l'anneau de la boîte (toujours actif, le bouton étant un descendant) qui porte le contraste. Pour rendre le remplissage autoporteur, retargeter `form.high.surface.focused` sur un cran plus soutenu (`primary-100` / `grey-700`) : changement de token, donc à arbitrer hors de ce lot
- `ui-field` : **focus et survol indiscernables au niveau `default`/`high`** — la boîte routait son état focus vers `--form-high-stroke-hover` (`primary-400`), alors que les niveaux `_success` et `_error` utilisaient bien leur token `stroke-focused`. Survoler et focaliser un champ normal donnaient donc exactement la même bordure. Elle consomme désormais `--form-high-stroke-focused` (`primary-500`), cohérent avec les autres niveaux et avec `ui-select`/`ui-input-tags`, qui référençaient déjà `form-high-stroke-focused` pour leurs anneaux internes
- `ui-badge` : **pastille à un seul caractère non circulaire** — la largeur vaut `contenu + padding`, le `min-width` (= hauteur) n'étant qu'un plancher : dès que le glyphe dépassait `hauteur − 2 × padding`, la pastille s'allongeait en ellipse là où un cercle est attendu (un « 1 » mesurait 24×22 en `small`, 33,6×32 en `large`). Un contenu à **un seul glyphe** — un caractère (compté en points de code : un emoji reste un glyphe) ou une **icône seule** — reçoit désormais le modifier `._single` qui retire le padding horizontal : la hauteur dicte seule la boîte, ratio 1:1 exact à toutes les tailles (22×22 / 28×28 / 32×32), en clair comme en sombre. Aucun impact sur les autres contenus, qui restent des pilules (`99+`, icône + texte). Story `IconOnly` corrigée au passage (`value: undefined`) : les `args` du meta y réinjectaient `1`, elle n'a donc jamais montré le cas icône seule
- `ui-badge` : **texte collé aux extrémités arrondies sur les pilules** — le padding horizontal (`units-sm`, `units-md` en `large`) était pensé pour des coins droits ; avec `radius-full`, la courbe rentre dans la marge et un compteur à plusieurs chiffres (`2222`, `455554`) venait toucher les bords. Padding porté d'un cran : **`units-md`** en `small`/`default`, **`units-lg`** en `large`. Le contenu à un seul glyphe n'est pas concerné (padding nul, cf. entrée précédente), donc les cercles gardent leur diamètre exact ; seules les pilules s'élargissent (`99+` en `default` : 49,5 → 57,5 px)
- `ui-rating`, `ui-slider`, `ui-file-upload` : **`aria-label`/`aria-labelledby` vides** — plusieurs bindings posaient `[attr.aria-label]="ariaLabel()"` (ou `aria-labelledby`) sans repli, contrairement au reste du kit : un input non renseigné émettait un attribut vide (`aria-label=""`) au lieu d'omettre l'attribut ; alignés sur le motif `ariaLabel() || null` déjà utilisé ailleurs (`ui-nudger`…)
- `ui-table` : **sélection par plage en mode `lazy` + `paginator`** — l'ancre du Shift+clic (`anchorRowIndex`, index absolu) n'était jamais réinitialisée au changement de page : une plage démarrée sur une page puis étendue sur une autre indexait `processedData()` (la page serveur courante) avec un offset négatif, produisant une sélection tronquée/fausse au lieu d'une erreur visible ; l'ancre repart désormais du clic courant dès qu'elle pointe hors de la page chargée
- `ui-select`, `ui-input-tags`, `ui-file-upload`, `ui-file-upload-list` : **`{0}` mal substitué dans les libellés a11y/i18n contenant `$`** — `.replace('{0}', valeur)` interprète les séquences `$&`/`` $` ``/`$'` de l'argument de remplacement quand celui-ci est une chaîne utilisateur (label de tag saisi, nom de fichier) ; nouveau helper partagé `formatLabel` (`forms/format-label.ts`, split/join) unifie les 9 sites `{0}` du kit et sort toute logique de formatage des templates (calculée en `computed()`, même motif que `sizeLabel`/`formatFileSize`)
- `ui-select` : **mode `editable` — deux « vides » différents** — vider le champ au clavier commitait `''` alors que la croix commitait `null` ; la saisie vidée commite désormais `null` (même normalisation que `ui-autocomplete` et `clear()`)
- `ui-datepicker` : **a11y du popup** — les attributs `aria-haspopup`/`aria-expanded`/`aria-controls` étaient posés sur l'hôte `<ui-input>` (sans rôle) et n'atteignaient jamais l'input natif focusable : la relation champ ↔ calendrier était invisible aux lecteurs d'écran ; ils sont désormais transmis à l'input natif via les nouveaux inputs ARIA de `ui-input`. Le panel `role="dialog" aria-modal` piège désormais réellement le focus (`cdkTrapFocus`, mode overlay) au lieu de laisser Tab s'échapper derrière le « modal »
- `ui-datepicker` : **navigation clavier en multi-mois** — avec `numberOfMonths > 1`, aucune cellule du calendrier n'était focusable (roving focus limité au mode mono-mois) : la grille était inatteignable au clavier ; le focus itinérant couvre désormais tous les panels (une seule tabulation par date, les cellules `otherMonth` dupliquées étant exclues) et la fenêtre de mois ne se décale que lorsque la date focalisée en sort
- `ui-input-tags` : **structure ARIA invalide** — le conteneur `role="listbox"` englobait aussi l'`<input>` de saisie (combobox), or un listbox ne peut contenir que des `option`/`group` ; le listbox n'enveloppe plus que les tags (wrapper `display: contents`, layout inchangé)
- `ui-slider` : **poignées indistinguables en mode `range`** — les deux poignées partageaient le même `aria-label` ; nouveaux inputs `ariaLabelStart`/`ariaLabelEnd` (repli sur `ariaLabel`) pour nommer distinctement chaque poignée
- `ui-rating` : **`readonly` non appliqué au clavier** — `onNativeChange` (flèches sur le range natif) modifiait la valeur d'un rating `readonly`/`disabled` (le guard n'existait que sur le clic) ; le handler ignore désormais l'événement (et les valeurs `NaN`) et resynchronise le range natif sur le modèle
- `ui-file-upload` : **remplacement impossible en mode single** — une fois un fichier sélectionné (`multiple=false`), toute nouvelle sélection/drop était silencieusement ignorée (il fallait `clear()` d'abord) ; le nouveau fichier **remplace** désormais la sélection courante, comme un `<input type=file>` natif (upload en cours annulé et object URL du fichier remplacé révoquée)
- `ui-datepicker` : **contrôle jamais `touched` sans ouverture du panel** — `emitTouch` n'était appelé qu'à la fermeture du calendrier : focus + Tab sur le champ sans ouvrir ne marquait jamais le contrôle `touched` (styles d'erreur jamais activés, notamment en mode `inline`) ; le blur du trigger marque désormais `touched` quand le focus sort du composant (aligné sur `ui-select`)
- `ui-autocomplete` : **valeur pré-remplie affichée brute avec `optionValue`** — `writeValue` résolvait le label depuis des suggestions encore vides : un formulaire édité affichait l'id au lieu du libellé, sans re-résolution ; le label est désormais re-résolu dès que des suggestions correspondantes arrivent (abandonné dès que l'utilisateur reprend la saisie) ; en outre, vider le champ commite `null` (au lieu de `''`), aligné sur `clear()`
- `BaseControlValueAccessor` : **miroir d'état figé sous Signal Forms** — l'interop fournit un `NgControl` sans flux `events`, l'état (`invalid`/`touched`/`dirty`/`errors`) n'était donc capturé qu'une fois au premier rendu : un champ requis restait par exemple en style erreur (`aria-invalid="true"`) même une fois valide. Les getters de l'`InteropNgControl` lisant les signals du champ, le miroir est désormais rafraîchi dans un `effect()` (chemin classique inchangé : seed + flux `events`)
- `ui-button` : suppression d'un `NG0951` (« Child query result is required but no value is available ») émis à chaque rendu. L'effet de forwarding des `buttonProps` lisait le `viewChild.required('#host')` dans un `effect()` nu, exécuté pendant `runEffectsInView` avant la résolution de la requête de vue (le `#host` est sous `@if/@else`). Basculé en `afterRenderEffect` (browser-only, vue garantie résolue) avec court-circuit quand `buttonProps` est absent : plus d'erreur dans le `ErrorHandler`, comportement de forwarding inchangé
- `ui-table` : **virtual scroll — boucle de défilement infinie** — le _scroll anchoring_ du navigateur combattait le fenêtrage : chaque changement de hauteur de la cale supérieure « ré-ancrait » la position de scroll, ce qui déclenchait un nouvel événement scroll, déplaçait la fenêtre, et ainsi de suite sans fin ; `overflow-anchor: none` sur le viewport en mode `virtualScroll` coupe la boucle (comportement de scroll normal et borné)
- `ui-table` : **colonnes figées × en-tête sticky** — en mode `scrollable`, les `th` non figés (z-index 2, règle plus spécifique) repassaient au-dessus des `th` figés lors du scroll horizontal ; la règle `th._frozen-*` du bloc scrollable rétablit la hiérarchie (cellules figées 1 < en-tête 2 < en-tête figé 3)
- `ui-table` : **interstice des lignes figées** — 1px de contenu défilant transparaissait entre deux lignes sticky : les offsets cumulés arrondissaient au plus proche alors que les hauteurs de lignes sont fractionnaires ; les tops sont désormais **arrondis à l'entier inférieur** (léger chevauchement < 1px, recouvert par la ligne suivante, au lieu d'un jour)
- Storybook (dark) : le recolor typographique global n'écrase plus les couleurs de tokens des composants rendus (canvas + docs)
- Page `Foundations/Shadows` : les aperçus utilisent les tokens d'ombre existants
- Explorateur `Colors` : la marque fonctionne aussi en mode clair (sélecteur `[data-theme='light']`)

### Removed

- Tokens sémantiques `global` sans aucun consommateur dans le kit : `low.content.focused`, `low.surface.focused`, `low.stroke.focused` (les trois teintés `secondary` — un anneau de focus rose n'a jamais servi) et `low.stroke.hover`. Voir l'entrée `Changed` sur la nouvelle nomenclature `global`
- Type `UiSize` (inutilisé)
