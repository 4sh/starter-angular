# Index des composants

> Story & doc **co-localisées** dans le dossier du composant : `projects/ui-kit/<famille>/ui-<nom>/ui-<nom>.stories.ts` + `ui-<nom>.mdx` (package `@4sh/ui-kit`) — la famille est la section de cette page (`actions`, `forms`, …)
> ✅ = implémenté · ⬜ = à construire (recopier le patron `ui-button`)

## actions

- ✅ `ui-button` — Bouton (niveaux high/low/success/warning/error, **variants** filled/outlined/ghost/contrast, tailles, icône, **mode lien** `href`/`routerLink` → rend un `<a>`)
- ⬜ `ui-button-group` — Groupe de boutons
- ✅ `ui-button-split` — Bouton d'action accolé à un déclencheur déroulant (composition `ui-button` × 2 + `ui-menu` popup ; modèle `UiMenuItem[]` : commandes, `routerLink`/`url`, sous-menus, séparateurs, icônes ; niveaux high/low/success/warning/error, **variants** filled/outlined/ghost/contrast (forwardés aux deux boutons), tailles default/small, désactivation indépendante `buttonDisabled`/`menuButtonDisabled`, `aria-haspopup`/`aria-expanded`, clavier hérité du menu)
- ✅ `ui-link` — Lien textuel inline (vrai `<a>`, `href`/`routerLink`/`external`, icônes gauche/droite, tailles, icon-only, souligné a11y)
- ✅ `ui-speed-dial` — Bouton flottant déployant ses actions autour de lui, sans panneau (composition `ui-button` × N, tokens `actions.*` ; inspiré de PrimeNG SpeedDial) ; modèle `UiSpeedDialItem[]` (sous-ensemble feuille de `UiMenuItem` : commandes, `routerLink`/`url`, icônes) ; 4 dispositions partageant un même calcul d'arc — `linear` (8 directions), `circle`, `semi-circle` et `quarter-circle` (`radius`) —, masque optionnel (tokens `ui-modal`), fermeture au clic extérieur/Échap ; `itemLevel` (`low` par défaut) distinct du `level` du déclencheur ; icône du déclencheur pivotante (`rotateAnimation`) ou remplacée (`hideIcon`) ; infobulles optionnelles (`showTooltips`, via `ui-tooltip`) ; `role="menu"` + roving tabindex (une flèche sur le déclencheur fermé l'ouvre et focus la première action) ; variants/tailles forwardés au déclencheur et à chaque action

## forms

- ✅ `ui-field` — Shell partagé des champs (label + boîte + helper + états) via projection
- ✅ `ui-input` — Champ texte (CVA ; label + helper, icônes, unité, zone d'action, niveaux, templates d'icône `#iconLeft`/`#iconRight`)
- ✅ `ui-input-number` — Champ numérique (min/max/step, spinner, clamp, `role="spinbutton"`)
- ✅ `ui-input-mask` — Champ masqué (tokens 9/a/*, littéraux auto, unmask)
- ✅ `ui-input-otp` — Saisie de code à usage unique / OTP (CVA ; rangée de cellules `<input maxlength=1>` backant une **chaîne** jointe, `length` cellules, `mask` (password), `integerOnly` (chiffres, `inputmode=numeric`), tailles `small`/`default`/`large`, avance auto + `Retour arrière` recule + collage réparti, `role="group"` + tabindex roving (`Tab` entre/sort, `←`/`→` naviguent), template `#input` (contexte `value`/`index`/`tabindex`/`events`), events `valueChange`/`completed` ; Signal Forms/Reactive/Template-driven)
- ✅ `ui-input-group` — Groupe champ + compléments (composition `ui-input-group` + `ui-input-group-addon` ; rangée collée en un seul champ : coins arrondis aux seules extrémités, trait partagé fusionné, taille du groupe transmise aux add-ons ; accueille `ui-input`/`ui-input-number`/`ui-select`/`ui-button` posés directement et texte/icône/`ui-checkbox`/`ui-radio` dans un add-on ; mise en forme des enfants **projetés** via les hooks `--ui-field-radius`/`--ui-button-radius`/`--ui-input-group-item-radius`, item focalisé remonté pour garder son anneau de focus visible ; aucun rôle ARIA propre)
- ✅ `ui-select` — Liste déroulante (CVA sur shell `ui-field` ; motif combobox WAI-ARIA `aria-activedescendant`, options primitives/objets + mapping de champs, groupes, `multiple` avec checkmark/checkbox + repli `maxSelectedLabels` (+n autres) + wrap auto-height, chips **retirables** via `#selectedItem` (callback `remove`, `Retour arrière`), `showClear`, filtre intégré, `editable` autocomplete (la frappe ouvre + filtre), `loading`, focus configurable `autoOptionFocus`/`selectOnFocus`/`focusOnHover`, type-ahead, virtual scroll CDK + `lazyLoad`, templates `#item`/`#group`/`#header`/`#footer`/`#empty`)
- ✅ `ui-autocomplete` — Champ de suggestions (CVA sur shell `ui-field` ; motif combobox WAI-ARIA `aria-activedescendant`, `completeMethod` anti-rebondi (`delay`/`minLength`) piloté par le parent, suggestions primitives/objets + mapping de champs, groupes, `dropdown` (`blank`/`current`), `forceSelection`, `showClear`, `loading`, `completeOnFocus`, focus configurable `autoOptionFocus`/`selectOnFocus`/`focusOnHover`, virtual scroll CDK + `lazyLoad`, templates `#item`/`#group`/`#header`/`#footer`/`#empty`)
- ✅ `ui-input-tags` — Saisie multi-valeurs / tags (CVA sur shell `ui-field` ; modèle tableau `string[]`/`T[]`, `Entrée`/`delimiter` pour ajouter, `Retour arrière` retire le dernier, tags rendus par défaut avec `ui-chip` (retirable, `chipLevel`/`chipSubLevel`) ou template `#item`, en `role="option"` dans un `listbox` horizontal + focus roving `←`/`→`/`Suppr`, `max`, `allowDuplicate`, `addOnBlur`/`addOnTab`/`addOnPaste`, `typeahead` (overlay CDK + `completeMethod` anti-rebondi), templates `#item`/`#option`/`#group`, events `tagAdd`/`tagRemove` ; Signal Forms/Reactive/Template-driven)
- ✅ `ui-checkbox` — Case à cocher (CVA, indeterminate, trueValue/falseValue, erreur auto)
- ✅ `ui-radio` — Bouton radio (CVA, groupe natif par name, erreur auto)
- ✅ `ui-label` — Libellé de champ (requis *, tailles, hook `--ui-label-color`)
- ✅ `ui-toggle` — Interrupteur on/off (`role="switch"`, CVA, trueValue/falseValue, erreur auto)
- ✅ `ui-toggle-button` — Bouton à état pressé (CVA ; `<button aria-pressed>` natif ; libellés/icônes par état `onLabel`/`offLabel`/`onIcon`/`offIcon` + repli commun `label`/`icon`, `iconPos`, `trueValue`/`falseValue`, icône seule carrée, `rounded` ; **mode groupe** dès que `options` est renseigné — un bouton par option, `role="group"`, modèle = **tableau des valeurs pressées** (toujours multi : le choix exclusif reste à `ui-segment-control`, qui le dit en `radiogroup`), options primitives/objets + mapping de champs ou forme riche `ToggleButtonOption` (`selectedIcon`, `disabled`, `ariaLabel`), `allowEmpty`, `orientation` horizontal/vertical ; `level` colore l'état pressé, `variant` outlined/filled/ghost dessine l'état relâché, tailles small/default/large, `fluid` ; templates `iconTemplate`/`contentTemplate`/`itemTemplate` ; garde-fou dev sur le nom accessible instable ; erreur auto, tokens `actions.*`)
- ✅ `ui-segment-control` — Sélection segmentée (CVA ; simple/`multiple`, options primitives/objets + mapping de champs, indicateur glissant motion, tailles, `fluid`, `orientation` horizontal/vertical, template de segment, clavier roving radiogroup/group, erreur auto)
- ✅ `ui-datepicker` — Sélecteur de date/heure (CVA, overlay CDK ou inline, min/max, jours désactivés, showTime 12/24h, inline, focus roving clavier, templates `#date`/`#buttonbar`/`#icon`)
- ✅ `ui-nudger` — Incrémenteur numérique `[−] valeur [+]` (CVA ; deux `ui-button` icon-only, min/max/step, bornes dérivées désactivant le bouton, tailles, niveaux, **variants** filled/outlined/ghost/contrast, `formatValue`, valeur en région live)
- ✅ `ui-rating` — Notation étoiles (CVA, clavier natif range, focus)
- ✅ `ui-file-upload` — Téléversement de fichiers (headless ; modes `field`/`drag` + glisser-déposer, `multiple`, validation `accept`/`maxFileSize`/`fileLimit`, `auto`, `customUpload` + `uploadHandler`, upload XHR avec suivi de progression, templates `file`/`content`/`toolbar`, région d'erreurs live, aperçus image révoqués ; compose `ui-file-upload-list`)
- ✅ `ui-file-upload-list` — Ligne de fichier (unité répétée : vignette/icône, nom + taille/état, suppression ; bascule sur `ui-spinner` pendant le téléversement + barre de progression)
- ✅ `ui-textarea` — Zone de texte multiligne (CVA ; label + helper, niveaux/états, `rows`, `maxlength`, `autoResize`, poignée native)
- ✅ `ui-editor` — Éditeur de texte riche (CVA, valeur HTML ; **sans moteur tiers** — zone `contenteditable` + commandes d'édition natives isolées dans `ui-editor-commands` ; barre d'outils configurable `tools` : boutons gras/italique/souligné/listes/**indent/outdent**/lien/**bloc de code**/effacer + **listes déroulantes popup** `blockFormat` (`<select>` natif, Normal, Titre 1-3) et `fontFamily`/`fontSize` (déclencheur `ui-button` + `ui-menu popup [size]="small"`, `fontSize` en opt-in — écarté du défaut car il ferait doublon visuel avec `blockFormat` sans porter la structure) ; **`textColor`/`highlightColor`** en opt-in — chacun ouvre un `ui-swatch-picker` `popup [size]="small"` ancré au bouton, palette réutilisant `DEFAULT_SWATCH_PALETTE` (`ui-swatch-picker`), liseré de couleur active sous l'icône, entrée « Aucune couleur » retirant juste la classe couleur (pas tout le formatage) ; `separator`, `toolbarPosition` haut/bas, roving tabindex `role=toolbar` + `aria-pressed`/`aria-haspopup`+`aria-expanded` recalculés au curseur, raccourcis Ctrl/Cmd+B/I/U ; police/taille/couleur/surlignage fermés aux tokens (`--fontfamily-*`, `--size-typography-text-*`, `--primitives-*`), écrits en **classe** jamais en style en ligne, noms de fontes lus depuis les tokens à l'exécution, curseur mémorisé/restauré pour les popups ; assainissement `DomSanitizer` + collage réduit à une liste blanche de balises et de classes ; `minRows`, `maxlength`/`showCount` mesurés sur le **texte**, placeholder, niveaux/états ; hooks `--ui-editor-*`)
- ✅ `ui-slider` — Curseur de sélection de valeur (CVA ; simple/`range`, `step`, `marks`, écart min. entre poignées, orientation, drag pointeur + clavier WAI-ARIA `role=slider`)
- ✅ `ui-knob` — Cadran circulaire de saisie numérique (CVA ; arc SVG de 300° dans un repère `100 × 100`, le tracé suit donc le diamètre rendu ; `min`/`max`/`step`, origine de l'arc au zéro quand il est dans les bornes, gabarit du libellé central `valueTemplate` (repris en `aria-valuetext`), `strokeWidth` proportionnel, tailles `small`/`default`/`large` + diamètre libre via `--ui-knob-size`, recoloration par instance `valueColor`/`rangeColor`/`textColor` ; rotation pointeur (souris/tactile/stylet, pointer capture) + clavier WAI-ARIA `role=slider` (flèches, Page haut/bas, Début/Fin), `readonly`/`disabled`, erreur auto)
- ✅ `ui-swatch-picker` — Nuancier couleur (grille de pastilles, statique ou `popup` en overlay CDK — mêmes mécanique/contrat de composition qu'`ui-menu`/`ui-button-split`, sans trigger propre : `toggle(event)`/`show(event)`/`hide(focusTrigger?)`) ; `palette` `UiSwatchGroup[]`, défaut `DEFAULT_SWATCH_PALETTE` = grille 7×5 dérivée des tokens `primitives.*` (7 teintes × 5 paliers 900→100) + noir/blanc, jamais un hex en dur ; `value` (two-way, `key` sélectionné ou `null`), `allowClear` (pastille « Aucune couleur »), tailles `default`/`small` ; `role="listbox"`/`option` sur de vrais `<button>`, `aria-selected`, clavier roving **grille 2D** (flèches, Début/Fin), sélection marquée par un liseré `--form-high-stroke-checked` (jamais une opacité)

## informative

- ✅ `ui-alert` — Message **inline** (catégorie _informative_, sibling non-flottant de `ui-toast`) : titre + texte + `<ng-content>`, niveaux `default`/`highlight`/`success`/`warning`/`error` × sous-niveau `high`/`low`, tailles `default`/`large`, icône de tête (nom FA / `false` / défaut du niveau), bouton de fermeture optionnel (`<button>` natif, clavier), **auto-disparition `life` (ms)** avec émission `close`, auto-masquage à la fermeture, racine `role="alert"` (`aria-live=assertive`/`aria-atomic`) + `ariaLabel`/`ariaLabelledBy` ; usages Dynamic (`@for`) et Forms (résumé d'erreurs) ; 100 % tokens `informative.*`
- ✅ `ui-badge` — Pastille compteur / statut
- ✅ `ui-tag` — Étiquette colorée / pilule (level × sub-level × size, icônes gauche/droite, forme pill/rounded, tokens informative)
- ✅ `ui-chip` — Jeton compact **interactif** (relative de `ui-tag` : icône **ou image** de tête façon avatar ; action de suppression `removable` — `<button>` natif, clavier `Entrée`/`Espace`/`Retour`/`Suppr`, output `remove` ; mode **sélectionnable** `selectable` — racine `<button aria-pressed>`, `[(selected)]` two-way, remplissage `high` + icône `selectedIcon` (check) sur sélection, motif choice/filter chip ; contenu projeté libre, `disabled`, level × sub-level × size, forme pill/rounded, `aria-label`/`aria-labelledby`, repli image + `imageError`, garde-fous a11y ; tokens `informative`)
- ✅ `ui-helper` — Texte d'aide sous un champ
- ✅ `ui-read-only` — Affichage **lecture seule** d'un couple label/valeur (présentationnel ; rendu natif `<dl>`/`<dt>`/`<dd>` pour l'association a11y gratuite — conteneur neutre si pas de label ; **libellé via `ui-label` par défaut** (parité typo + écart label→valeur des champs → alignement pixel-perfect à côté d'un `ui-field`), personnalisable (`labelTemplate`, `--ui-label-color`, `required`) ; dispositions `vertical`/`horizontal` avec colonne pilotable `labelWidth` + `labelAlign`, **ou `grid` : contrôle total via `rowClass`/`labelClass`/`valueClass`** injectables (Gridaflex ou classes utilitaires) ; **`matchFieldHeight`** aligne la zone de valeur sur la hauteur d'un champ pour cohabiter avec un `ui-field` ; tailles default/small, `fallback` atténué + **`emptyLabel`** annoncé aux lecteurs d'écran, `multiline`, mode `inline`, valeur riche via `<ng-content>` détecté façon `ui-button` ; 100 % tokens `global.*`/metrics/typography, consomme les `$form-field-*` de `ui-config`)
- ✅ `ui-progress-bar` — Barre de progression (headless ; modes `determinate`/`indeterminate`, `value` 0–100 borné, libellé `right`/`bottom` ou masqué, formateur libre `valueTemplate`, mode `steps` (segments discrets), couleur surchargeable `color`/`--ui-progress-bar-color`, `role="progressbar"` + aria-value*, motion réductible)
- ✅ `ui-avatar` — Avatar utilisateur (image / initiales / icône, tailles, forme, badge projeté, repli image)
- ✅ `ui-avatar-group` — Empilement d'avatars avec chevauchement (débordement « +N » par avatar Label)
- ✅ `ui-tooltip` — Infobulle headless (directive `[uiTooltip]` + CDK Overlay ; placement 4 côtés avec retournement auto `fitContent`, hover/focus/both, délais, `escape` HTML/`TemplateRef`, `autoHide`/`hideOnEscape`/`life`, `tooltipStyleClass`)
- ✅ `ui-separator` — Séparateur visuel (horizontal/vertical, solid/dashed, épaisseur, libellé start/center/end)
- ✅ `ui-accordion` — Accordéon (composition `ui-accordion` + `ui-accordion-panel` ; single/multiple, en-tête bouton WAI-ARIA, clavier roving ↑↓/Début/Fin, `selectOnFocus`, collapse animé grid-rows via motion system, `disabled`, chevron/séparateur optionnels)
- ✅ `ui-empty-state` — État vide (headless ; visuel optionnel `showMedia` = illustration projetée `uiEmptyStateMedia` **ou** raccourci `icon` rendu en grand, titre + description, corps libre projeté, slot d'actions `uiEmptyStateActions` ; tailles default/small, densités via custom properties `--ui-empty-state-media-size/-color`, région nommée `role="region"` sur `ariaLabel`/`ariaLabelledBy`, visuel décoratif, 100 % tokens `global`/metrics/typography)
- ✅ `ui-skeleton` — Placeholder de chargement (headless ; formes `text`/`circle`/`rectangle` avec dimensions par défaut par forme × taille default/small, surcharges CSS brutes `width`/`height`/`borderRadius`, couleur de fond via `--ui-skeleton-background`, animations `wave` (défaut, reflet glissant `--ui-skeleton-shine`)/`pulse`/`none`, `aria-hidden`, `prefers-reduced-motion` + `data-motion="off"` ; tokens `global`/metrics)
- ✅ `ui-spinner` — Indicateur de chargement indéterminé (cercle SVG token-coloré par défaut, marqueur **remplaçable** image/icône FontAwesome/template libre, tailles default/small + `--ui-spinner-size`, `orientation` + libellé, réglages `strokeWidth`/`fill`/`animationDuration`, `delay` anti-flash, `role="status"` live region, reduced-motion)
- ✅ `ui-toast` — Notification éphémère (carte `ui-toast` + `UiToastService` `add`/`clear`/`remove` + `ui-toast-container` ; 7 positions, niveaux `informative`, auto-fermeture pausable au survol/focus, `sticky`, mode bannière `expanded`, contenu `template` custom + actions, entrée/sortie via motion system, région live a11y)

## layout

- ✅ `ui-card` — Conteneur flexible (slots média/titre/sous-titre/contenu/pied via directives marqueurs + raccourcis `header`/`subheader`, variantes outlined/elevated/flat)
- ✅ `ui-modal` — Fenêtre modale headless (two-way `visible`, masque modal / non-modal, `dismissableMask`, blocage du scroll de fond, piège + restauration de focus CDK, `Échap`, déplaçable, agrandissable, redimensionnable, `contained` (embarqué), 9 positions, `breakpoints`, motion `zoom`/fondu, templates en-tête/pied)
- ✅ `ui-drawer` — Panneau latéral glissant headless (two-way `visible`, 4 bords `position` + `fullScreen`, glissement directionnel dérivé de la position via le système de motion, masque modal/non-modal, `dismissableMask`, blocage du scroll, piège + restauration de focus CDK, `Échap`, `contained` (embarqué), templates en-tête/contenu/pied + `#headless`)
- ✅ `ui-popover` — Panneau flottant ancré à un déclencheur headless (overlay CDK connecté, méthodes impératives `show`/`hide`/`toggle` avec ancre via l'événement ou passée en 2ᵉ argument, contenu projeté quelconque ; 4 côtés `position` avec retournement auto + flèche suivant le côté effectif, `role="dialog"` + `aria-modal`, piège de focus CDK + focus du premier focusable/`[autofocus]` à l'ouverture, `Échap` + retour du focus au déclencheur, `aria-expanded`/`aria-controls` reflétés sur le déclencheur, `dismissable` clic extérieur, motion `zoom`, coque partagée `overlay-panel` + tokens `global.*`)

## navigation

- ✅ `ui-breadcrumb` — Fil d'Ariane (modèle déclaratif `UiBreadcrumbItem[]` : `url`/`routerLink`/`command`, éléments rendus en instances `ui-link`, dernier élément `aria-current="page"` ; séparateur string ou template `#separator`, repli `maxItems` derrière un bouton ellipsis accessible qui révèle les éléments masqués, template `#item`, tailles default/small, landmark `nav` + `ol`)
- ✅ `ui-context-menu` — Menu contextuel (clic droit / `triggerEvent` custom sur un `target` ou `global` document, ouvert aux coordonnées du pointeur en overlay CDK avec repli sur les bords du viewport, repositionnement au clic droit répété ; panneau = `ui-menu` embarqué : `UiMenuItem[]` imbriqués en **sous-menus flyout** (cascade latérale, défaut) ou inline, clavier, tokens `navigation.*`, densité `small` par défaut, fermeture au `pointerdown` extérieur ; méthodes `show`/`hide`/`toggle`, templates `#item`/`#submenuheader` transmis, motion `zoom`, focus premier item + retour focus cible à `Échap`)
- ✅ `ui-menu` — Menu de navigation / commandes (modèle déclaratif `UiMenuItem[]` : groupes à en-tête + séparateurs, sous-menus `submenus` inline (repliables `toggleable`, contrôlables via `expandedKeys` two-way) ou **flyout** (panneaux latéraux en cascade), `command`, `routerLink`/`url` ; mode `popup` en overlay CDK ancré au déclencheur, motion `slide-down` + repli grid-rows, clavier roving WAI-ARIA, templates `#item`/`#submenuheader`/`#start`/`#end`, familles `navigation.high`/`low`, densités `size` default/small, panneau borderless à fond `global` opaque, coque partagée `overlay-panel`)
- ✅ `ui-tabs` — Onglets (composition `ui-tabs` + `ui-tab-list`/`ui-tab` + `ui-tab-panels`/`ui-tab-panel` ; two-way `value`, orientation horizontale/verticale, `scrollable` + navigateurs, `lazy` (groupe/panneau, template `#content`), `selectOnFocus`, boutons `role="tab"` natifs + clavier roving WAI-ARIA, indicateur actif glissant motion personnalisable `--ui-tabs-active-bar-*`, panneaux en fondu, mode menu de navigation)
- ✅ `ui-stepper` — Assistant de progression / wizard (composition `ui-stepper` + `ui-step-list`/`ui-step` + `ui-step-panels`/`ui-step-panel`, enveloppe `ui-step-item` en vertical ; two-way `value`, orientations horizontale/verticale, mode `linear` (étapes en avant désactivées, `next()`/`prev()`), `lazy` + template `#content`, `completedIcon`, `icon` par étape ; état à venir/actif/terminé **dérivé** de l'étape active, marqueurs numérotés + connecteurs `navigation.*`/`informative.*`, en-têtes `<button role="tab">` natifs `Tab`/`Entrée`/`Espace`, panneaux `role="tabpanel"` en fondu motion, mode « étapes seules » sans panneaux)
- ✅ `ui-sidebar` — Barre latérale applicative (chrome de navigation ; composition `ui-sidebar` + `ui-sidebar-menu` + directive `[uiSidebarTrigger]` ; bord `side` left/right, présentation **statique** (dans le flux, pousse le contenu, repli en **rail d'icônes** `collapsed` two-way, `openOnHover` flottant) ou **overlay** offcanvas (`visible` two-way, `backdrop`, `dismissable`, `Échap`, piège de focus CDK, blocage scroll, `contained`) ; **responsive** — bascule auto en overlay sous `breakpoint` (échelle `$breakpoint-*`) ; slots `#header`/`#footer` + corps projeté ; `ui-sidebar-menu` = modèle déclaratif `UiSidebarMenuItem[]` rendu en landmark `<nav>` + `role=list`/`listitem`, **sections** groupées + **groupes repliables** `toggleable` (sous-arbres profonds grid-rows, `expandedKeys` two-way), **suivi d'état actif** (RouterLink / `active`, auto-dépliage du groupe actif), `badge`, repli rail piloté par DI ; état replié résolu partagé aux descendants par injection ; tokens `navigation.*`/`global.*`)
- ✅ `ui-bottom-tab-bar` — Barre de navigation basse tactile (mobile / tablette ; composition `ui-bottom-tab-bar` + `ui-bottom-tab` + `ui-bottom-tab-action` ; landmark `<nav>` + `aria-current="page"` (et non le patron `role="tab"`, sans panneau associé), two-way `value`, `<button>` ou `<a>` natif (`href`/`routerLink`), bascule d'icône outline → solid sur l'item actif (`activeIcon`/`activeIconType`), `showLabels` pour une barre en icônes seules, contenu projeté en couche d'ornement (badge de notif) sans interception du tap, action circulaire surélevée `level` `actions.*` ; **terrain mobile** — `safeArea` (`env(safe-area-inset-bottom)`), cibles ≥ 56px, `touch-action: manipulation`, pas de flash de tap natif, `user-select: none`, `position: fixed` ou `contained`, masquée à l'impression ; clavier `Tab` complet + flèches / Début / Fin, ripple par défaut, tokens `navigation.high.*`/`actions.*`/`global.*`)

## table

- ✅ `ui-table` — Tableau de données (headless ; markup des lignes projeté via templates `#header`/`#body`/`#footer`/`#caption`/`#expandedrow`/`#emptymessage` ; tailles default/small/large, `showGridlines`, `stripedRows` ; tri simple (cycle asc/desc/aucun)/multiple (Ctrl/Cmd)/presort/`customSort` via `uiSortableColumn` + `ui-table-sort-icon` ; sélection single/multiple/`metaKeySelection` + plage Maj, checkbox (`ui-table-checkbox`/`ui-table-header-checkbox`), radio (`ui-table-radio`), clavier ↑↓/Espace/Entrée/Maj+flèche, events `rowSelect`/`rowUnselect` ; pagination intégrée (`paginator`, `rows`, two-way `first`, `rowsPerPageOptions`, composée de `ui-button`/`ui-select`) ; scroll vertical sticky `scrollHeight` (+ `'flex'`), horizontal ; colonnes figées `uiFrozenColumn`/`alignFrozen` multiples, lignes figées `frozenValue` ; expansion `uiRowToggler` + `expandedRowKeys` ; resize colonnes mode fit `uiResizableColumn` ; **réordonnancement de lignes** drag & drop `uiReorderableRow` + `rowReorder` ; **column toggle** par templating ; **`loading`** masque + `ui-spinner` ; **virtual scroll** fenêtré `virtualScroll`/`virtualScrollItemSize` + mode `lazy`/`lazyLoad` ; **mode lazy serveur** (`lazy` + `totalRecords` : `value` = page courante, refetch sur `pageChange`/`sortChange`) ; état vide = instance `ui-empty-state` ; clavier complet (roving tabindex, ↑↓/Début/Fin/Espace/Entrée/Maj+flèche/Maj+Espace/Ctrl+A/Ctrl+Maj+Début/Fin) ; tokens `table.*` + `informative.highlight*`)
- ✅ `ui-paginator` — Pagination autonome (miroir Figma `_paginator-button-*` : `<button>` natifs token-stylés, carrés arrondis 40px `--radius-default`, contrôles `actions.low`, page courante `actions.high` + `aria-current="page"`, icônes `ui-icon`, `ui-select` lignes/page (`rowsPerPageOptions` = booléen Figma `selectPage`) ; two-way `first`/`rows`, `totalRecords`, fenêtre `pageLinks`, **mode compact `ellipsis`** (`1 2 3 … 30 31 32`, `boundaryCount`), **rapport de page** `showCurrentPageReport`/`currentPageReportTemplate`, barre modulable `showFirstLastIcon`/`showPageLinks`, **templates** `#start`/`#end`/`#report` (contenus embarqués, contexte état de pagination) + `#firsticon`/`#previcon`/`#nexticon`/`#lasticon`/`#pagelink`, `disabled`, output `pageChange`, labels a11y surchargeables ; consommée par `ui-table`)

## root

- ✅ `ui-icon` — Icône (size sm|md|default|lg|xl, type solid|outline, accessible) ; n'importe quelle police via `provideUiIconFamilies()` — FontAwesome par défaut, `family` par icône, directive `uiIconFamily` par sous-arbre (icônes internes des composants comprises, overlays CDK inclus)
- ✅ `ui-image` — Image (deux sources : `name` asset local résolu thème/marque via assets-map, ou `src` URL distante prioritaire via `NgOptimizedImage` ; SVG local inliné (`httpResource` + cache partagé), SVG distant jamais inliné ; `fallback` local sur erreur + placeholder tokenisé + output `loadFailed` ; `fill`/`width`/`height`, `priority` LCP)
