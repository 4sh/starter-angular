# Angular
.angular
dist

# Dependencies
node_modules

# Environment / Secrets
.env
*.local

# Design Tokens (temp / generated)
.temp-tokens
.tokenflow

# Generated Tokens (npm run tokens:build)
projects/ui-kit/styles/generated

# Copie servie par Storybook pour le téléchargement (regénérée par docs:config)
storybook/public/component-vars.scss

# Compodoc (generated for Storybook)
documentation.json

# Storybook doc config manifest (npm run docs:config)
storybook/generated

# Storybook build output
storybook-static

# Test coverage
coverage

# Cache
.cache
.storybook-cache

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local editor / tool configs
.vscode
# Ignore local Claude config everywhere (incl. nested settings.local.json),
# but track the shared project assets at the repo root.
.claude
!/.claude
/.claude/*
!/.claude/skills/
!/.claude/hooks/
!/.claude/rules/
!/.claude/agents/
!/.claude/settings.json

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# npm pack output (local package testing)
*.tgz
