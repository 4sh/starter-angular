import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Package npm" />

# Package npm — `@4sh/ui-kit`

Les composants de ce Design System sont publiés sous forme d'**un seul package
npm**, consommable par n'importe quelle application Angular.

```bash
npm install @4sh/ui-kit
```

<div className="sb-callout">

**Pas de risque d'oubli d'installation.** `@4sh/ui-kit` est un **package unique**
avec des *secondary entry points* — pas une collection de packages séparés. Tous
les entry points sont présents dès `npm install`, et les dépendances entre eux
se résolvent automatiquement : importer `UiDatepicker` tire tout ce dont il a
besoin (son socle de formulaire, ses icônes…) sans que vous ayez quoi que ce
soit à déclarer.

</div>

```ts
import { UiDatepicker } from '@4sh/ui-kit/forms/ui-datepicker';

@Component({
  selector: 'app-booking',
  imports: [UiDatepicker],
  template: `<ui-datepicker label="Date" valueType="date" [(ngModel)]="date" />`,
})
export class Booking { date = signal<Date | null>(null); }
```

Le nom de l'entry point est celui du composant : la page de doc de chaque
composant, dans la barre latérale, donne l'import exact à recopier.

---

## Ou : posséder les sources (mode *starter*)

`npm install` livre les composants **compilés**. Le second mode de consommation
les installe en **sources**, dans votre dépôt, libres d'être modifiées :

```bash
ng add @4sh/ui-kit-schematics                    # fondation + composants, en une commande
ng add @4sh/ui-kit-schematics --with-storybook   # avec la doc des composants copiés
ng generate @4sh/ui-kit-schematics:update        # diff par composant après une release du kit
```

Le parcours passe par le package compagnon, et `@4sh/ui-kit` n'entre jamais dans
`node_modules` : absent, rien ne peut importer son code compilé à la place de vos
copies.

Le choix entre les deux — ce qui arrive dans le dépôt, les imports, les styles, la
personnalisation, les mises à jour — est comparé dans le README du package,
ci-dessous. La documentation des composants, elle, est la même dans les deux cas :
c'est ce Storybook.

---

## Le mode d'emploi complet vit dans le README du package

Installation, liste des entry points, feuille de styles à charger, providers
attendus par `ui-image` et `BrandService`, construction de votre propre champ
sur `@4sh/ui-kit/forms` : tout est décrit **une seule fois**, dans le README
livré avec le package — c'est aussi ce qu'affiche la page npmjs.

| | |
|---|---|
| Page du package | <a href="https://www.npmjs.com/package/@4sh/ui-kit" target="_blank" rel="noopener noreferrer">npmjs.com/package/@4sh/ui-kit</a> |
| README (anglais) | <a href="https://github.com/4sh/starter-angular/blob/main/projects/ui-kit/README.md" target="_blank" rel="noopener noreferrer">projects/ui-kit/README.md</a> |
| README (français) | <a href="https://github.com/4sh/starter-angular/blob/main/projects/ui-kit/README.fr.md" target="_blank" rel="noopener noreferrer">projects/ui-kit/README.fr.md</a> |
| Dépôt | <a href="https://github.com/4sh/starter-angular" target="_blank" rel="noopener noreferrer">github.com/4sh/starter-angular</a> |

<div className="sb-callout">

**Pourquoi un simple renvoi ?** Ces informations étaient auparavant recopiées
ici, dans le README anglais et dans le README français. Trois copies d'une même
page d'installation divergent — c'est ainsi que le nombre d'entry points annoncé
avait fini par être faux à deux endroits sur trois. La page publique du package
fait foi ; cette page ne garde que ce qui lui est propre.

</div>
