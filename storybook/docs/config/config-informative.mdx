import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Components/configuration/Informative" />

# Configuration: Informative category

Constants of the **informative** family (`$avatar-*`). Avatar diameters (no matching
`--size-components-*` tier) and group overlap, shared by `ui-avatar`
and `ui-avatar-group`.

> Source file: `projects/ui-kit/styles/settings/_ui-config.scss` · exposed via `@use 'utils'`.
> Consumed by: `ui-avatar`, `ui-avatar-group`. Back to
> [how it works & summary](?path=/docs/components-configuration--docs).

<table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 14 }}>
  <thead>
    <tr style={{ textAlign: 'left', borderBottom: '2px solid rgba(128,128,128,.25)' }}>
      <th style={{ padding: 8, width: 280 }}>Variable</th>
      <th style={{ padding: 8 }}>Role</th>
      <th style={{ padding: 8 }}>Default</th>
      <th style={{ padding: 8 }}>Value</th>
    </tr>
  </thead>
  <tbody>
  <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><code>$avatar-size-tiny</code></td>
      <td style={{ padding: 8 }}>Diameter of the tiny-size avatar</td>
      <td style={{ padding: 8 }}>raw px (avatar-specific)</td>
      <td style={{ padding: 8 }}>34px</td>
  </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><code>$avatar-size-small</code></td>
      <td style={{ padding: 8 }}>Diameter of the small-size avatar</td>
      <td style={{ padding: 8 }}>raw px (avatar-specific)</td>
      <td style={{ padding: 8 }}>42px</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><code>$avatar-size-default</code></td>
      <td style={{ padding: 8 }}>Diameter of the default-size avatar</td>
      <td style={{ padding: 8 }}>raw px (avatar-specific)</td>
      <td style={{ padding: 8 }}>56px</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><code>$avatar-size-large</code></td>
      <td style={{ padding: 8 }}>Diameter of the large-size avatar</td>
      <td style={{ padding: 8 }}>raw px (avatar-specific)</td>
      <td style={{ padding: 8 }}>64px</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><code>$avatar-square-radius</code></td>
      <td style={{ padding: 8 }}>Border radius in <code>square</code> shape</td>
      <td style={{ padding: 8 }}><code>var(--radius-md)</code></td>
      <td style={{ padding: 8 }}>:</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><code>$avatar-group-overlap</code></td>
      <td style={{ padding: 8 }}>Overlap between stacked avatars (<code>ui-avatar-group</code>)</td>
      <td style={{ padding: 8 }}><code>var(--units-md)</code></td>
      <td style={{ padding: 8 }}>12px</td>
    </tr>
  </tbody>
</table>
