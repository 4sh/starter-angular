import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Components/configuration/Actions" />

# Configuration: Actions category

Constants of the **actions** family (`$action-*`). Inherits from the global, overridable
for all buttons / actions at once.

> Source file: `projects/ui-kit/styles/settings/_ui-config.scss` · exposed via `@use 'utils'`.
> Consumed by: `ui-button`. Back to
> [how it works & summary](?path=/docs/components-configuration--docs).

<table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 14 }}>
  <thead>
    <tr style={{ textAlign: 'left', borderBottom: '2px solid rgba(128,128,128,.25)' }}>
      <th style={{ padding: 8, width: 280 }}>Variable</th>
      <th style={{ padding: 8 }}>Role</th>
      <th style={{ padding: 8 }}>Default</th>
      <th style={{ padding: 8 }}>Value</th>
    </tr>
  </thead>
  <tbody>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><code>$action-focus-ring-width</code></td>
      <td style={{ padding: 8 }}>Focus ring of buttons / actions</td>
      <td style={{ padding: 8 }}><code>$focus-ring-width</code> (global)</td>
      <td style={{ padding: 8 }}>2px</td>
    </tr>
  </tbody>
</table>
