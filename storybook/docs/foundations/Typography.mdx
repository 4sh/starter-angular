import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundations/Typography" />

# Typography

Utility class system based on the tokens (automatic **responsive** sizes).
Definition: `projects/ui-kit/styles/base/_typography.scss`.

## Font families

Here are the fonts configured via the design tokens. Each family has a precise role in the project's visual identity.

<div className="sb-unstyled">
  <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))', gap: '20px', margin: '20px 0' }}>

    {/* Title Font */}
    <div style={{ padding: '24px', border: '1px solid var(--sb-border)', borderRadius: '12px', background: 'var(--sb-bg)' }}>
      <div style={{ fontSize: '12px', color: 'var(--sb-text-subtle)', textTransform: 'uppercase', letterSpacing: '1px', marginBottom: '8px' }}>Titles & Headings</div>
      <div style={{ fontSize: '32px', fontFamily: 'var(--fontfamily-title)', fontWeight: 700, lineHeight: 1, marginBottom: '12px', color: 'var(--sb-text)' }}>
        DM Sans
      </div>
      <div style={{ fontSize: '14px', color: 'var(--sb-text-subtle)', fontFamily: 'var(--fontfamily-title)' }}>
        ABCDEFGHIJKLMN<br/>
        abcdefghijklmn<br/>
        1234567890
      </div>
      <div style={{ marginTop: '16px', fontSize: '12px', background: 'var(--sb-bg-subtle)', padding: '4px 8px', borderRadius: '4px', display: 'inline-block' }}>
        <code style={{ background: 'transparent', border: 'none' }}>--fontfamily-title</code>
      </div>
    </div>

    {/* Base Font */}
    <div style={{ padding: '24px', border: '1px solid var(--sb-border)', borderRadius: '12px', background: 'var(--sb-bg)' }}>
      <div style={{ fontSize: '12px', color: 'var(--sb-text-subtle)', textTransform: 'uppercase', letterSpacing: '1px', marginBottom: '8px' }}>Body text & UI</div>
      <div style={{ fontSize: '32px', fontFamily: 'var(--fontfamily-base)', fontWeight: 400, lineHeight: 1, marginBottom: '12px', color: 'var(--sb-text)' }}>
        Inter
      </div>
      <div style={{ fontSize: '14px', color: 'var(--sb-text-subtle)', fontFamily: 'var(--fontfamily-base)' }}>
        ABCDEFGHIJKLMN<br/>
        abcdefghijklmn<br/>
        1234567890
      </div>
      <div style={{ marginTop: '16px', fontSize: '12px', background: 'var(--sb-bg-subtle)', padding: '4px 8px', borderRadius: '4px', display: 'inline-block' }}>
        <code style={{ background: 'transparent', border: 'none' }}>--fontfamily-base</code>
      </div>
    </div>

    {/* Monospace Font */}
    <div style={{ padding: '24px', border: '1px solid var(--sb-border)', borderRadius: '12px', background: 'var(--sb-bg)' }}>
      <div style={{ fontSize: '12px', color: 'var(--sb-text-subtle)', textTransform: 'uppercase', letterSpacing: '1px', marginBottom: '8px' }}>Code & Technical</div>
      <div style={{ fontSize: '32px', fontFamily: 'var(--fontfamily-monospace)', fontWeight: 400, lineHeight: 1, marginBottom: '12px', color: 'var(--sb-text)' }}>
        Roboto Mono
      </div>
      <div style={{ fontSize: '14px', color: 'var(--sb-text-subtle)', fontFamily: 'var(--fontfamily-monospace)' }}>
        ABCDEFGHIJKLMN<br/>
        abcdefghijklmn<br/>
        1234567890
      </div>
      <div style={{ marginTop: '16px', fontSize: '12px', background: 'var(--sb-bg-subtle)', padding: '4px 8px', borderRadius: '4px', display: 'inline-block' }}>
        <code style={{ background: 'transparent', border: 'none' }}>--fontfamily-monospace</code>
      </div>
    </div>

  </div>
</div>

---

<table className="doc-table">
  <thead>
    <tr>
      <th>Family</th>
      <th>Classes</th>
      <th>Sizes</th>
    </tr>
  </thead>
  <tbody>
    <tr><td>Titles</td><td><code>.title-&#123;size&#125;</code></td><td>sm · md · default · lg · xl · 2xl</td></tr>
    <tr><td>Text</td><td><code>.text-&#123;weight&#125;-&#123;size&#125;</code></td><td>weight: light · regular · bold · italic : size: sm · md · default · lg · xl</td></tr>
    <tr><td>Accent</td><td><code>.accent-&#123;size&#125;</code></td><td>sm · default · lg</td></tr>
  </tbody>
</table>

export const Row = ({ cls, sample }) => (
  <div style={{ display: 'flex', alignItems: 'baseline', gap: 20, padding: '6px 0', borderBottom: '1px solid var(--sb-border,#eee)' }}>
    <code style={{ fontSize: 12, minWidth: 180, flexShrink: 0 }}>{cls}</code>
    <span className={`sb-unstyled ${cls.slice(1)}`}>{sample || 'The quick brown fox : 0123'}</span>
  </div>
);

## Titles

export const TITLES = ['sm','md','default','lg','xl','2xl'];

{TITLES.map((s) => <Row key={s} cls={`.title-${s}`} sample="Section title" />)}

## Text

export const TWEIGHTS = ['light','regular','bold','italic'];
export const TSIZES = ['sm','md','default','lg','xl'];

{TWEIGHTS.map((w) => (
  <div key={w}>
    <h4 style={{ marginTop: 24, textTransform: 'capitalize' }}>{w}</h4>
    {TSIZES.map((s) => <Row key={s} cls={`.text-${w}-${s}`} />)}
  </div>
))}

## Accent

export const ASIZES = ['sm','default','lg'];

{ASIZES.map((s) => <Row key={s} cls={`.accent-${s}`} sample="Accent text" />)}
