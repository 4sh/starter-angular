import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Components/Configuration" />

# Configuration des styles composants

Cette page explique **comment fonctionne** la configuration structurelle des composants `ui-*`,
et sert de **sommaire** vers les groupes de variables et les sections de configuration de chaque
composant.

À ne pas confondre avec les deux autres leviers de configuration du design system :

<table style={{ width: '100%', borderCollapse: 'collapse' }}>
  <thead>
    <tr style={{ textAlign: 'left', borderBottom: '2px solid rgba(128,128,128,.25)' }}>
      <th style={{ padding: 8 }}>Axe</th>
      <th style={{ padding: 8 }}>Quoi</th>
      <th style={{ padding: 8 }}>Quand</th>
      <th style={{ padding: 8 }}>Où</th>
    </tr>
  </thead>
  <tbody>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><strong>Theming runtime</strong></td>
      <td style={{ padding: 8 }}>Couleurs (marque, clair/sombre), typo, espacements : via variables CSS</td>
      <td style={{ padding: 8 }}>À l'exécution (attributs sur <code>&lt;html&gt;</code>)</td>
      <td style={{ padding: 8 }}>Design tokens · <code>ThemeService</code> / <code>BrandService</code></td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><strong>Structure (ui-config)</strong></td>
      <td style={{ padding: 8 }}>Choix mutualisé d'un token pour un rôle : focus ring, bordure, taille des contrôles, transition</td>
      <td style={{ padding: 8 }}>À la compilation (SASS)</td>
      <td style={{ padding: 8 }}><code>src/styles/src/settings/_ui-config.scss</code></td>
    </tr>
  </tbody>
</table>

> Pour le theming runtime, voir **Spécifications → Thème & Système de Tokens** et
> **Foundations → Colors**. Cette page documente l'axe **structure**.

<br/>
<br/>

## Principe : 3 niveaux, une variable locale par composant

`_ui-config.scss` (exposé par <code>@use 'utils'</code>) déclare des constantes à 3 portées,
de la plus large à la plus étroite :

```
GLOBAL UI  ──►  CATÉGORIE  ──►  COMPOSANT (variable locale)
(tous)          (forms /        (un seul .scss)
                 actions /
                 informative)
```

- Chaque variable de **catégorie** hérite par défaut de la valeur **globale**.
- Chaque **composant** lit la valeur de catégorie dans une **variable locale**, en tête de
  son `.scss`. C'est ce qui préserve l'indépendance : ajuster un composant = changer sa
  variable locale, sans impacter les autres.

```scss
// Dans ui-radio.scss : la valeur commune arrive via une variable LOCALE :
$focus-ring-width: utils.$form-focus-ring-width; // ← remplacer ici = ajuster CE composant
```

<br/>
<br/>

## Sommaire : groupes de variables partagées

Le référentiel des constantes **mutualisées** (`_ui-config.scss`) est découpé par groupe. Chaque
page liste les variables, leur rôle et leur valeur par défaut.

<table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 14 }}>
  <thead>
    <tr style={{ textAlign: 'left', borderBottom: '2px solid rgba(128,128,128,.25)' }}>
      <th style={{ padding: 8, width: 220 }}>Groupe</th>
      <th style={{ padding: 8 }}>Préfixe</th>
      <th style={{ padding: 8 }}>Portée</th>
    </tr>
  </thead>
  <tbody>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><a href="?path=/docs/components-configuration-global-ui--docs"><strong>Global UI</strong></a></td>
      <td style={{ padding: 8 }}><code>$focus-ring-width</code>, <code>$control-*</code></td>
      <td style={{ padding: 8 }}>Tous les composants interactifs</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><a href="?path=/docs/components-configuration-forms--docs"><strong>Forms</strong></a></td>
      <td style={{ padding: 8 }}><code>$form-*</code></td>
      <td style={{ padding: 8 }}>checkbox, radio, toggle, field (input / mask / number)</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><a href="?path=/docs/components-configuration-actions--docs"><strong>Actions</strong></a></td>
      <td style={{ padding: 8 }}><code>$action-*</code></td>
      <td style={{ padding: 8 }}>button</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><a href="?path=/docs/components-configuration-informative--docs"><strong>Informative</strong></a></td>
      <td style={{ padding: 8 }}><code>$avatar-*</code></td>
      <td style={{ padding: 8 }}>avatar, avatar-group</td>
    </tr>
  </tbody>
</table>

<br/>

## Sommaire : configuration par composant

Les variables **locales** de chaque composant (dont celles qui pointent vers `ui-config` =
points d'ajustement per-composant) sont documentées dans une section **« Configuration (SCSS) »**
sur la doc du composant :

<table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 14 }}>
  <thead>
    <tr style={{ textAlign: 'left', borderBottom: '2px solid rgba(128,128,128,.25)' }}>
      <th style={{ padding: 8, width: 220 }}>Composant</th>
      <th style={{ padding: 8 }}>Groupe(s) consommé(s)</th>
    </tr>
  </thead>
  <tbody>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><a href="?path=/docs/components-ui-actions-ui-button--docs"><code>ui-button</code></a></td>
      <td style={{ padding: 8 }}>Actions + local (<code>$sizes</code>, <code>$levels</code>)</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><a href="?path=/docs/components-ui-forms-ui-checkbox--docs"><code>ui-checkbox</code></a></td>
      <td style={{ padding: 8 }}>Forms</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><a href="?path=/docs/components-ui-forms-ui-radio--docs"><code>ui-radio</code></a></td>
      <td style={{ padding: 8 }}>Forms + local (<code>$dot-size</code>)</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><a href="?path=/docs/components-ui-forms-ui-toggle--docs"><code>ui-toggle</code></a></td>
      <td style={{ padding: 8 }}>Forms + local (<code>$sizes</code>)</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><a href="?path=/docs/components-ui-forms-ui-input--docs"><code>ui-input</code></a> · <code>ui-input-mask</code> · <code>ui-input-number</code></td>
      <td style={{ padding: 8 }}>Forms (via le shell <code>ui-field</code>)</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><a href="?path=/docs/components-ui-informative-ui-avatar--docs"><code>ui-avatar</code></a> · <code>ui-avatar-group</code></td>
      <td style={{ padding: 8 }}>Informative</td>
    </tr>
  </tbody>
</table>

<br/>
<br/>

## Comment surcharger

<table style={{ width: '100%', borderCollapse: 'collapse', fontSize: 14 }}>
  <thead>
    <tr style={{ textAlign: 'left', borderBottom: '2px solid rgba(128,128,128,.25)' }}>
      <th style={{ padding: 8 }}>Objectif</th>
      <th style={{ padding: 8 }}>Où intervenir</th>
    </tr>
  </thead>
  <tbody>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><strong>Tout le kit</strong> (ex : focus ring de 2px → 3px partout)</td>
      <td style={{ padding: 8 }}>Éditer la valeur globale dans <code>_ui-config.scss</code>. Tous les composants re-<code>@use</code> ce fichier, ils suivent.</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><strong>Une catégorie</strong> (ex : contrôles de formulaire plus grands)</td>
      <td style={{ padding: 8 }}>Éditer la variable `$form-*` (ou `$action-*` / `$avatar-*`) dans <code>_ui-config.scss</code>.</td>
    </tr>
    <tr style={{ borderBottom: '1px solid rgba(128,128,128,.15)' }}>
      <td style={{ padding: 8 }}><strong>Un seul composant</strong> (ex : anneau plus épais sur le radio uniquement)</td>
      <td style={{ padding: 8 }}>Remplacer la valeur de la variable locale en tête du <code>.scss</code> du composant.</td>
    </tr>
  </tbody>
</table>

> Deux leviers, c'est tout. Les composants font <code>@use 'utils'</code> (qui expose
> `ui-config`) et lisent chaque valeur dans une variable **locale** ; éditer `_ui-config.scss`
> se répercute donc partout, et remplacer la variable locale n'affecte que le composant.

<br/>
<br/>

## Mixin `control-transition()`

Applique la recette de transition mutualisée à une liste de propriétés (durée + easing pris
dans `ui-config`), en une déclaration :

```scss
@use 'utils';

.ui-checkbox-box {
  @include utils.control-transition(background-color, border-color, box-shadow);
}
// → transition: background-color 80ms ease-in-out, border-color 80ms ease-in-out, box-shadow 80ms ease-in-out;
```

Changer la durée/courbe pour tous les composants = éditer `$control-transition-duration` /
`$control-transition-easing` dans `ui-config`.

<br/>
<br/>

## Gouvernance : quand créer une constante partagée

- Valeur utilisée par **≥ 2 composants** → la remonter dans `ui-config` (niveau **catégorie**
  si propre à une famille `forms`/`actions`/`informative`, **global** sinon), et l'ajouter à la
  page de groupe correspondante ci-dessus.
- Valeur d'un **seul** composant → elle reste **locale** à son `.scss` (documentée dans la
  section « Configuration (SCSS) » de sa doc).
- `ui-config` porte des **choix de tokens** (structure). Les couleurs thémables restent des
  **design tokens** runtime : **jamais** de couleur ici.

<br/>

> **Recherche Storybook** : la barre de recherche indexe les **titres** de l'arborescence
> (nom / chemin), pas le contenu des pages. Pour retrouver une variable, cherchez son **groupe**
> (`Global UI`, `Forms`, `Actions`, `Informative`) ou le **composant** qui la consomme
> (`ui-radio`, `ui-avatar`…), puis sa section « Configuration (SCSS) ».
